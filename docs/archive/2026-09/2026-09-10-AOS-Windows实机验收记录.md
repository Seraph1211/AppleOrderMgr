# AOS Windows 实机验收记录

> 状态：历史归档（Windows 单机合成验收）
>
> 最近核对：2026-09-10
>
> 基线：`codex/aos-file-ingestion` 未提交工作树，起点 `main@d052678`
>
> 适用范围：用户授权的 Windows 测试机、独立合成目录和隔离 API／PostgreSQL；不代表真实抢购软件或生产业务验收。

本次承接[本地实现与验证记录](2026-09-10-AOS本地实现与验证记录.md)。该历史记录保留当时尚未安装的结论，本报告记录后续实机证据。

## 环境和授权边界

用户确认测试机为 Windows 10 专业版 22H2、内部版本 19045.6456、x64，并明确允许继续 RDP 证书提示、安装运行本次自行编译的采集器及合成验收脚本。测试使用管理员安装、LocalService 后台运行；没有操作正在运行的真实 AOS 软件，也没有修改防火墙或系统防护。

Windows 安装副本位于 `Downloads\package`，程序安装到 `C:\Program Files\AppleOrderMgr\AosCollector`。本次 SHA-256 与此前制品一致：`cb47069d5348e4a7d14337aec596045f3824776c06266a2567d3165a8f6780d0`；Windows 校验输出 `PACKAGE_SHA256_OK`。包为自包含、未签名的 1.0.0 win-x64 测试制品。

仅采集 `C:\AosCollectorTest20260910\watched` 内合成文件，使用虚构联系人、账号和订单号；真实附件只在前期离线解析，未上传。服务器使用新建的 `aos_windows_test_20260910` 数据库并执行完整正式 Migration。临时 API 只挂载设备协议与 AOS 执行器，没有启动邮件或官网爬虫 Worker。临时 HTTPS 隧道验证：授权设备 context 返回 200、缺少凭证返回 401，订单管理与来源设置路由返回 404。

## 已完成的实机检查

Windows 日志汇总为 26 项 PASS、0 项 TEST_FAILED，另完成安装向导和 GUI 目录／配置操作。

| 场景 | 实测结果 |
| --- | --- |
| 安装与首次启动 | 安装向导完成，后台服务运行，GUI 通过命名管道显示等待配置 |
| GUI 本机目录 | 添加、移除、文件夹树选择通过；最终只有独立测试目录 |
| 配置与 HTTPS | 测试脚本经采集器自身 test/save 接口保存，随后 GUI 留空保留凭证的连接测试、目录权限和保存均通过；界面显示已连接 |
| 初始完整文件与重复行 | 两条唯一新订单成功，重复行没有重复建单 |
| 追加与未完成尾行 | 完整新行入库，未完成尾行保持等待；补齐后只增加一单 |
| 整文件重写 | 调整行序并重写后，订单数量不增加 |
| 服务重启 | 停服务期间追加一条记录，重启后自动入库；原队列和配置可读取 |
| 设备禁用与恢复 | 禁用时服务器维持 9 单，本地待上传保留；启用后自动补到 10 单，无重复 |
| 来源切换与补录 | 邮件模式下 1 条新记录为 `ready/source_disabled`，订单维持 10 单；切回 AOS 后自动补到 11 单 |
| 接口断连与离线重启 | 暂停独立 API 容器模拟连接中断，本地保留待上传；断连期间重启服务后仍保留，恢复 API 后自动补到 12 单 |
| 单机 100 单突发 | 一次追加 100 个新单，到采集器看到全部入库的计数约 18 秒；服务器从首条接收到最后处理完成约 1.535 秒（不含采集发现和网络传输）；累计 112 个唯一订单；本地待上传、上传异常及服务器待人工均为 0 |
| 已存在订单只留痕 | 同单号变更密码和 TAG 再写入，新增 1 条 duplicate 记录及来源关系；原订单密码和 TAG 均保持原值 |
| 服务与登录任务配置 | 服务为 Running／Auto／LocalService，登录托盘任务为 Ready；未以此冒充真实系统重启或重新登录测试 |
| 本机权限与加密 | 数据目录关闭 ACL 继承，仅 Administrators、SYSTEM、LocalService；DPAPI 配置解密回读正确；命名管道配置响应不返回凭证明文 |
| 队列密文与重启 | 停服务后扫描配置及 SQLite 文件，未发现合成密码或测试设备凭证明文；重新启动后仍为 112 单 |
| 同版本修复安装 | 从本地 Downloads 副本执行安装／修复，配置摘要不变，历史计数保持 112 单 |
| 卸载保留与重装 | 卸载后服务和登录任务不存在，config.enc 与 queue.sqlite 摘要均与停服基线一致；重装后读取原设备配置和历史计数 |
| 服务异常退出 | 接口断连并有待上传时强制结束采集器服务进程，SCM 自动拉起新进程；队列仍保留，恢复接口后自动补到 113 单 |
| 关闭 GUI | 关闭窗口后后台继续；其后异常退出与补传测试在 GUI 隐藏时完成 |

远程资源管理器刷新时列表逐步重排，最初先运行了重写准备脚本，形成 4 条合成基线。随后连续文件套件使用另一组 5 个订单号；没有删除历史测试记录或把基线误计为连续场景的新发现。第一组文件场景结束时服务器为 9 单、9 个唯一单号。

远程键盘／剪贴板曾丢失特殊字符，导致首次凭证输入返回 401；改用本机脚本传入正确测试值后成功，未放宽设备认证或证书校验。GUI 的目录操作和已有凭证保留、测试、保存单独验证；不能把自动化输入问题描述为正常用户粘贴验收通过。

## 服务端核对

最终合成数据为 113 个订单、114 条 AOS 记录和 114 条来源关系，其中 1 条为 duplicate；旧密码和 TAG 保留断言均为 true。113 个订单均为 pending，没有从 AOS 内容推断已支付；生成 113 个首次刷新任务，但没有运行官网 Worker。隔离库付款分配保持默认关闭，付款任务为 0，不将此测试当作付款业务验收。AOS 当天补录接口返回 completed，设备扫描 completed、待人工和待处理均为 0；切走的邮件补录显示 superseded。数据库原始 payload／draft 的合成密码明文检查为 0 行。

## 业务边界

本次未修改业务源码，沿用此前通过本地回归的同一 SHA-256 制品。没有重启整台电脑，避免影响其已有业务程序；开机／重新登录触发、真实 AOS 写文件方式、3–4 台同时工作、跨自然日实机等待、真实断电、跨版本升级、真实官网同步和付款闭环仍需另行验收。

## 测试状态与证据保留

测试结束后已卸载 Windows 采集服务和登录托盘任务，关闭本次测试控制台并将剪贴板替换为无敏感内容。合成设备已禁用，公网设备 context 验证返回 403；随后关闭临时 HTTPS 隧道并停止、删除唯一临时 API 容器，3012 端口无监听。隔离数据库保留 113 单／114 条记录，启用设备为 0；本地开发库来源仍为 email。未提交、推送或改动生产。

Windows 日志保存在 `C:\AosCollectorTest20260910`：`file-cases.log`、`recovery-cases.log`、`platform-checks.log`、`installer-cases.log`、`crash-cases.log`、`cleanup.log`。本地汇总证据保存在 `/tmp/aos-windows-final-assertions.json`、`/tmp/aos-windows-backfill-evidence.jsonl`、`/tmp/aos-windows-server-evidence.jsonl`，临时文件可能被系统清理；以上表格保留当次结论。

卸载后，将仅属于本次合成设备的 `%ProgramData%\AppleOrderMgr\AosCollector` 移至 `C:\AosCollectorTest20260910\collector-data-archive`，config.enc／queue.sqlite 移动前后摘要一致；避免下次正式安装继续绑定已失效的临时服务器和设备。安装副本及程序文件保留。本次 RDP 只读文件夹重定向已移除，Mac 临时传输目录和测试凭证明文已删除；未保存临时 TextEdit 文稿。清理文件夹设置后，客户端重连停留在配置阶段，已取消此次重连并返回连接中心；没有重启或注销 Windows。

本次最终 `docs:check` 通过：69 份文档全部可达、29 个模型字段覆盖通过；`git diff --check` 通过。本阶段只有验收文档变更，未重复执行没有变更的业务源码全量测试；前期回归范围见关联的本地验证记录。
