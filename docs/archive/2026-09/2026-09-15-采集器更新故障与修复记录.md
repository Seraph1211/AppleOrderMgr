# 采集器更新故障与修复记录

> 日期：2026-09-15
> 状态：全部11台已升级1.2.3并回报自动更新成功；一键退出、轮询不自启及恢复已通过用户协助实机验收

## 现象与根因

监控功能提交5f8ad2f后，生产制品20260915T054202Z-75cffef5692e于13:44:43切换。正式迁移34→35，备份恢复、迁移和行数对照通过。11台1.1.0设备领取1.2.0升级后，均因TRAY_STILL_RUNNING失败，旧采集服务保持运行。旧SYSTEM更新器使用EnumWindows发送退出消息，不能枚举用户交互桌面中的GUI窗口。另发现更新Mutex跨await后可能在不同线程释放，固定AosUpdater副本不会随主程序更新。

批量请求旧发布接口时，完整118MB EXE通过readFile读入内存，触及768MB容器上限。API在13:48及13:49发生2次OOM自动重启；临时调整到2GB后恢复。临时上限仅属于原容器，不是持久Compose配置。

## 修复与技术验证

修复提交7e534b7：本机管理员／SYSTEM受限命名管道按GUI进程请求退出；忙碌时拒绝，不强杀配置写入；旧版首次由同一交互会话运行新安装程序兼容窗口消息。更新Mutex在同一线程取得与释放，刷新固定更新组件保留信任公钥。窗口底部和托盘新增“一键退出（停止服务）”，确认SCM停止后退出GUI，失败留窗；手动停止期间不领取自动升级任务，Windows原开机启动设置不变。

服务端流式SHA-256，合并相同文件身份的在途校验，不缓存已完成成功结果。20项签名和发布校验测试通过，包括20并发禁止整体EXE读取、同长度篡改、软链接、截断和失败恢复；服务行覆盖75%。采集器Core73项通过，Windows自包含x64编译0警告0错误。实际Windows服务／管道／退出行为不能由macOS编译或Core测试代替。

生产宿主机隔离768MB、禁用网络容器，20并发校验真实1.2.1签名118MB制品通过，采样RSS峰值71622656字节（约68.3MiB），结束37580800字节。压力检查使用新运行镜像，不连接业务数据库。

## 生产发布

制品20260915T060317Z-952a9da57b6d（源码7e534b7），北京时间14:07:18切换前端、API与两个Worker；本次无新增Migration。停写备份目录`/var/www/apple-order-mgr/backups/collector-fix-20260915/`，final.dump SHA-256为`f81077abb4ba9458558fa344efd48a573440717883f313d9e924d08795df31eb`；迁移35、订单37、用户13、设备11。原容器inspect保存在同目录。新API恢复768MB，readiness内外网正常、重启数0；共享Nginx、PostgreSQL、其他项目和app.env未改动。

1.2.1制品已通过原公钥验签及文件校验，EXE SHA-256为`0d330a099665f0ca2bbd4927773be25924789ed8cbc624123f6144fa3a2f9b1d`。完整本地修复包在`collector/artifacts/AOS采集器-1.2.1-修复版.zip`，含许可证、签名、公钥、管理员引导脚本和说明；配置摘要在引导安装前后核对，主程序和固定更新组件须匹配同一EXE摘要。安装只使用既有设备和信任根，不重注册或导入日志。

## Windows实机卡点与下一步

已选中CN 01远程桌面，但自动化坐标点击返回noWindowsAvailable、键盘输入无响应；AX菜单与截图可读。切换前台、退出全屏和重取窗口后仍无法输入。已请求用户将CN 01置前并点击桌面以恢复交互，未凭截图宣称安装成功。首台1.2.1任务于14:09:13进入installing，尚未回报成功；CN 01最后业务心跳为13:50:10（早于本次1.2.1下发），其余10台1.1.0心跳正常，流量样本仍0。需要现场确认SCM与更新恢复日志，不能将installing当作已升级。旧更新器预计仍需同会话引导，完成首台后应验证新版GUI保持打开时自动更新、一次一键退出／等待轮询／重启，然后逐台更新其余10台并核对心跳版本及流量样本。

## 回退与未完成项

服务端若需回退，原制品为20260915T054202Z-75cffef5692e，回退会重新引入制品整体读取问题，需暂停批量升级并临时保留2GB上限；不执行数据库down，不删除新增监控表。采集器保留主程序previous及原配置和队列；不得用旧队列备份覆盖升级期间新数据。

该阶段尚未完成的Windows引导与跨会话自动更新，已于下文1.2.3在No.1完成。一键退出已在下文完成实机验收；其他10台更新已于下文完成，当前剩余日志目录／规则和代理计费对照。

## US Server No.1 实机验证（用户指定）

用户将验证目标改为US Server No.1，设备UUID为`8fe535d7-3bdf-4ea6-87c1-d2aba82a10b3`，云服务器实例为`i-rj91m8vav5ehaar4nxbv`。通过云助手核对设备UUID、签名EXE与既有公钥摘要后，在原Administrator交互会话执行修复安装。云助手任务`t-usw6x47sbuwfjeo`于14:40:34成功：主程序和固定更新组件均1.2.1且SHA-256一致，配置文件摘要未变，服务Running，临时引导任务已移除。生产已收到1.2.1心跳及流量数据。

托盘保持运行时两次平台回归（`9a345df0-7fbe-4610-bbda-c2a29271df4c`、`350437df-28bd-4878-b71f-391fe3e5ab84`）仍返回`LEGACY_TRAY_SESSION`。通过SYSTEM只读管道连接检查成功，再独立发送退出协议，回报`READ=1 ACK=1`且GUI进程正常退出，随后恢复托盘。定位C#内存读写重载返回`ValueTask`，直接`GetAwaiter().GetResult()`不保证等待尚未完成的I/O。1.2.2将读写转换为Task后同步等待；连接超时、ACL和忙碌拒绝保持。

截至本节记录，1.2.1安装、配置保持及真实流量上传通过，平台自动更新与一键退出仍不能标记验收通过；1.2.2构建和实机回归进行中。用户已打开No.1远程桌面，但全屏画面仍无法由自动化读取，已请求改为普通窗口。未操作抢购软件、重启服务器或更换凭证。

### 1.2.2平台切换发现权限问题

平台任务`64e0c50a-4cf5-4565-b4ae-02f1d2e312c9`切换期间LocalService未能启动。14:55:46只读检查显示服务Stopped、主程序已回退1.2.1，而更新恢复日志仍在switched阶段。候选文件来自仅管理员/SYSTEM可读的Updater目录，File.Move将其ACL带入安装目录；回退覆盖内容也未恢复原ACL。14:57:15以安装目录内原有`.previous`文件ACL恢复主程序权限，服务回到Running。1.2.3改为安装目录内暂存、显式保留现有程序ACL再原子替换；需要先修复固定更新组件，才能验证该逻辑。

新增测试链接真实TrayControl客户端，覆盖延迟成功回执与忙碌拒绝，共75项通过。macOS管道实现未复现Windows原ValueTask提前读取行为，因此该测试不是Windows故障的替代验收；Windows实际跨会话更新仍待最终修复版验证。

### 1.2.3签名发布与首台修复安装

提交`45b2587`包含权限保留和真实管道客户端回归；退出等待修复提交为`34c64a5`。75项测试、Windows x64自包含构建及文档检查通过。1.2.3 EXE SHA-256为`6bc7d34c6232565e62b7fe8dd00b96f028958b1544d3ecc3c155d8b7c623042f`，继续使用原公钥签名，发布到`/var/www/apple-order-mgr/shared/collector-updates/releases/1.2.3`。未重建API或Worker。修复包`collector/artifacts/AOS采集器-1.2.3-修复版.zip` SHA-256为`3fb5c9ccec3917705da157bd30ad88a6735ab93374a5c6e79b47dbd8d9dd455b`。

旧更新器恢复日志于14:58:02正常回报rolled_back，服务1.2.1恢复心跳。15:00临时暂停旧更新计划任务，使用设备原有认证从本系统下载1.2.3受信包，拒绝重定向并核对EXE／原公钥摘要，再由原Administrator交互会话完成修复。云助手任务`t-usw6x49wdbcht6o`于15:04:31回报成功：主程序和更新组件1.2.3且摘要匹配、配置未变、服务Running；临时安装任务已移除，原自动更新与托盘任务恢复。

平台任务`2720fdfa-eb93-4dc9-b3cf-d5ad30f19641`于15:05:05重新自动领取，开始在托盘运行状态下执行同版修复回归。此处安装完成不等同此回归已成功，一键退出仍待图形界面验收。

### 1.2.3最终自动更新回归通过

平台任务`2720fdfa-eb93-4dc9-b3cf-d5ad30f19641`于15:05:36回报succeeded。15:07:17云助手只读验收确认：服务Running（session 0）、托盘新进程运行于Administrator session 1，主程序和固定更新组件1.2.3摘要均匹配，自动更新任务Ready且USER=SYSTEM，托盘任务Running，安装文件保留原有读取ACL。证明托盘运行中的跨会话退出、程序替换、服务健康恢复与托盘恢复链路已完成。

生产网站按No.1筛选，显示已接收21个采样区间，下载522.17MB、上传42.48MB，总计564.65MB；明确显示21/909分钟及存在未覆盖时段。此数值包括本轮多次更新下载和远程桌面，不能当作全天代理用量。已有设备身份与配置摘要保持，真实日志规则与账单比对未验收。

图形控制仍无法稳定切换到No.1，已请求用户在No.1点击“一键退出（停止服务）”，等待用户动作后核对SCM／进程及轮询期间不自启，再恢复服务；没有将管道退出测试冒充按钮验收。其他10台未在本轮升级为1.2.3。


### 一键退出与恢复实机验收

用户确认在No.1点击“一键退出（停止服务）”。云助手任务`t-usw6x4aw9y3durk`于15:15:14核对SCM为Stopped、AosCollector进程数量0，自动更新任务仍Ready且正常执行。任务`t-usw6x4az3t9tb0g`于15:16:07再次确认Stopped／0进程；显式触发原自动更新计划任务并等待，15:16:16轮询返回0，服务仍Stopped且无采集器进程，证明手动退出后不会被该轮询重新启动。

随后按验证安排恢复原服务和托盘：15:16:22服务Running，服务进程PID9288位于session 0，托盘PID12056位于原Administrator session 1，两者版本1.2.3.0；生产收到15:16:18的新1.2.3心跳，并在15:17:35核对到恢复后的15:17:17流量采样，总采样数由24增至25。没有重启Windows或操作抢购软件。一键退出按钮由用户实际点击，服务停止、进程退出、轮询不自启及恢复由云助手和生产数据联合核验；其他10台仍未在本轮升级。


### 其余设备批量升级（用户继续授权）

15:27开始核对：US Server No.2～No.10在线且版本1.1.0；CN Server 01最后心跳13:50。美国九台云助手预检任务`t-usw6x4c65mnuj9c`全部成功，按设备UUID限定目标后暂停旧更新器；通过正式升级服务创建九个1.2.3任务，以各自设备原认证下载对应受信包，原Administrator交互会话执行修复安装。任务`t-usw6x4cb4lqfu2o`九台全部退出码0，逐台核对主程序／固定更新组件摘要和原配置摘要，移除临时安装任务并恢复原托盘任务。

九个正式自动更新任务全部succeeded，生产均收到1.2.3心跳与真实流量。云助手最终检查`t-usw6x4cr5rxtg5c`九台全部成功：服务Running、主程序及固定更新组件摘要匹配、交互托盘存在、自动更新任务启用、临时安装任务已移除。包含先前No.1，美国10台升级完成。

CN Server 01对应杭州实例`i-bp1hi3u3w5p2lolvtmo8`。诊断任务`t-hz06x4ciotctslc`确认主程序1.1.0、SCM Stopped、无采集器进程，主程序ACL仅SYSTEM／管理员可读，旧1.2.1任务恢复日志停在switched。使用安装目录原`AosCollector.exe.previous`的读取ACL恢复主程序，任务`t-hz06x4cm1h29dds`于15:34:29成功启动服务，旧任务于15:34:30正式回报rolled_back／UPDATE_RECOVERED。临时暂停旧更新器后创建1.2.3任务`a736256f-b045-46f7-af48-4e0462ea8a6a`，继续修复安装。

CN修复安装任务`t-hz06x4cu81c7g8w`于15:37:34成功，主程序／固定更新组件均匹配1.2.3签名摘要，配置摘要未变、服务Running、临时安装任务移除。正式1.2.3任务于15:38:15回报succeeded。15:38:39生产核对11台版本均1.2.3、心跳新鲜、11个正式更新任务全部成功；API健康且重启数仍0。此次未重建或重启API／Worker，未执行数据库迁移，未重启Windows或改动抢购软件。

15:39:27确认CN首条恢复后流量采样为15:39:15，随后生产网站已显示全部11台流量列表及各自覆盖分钟数。CN初次验收脚本错误地要求成功上报后仍保留journal.json；代码按设计会删除该文件，平台任务已成功。去除这一错误检查后，最终只读验收任务`t-hz06x4d40qqkb9c`于15:40:05成功：服务Running、两份程序摘要一致、交互托盘1个、更新任务Ready、临时安装任务已移除。当前采集服务全部恢复运行；采样包含本次安装下载和远程桌面，尚不足全天代理账单对照。
