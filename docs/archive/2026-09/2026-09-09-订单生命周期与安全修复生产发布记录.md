# 订单生命周期与安全修复生产发布记录

> 状态：历史归档
> 日期：2026-09-09（北京时间）；发布 ID 使用 UTC
> 范围：本地提交／普通推送、精确制品、生产备份演练／迁移／切换及技术检查
> 结论：生产部署与技术检查通过；不代替独立测试、人工或完整业务验收

## 授权与版本

用户明确授权继续 commit、push 和生产部署，同时明确暂不强制改写远程分支、暂不轮换外部凭据。本次未改变该边界，未启用爬虫、代理或自动刷新。

| 项目                   | 证据                                                                                     |
| ---------------------- | ---------------------------------------------------------------------------------------- |
| 分支                   | 本地 main，构建时工作区干净                                                              |
| 代码提交               | `61499d04351ea1af28501433759503249514aa07`                                               |
| tree                   | `0356bfe7d0b76fb21079d63560846e5db98a1255`                                               |
| 普通推送               | 远程 main 从 `0fd7f80` 快进至 `61499d0`；没有 force                                      |
| 发布 ID                | `20260908T173057Z-0356bfe7d0b7`                                                          |
| 旧发布                 | `20260906T092727Z-81340dbf509e`，保留                                                    |
| 制品平台               | `linux/amd64`，Node 20.20.2，运行／迁移镜像均核对                                        |
| Node 基础镜像          | `node:20-alpine@sha256:fb4cd12c85ee03686f6af5362a0b0d56d50c58a04632e6c0fb8363f609372293` |
| images.tar.gz SHA-256  | `e00802fab4b0ff5e066eccb807796d76b079b44489dded5f076ffb9183a7d126`                       |
| 运行镜像 config digest | `sha256:40db589a7291da933f8cec6ae1e481aac77b696d6fee23a464dda6fd7ab88aa1`                |
| 迁移镜像 config digest | `sha256:d739ae3e859742e9467f09917c70a396c01c994359cab0b19cc2f12498bada21`                |

生产目录为 `/var/www/apple-order-mgr/releases/20260908T173057Z-0356bfe7d0b7`，`current` 原子切换至此目录。发布后补充的文档不在上述制品代码提交中，另行提交，不混淆制品来源。

## 文件与实现范围

代码提交共 75 个文件、4,460 行新增／1,868 行删除。完整清单可复现：`git show --stat --oneline 61499d0`、`git show --name-status 61499d0`。包括：

- 订单生命周期、官网权威数据和来源冲突提示、付款任务联动及相关 Migration、测试与文档，详见[生命周期自测](2026-09-09-订单生命周期改造开发自测.md)。
- 依赖升级、官方 SheetJS 固定制品及校验、Excel 回归与原型继承键修复、构建上下文与 CI，详见[依赖风险收尾](2026-09-09-依赖风险收尾与构建回归.md)。
- 保留并提交用户现有改动；本发布轮没有另改业务逻辑。后续仅新增本记录、历史索引、开发进度、安全记录及部署指南的非交互执行说明。

## 提交与构建门禁

本地复核使用不加载真实 .env 的独立 Docker 容器，不重装现有开发服务依赖：

| 检查             | 命令／结果                                                                                                         |
| ---------------- | ------------------------------------------------------------------------------------------------------------------ |
| 后端测试         | `npm test -- --runInBand --coverageReporters=text-summary`：39 套件、283 项通过，3 套件／41 项环境门禁跳过，退出 0 |
| Lint             | 前后端 `npm run lint -- --quiet` 均退出 0；不代表既有 warning 消失                                                 |
| 文档             | `npm run docs:check`：提交前 54/54 文档、19 模型；发布后记录复核 55/55 文档、19 模型，均退出 0                      |
| 依赖审计         | 前后端 `npm audit --audit-level=low` 均零漏洞，退出 0                                                              |
| 当前树秘密扫描   | Gitleaks 扫描 348 个非忽略普通文件，零命中，退出 0                                                                 |
| 提交区间秘密扫描 | `git /repo --log-opts='origin/main..HEAD' --redact --no-banner`，9 个提交零命中，退出 0                            |
| 格式差异         | `git diff --check`、`git diff --cached --check` 退出 0                                                             |
| 构建             | 指定上述 `PRODUCTION_NODE_IMAGE` 执行 `./scripts/buildProductionRelease.sh`，退出 0                                |
| 制品完整性       | 本地 `shasum -a 256 -c SHA256SUMS`、服务器 `sha256sum -c SHA256SUMS` 全部通过                                      |

[GitHub CI 34257553332](https://github.com/Seraph1211/AppleOrderMgr/actions/runs/34257553332) 的 backend、frontend、secret-scan 全部 success。Secret Scan 日志明确为本次 9 个提交的区间扫描，并非全历史。没有降低规则或新增忽略项。前端保留 891.23 kB 主 JS 包警告。

## 备份与隔离演练

初次备份：`/var/backups/apple-order-mgr/20260908T173057Z-preflight/`；最终停机备份：`/var/backups/apple-order-mgr/20260908T173057Z-final/`。目录权限 700，数据库与配套配置归档权限 600，均仅保存在服务器，未下载真实数据。

- 使用宿主机 `pg_dump 15.18 -Fc` 完整备份 `apple_order_mgr`，包含可恢复的对象定义和数据；配置包配套保存当时的 app.env、Nginx 站点、旧 release.env 与 Compose，另保存旧容器 inspect。
- 初次 database.dump SHA-256：`29f2e16f808ba08fd4d45996f00d9e60fd139cca2595d7c180411907333ce06a`。
- 最终 database.dump SHA-256：`039543cabd418e3e5a21e4f162dc66b008e846c759e500cfce106e70bd9a5652`。
- 在服务器创建精确命名的 internal Docker 网络、无宿主机端口、内存文件系统数据库；使用 PostgreSQL 15.19 Debian 镜像 `postgres:15-bookworm@sha256:5d1d70e254e3c5d7d76847a9deebb18478cd518df37abf6b278d4bdb1fe5d96c`。只在此隔离网络使用临时 trust 认证，不改变生产认证。
- 恢复使用 `pg_restore --no-owner --role apple_user --exit-on-error`，目标仅 `apple_order_release_rehearsal`。迁移容器显式覆盖 DB_HOST、DB_NAME、DATABASE_URL 和 DB_PASSWORD，不能访问生产数据库或公网。
- 初次与最终备份均恢复并执行 8 项 Migration，13 → 21；初次另演练 `scripts/migrateSensitiveData.js --execute` 并再次校验。生产敏感字段此前已加密，本次生产没有重复执行转换。
- `/var/backups/apple-order-mgr/20260908T173057Z-preflight/verifyDatabase.js` 保存不含秘密的复核脚本：核心行数、1,312 个敏感字段解密、30 个身份证盲索引与末四位、Migration 数量、关键唯一／普通索引、官网非空字段、付款调度默认关闭及 100 条刷新计划均通过。
- 演练结束清理专用 b/c 容器、内存库及 internal 网络，未清理共享资源。临时克隆数据不再保留，可从以上备份恢复；旧发布及备份均保留。

## 生产切换和数据变化

北京时间 01:35:40 停止旧 Apple API／邮件 Worker，最终备份与再次隔离恢复通过后执行生产迁移；01:36:18 完成新 API／邮件 Worker 启动和 current 原子切换，窗口约 38 秒。

实际执行的新增 Migration：

1. `20260907000001-create-order-refresh-queue.js`
2. `20260907000002-enhance-email-processing.js`
3. `20260907000003-add-proxy-provider-switch-state.js`
4. `20260907000004-create-access-control-and-payment-tasks.js`
5. `20260908000001-add-official-order-created-at.js`
6. `20260908000002-add-fanproxy-tunnel-provider.js`
7. `20260908000003-add-yiyou-http-provider.js`
8. `20260908000004-add-official-order-lifecycle.js`

生产迁移日志保存于最终备份目录 `production-migration.log`。迁移后启动前再次复核：542 个 Apple ID、30 个取机人、100 个订单、4 条邮件日志、1 个用户与备份一致，21 项 Migration 完整，加密数据可读。

邮件 Worker 保留原配置：IMAP_MARK_SEEN 未显式设置，代码默认 true；来源白名单已配置 1 项。启动回查新增 12 条 ignored／ack succeeded 的非订单审计记录，未保留正文；邮件日志由 4 变 16，订单仍为 100，付款任务为 0。这是本轮实际生产写入与邮箱已读确认，不能写成“生产数据完全未变化”。

## 发布后技术证据

- API healthy、端口仅 `127.0.0.1:3001`；回环和 HTTPS `/api/health/ready` 均通过。
- HTTPS 首页 SHA-256 `e804dc1c888b27466d9d3ad82c5aba2b2ab9a1223a0c9dd3b1d9d65743cd579b`，JS SHA-256 `d0cbc93ee09b312b422a8cc22df28c59b19d4d1a4b9ad6818cd14cfcf9422606`，与本地制品一致。
- 未认证 `/api/orders` 返回 401；没有借用或伪造 Token 绕过真实登录验收。
- 邮件心跳 is_connected=true、consecutive_failures=0、last_error_code=null；受检新容器日志为 9 条 info，未发现 error/warn。
- app.env 与 Nginx 站点文件散列保持不变。`nginx -t` 通过，配置相同而未 reload；Nginx 和 `postgresql-15.service` 的启动时间及其他容器身份／启动时间均保持不变。
- PROXY_ENABLED=false、AUTO_ORDER_REFRESH_ENABLED=false，crawler-worker 未启动；手动刷新任务也需要后续明确启用 Worker 才能执行。

## 中途失败与处理

- 新 Alpine PostgreSQL 15 镜像在服务器初始化时写 postmaster.pid／WAL 返回 Operation not permitted，退出 1，尚未恢复任何业务数据；未修改宿主机安全策略。换用同主版本 Debian 镜像恢复与迁移通过，不能据此认定具体内核或镜像缺陷根因。
- 一次配置探针错误地把模块导出对象当 config，退出 1；改为解构 `{ config, validateConfig }` 后通过。业务代码未因此改动。
- 通过 SSH 标准输入传脚本时，Compose migrate 消费了后续输入；当次只完成迁移。通过单独命令重新确认迁移结果、启动服务并切换指针，没有重复执行生产迁移。后续非交互脚本规则已同步部署指南。

## 结论和未验证边界

本地开发自测、远程 CI、制品与备份、生产迁移及服务技术检查通过。未执行生产账号登录、登录后的只读业务 API、人工 UI、新订单邮件入库、Apple 官网／代理或真实付款验收；保留为后续验收，不把健康接口当成业务完成证据。

未强推远程、未轮换外部凭据，历史敏感资产风险按用户决定延期，详见[安全处置记录](../../security/敏感资产处置记录.md)。没有测试环境部署、开发数据库迁移、生产凭据修改、共享服务操作或无关资源清理。

回滚如需要，须停止新应用、使用经过恢复验证的最终备份配套配置恢复，再切回保留的旧版本；本次没有实际回滚，不默认执行 migrate undo，并须评估切换后邮件等写入窗口。
