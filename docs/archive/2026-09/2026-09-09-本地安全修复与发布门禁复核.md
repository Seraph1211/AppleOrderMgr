# 本地安全修复与发布门禁复核

> 日期：2026-09-09；性质：Coding Agent 开发自测与只读安全核查，不是独立测试或生产验收。
> 基线：main@57859e7 经批准局部历史脱敏后为 main@405ed22；原生命周期工作区保留，未新增工作区交付提交。

## 一、范围和实际文件

本轮执行用户批准的 A1/A2 本地归档及未推送历史修复、A3 调查与方案、B1～B10 中不需要另行选择来源或替代库的兼容修复。未替换 IMAP 库、未修改 xlsx 来源、未升级前端依赖、未修改业务 API 或数据库结构。

| 文件                                                   | 用途                                             |
| ------------------------------------------------------ | ------------------------------------------------ |
| `docs/archive/2026-07/开发进度记录.md`                 | 代理 URL、ID、Key 改为占位符，同步重建未推送历史 |
| `docs/archive/2026-09/整理前接口设计.md`               | Token 示例改为明确占位符，同步重建未推送历史     |
| `package.json`、`package-lock.json`                    | 兼容依赖更新及作用域受限的 overrides             |
| `test/dependencyCompatibility.test.js`                 | 新增 10 项真实依赖调用的兼容回归                 |
| 本报告、历史索引、开发进度、优化计划、敏感资产处置记录 | 证据、部分完成状态、治理方案与剩余门禁           |

没有删除项目文件；原生命周期改造文件属于前一阶段工作，详见[生命周期自测报告](2026-09-09-订单生命周期改造开发自测.md)。

## 二、历史处理证据

- 远程基线 `0fd7f80584aca4f69ebfb087262629bcbab4ecc2`，本地 8 个线性未推送提交重建后 HEAD 为 `405ed22e3af2f5b09cc2ada4cfe2ac4219560b80`。保留提交作者、说明、顺序与其他树内容，仅两份归档及必然产生的父提交哈希变化。
- 操作前后比较其他工作区文件 SHA-256；逐提交比较树路径，均仅涉及两份归档。激活后暂存区为空。未使用 reset/checkout、未覆盖既有工作区。
- 官方 Gitleaks 镜像固定为 `ghcr.io/gitleaks/gitleaks@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f`。新 8 提交区间扫描退出 0、零命中。
- 当前工作区按 Git 已跟踪及未忽略文件清单，将 343 个现存普通文件流式送入 Gitleaks `stdin --redact`，退出 0、零命中；未扫描被忽略的真实 `.env` 或导入文件，不代表那些本地资产已被处置。
- 对已发布 origin/main 历史只读扫描退出 1、3 处命中；全历史和外部凭据仍未闭环，未改写远程、未强推、未轮换。旧本地对象保留在 reflog，未新增备份分支。
- 历史定位（不是有效文档入口）：`f1ed27f` 的 `docs/design/API设计.md:50`，以及 `ff69d52` 的 `docs/development/DEVELOPMENT_PROGRESS.md:215/216`。
- 脱敏扫描报告、重建脚本和哈希映射留在本机临时证据目录 `/tmp/apple-order-security.YJSdQX`，不是持久交付或敏感资产加密备份。报告不记录秘密原文。

复扫（仓库作为只读挂载）：

```bash
docker run --rm -v /Users/seraph/GitHouse/AppleOrderMgr:/repo:ro \
  ghcr.io/gitleaks/gitleaks@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f \
  git /repo --log-opts='0fd7f80584aca4f69ebfb087262629bcbab4ecc2..405ed22e3af2f5b09cc2ada4cfe2ac4219560b80' \
  --redact --no-banner
```

## 三、依赖决策

| 项目 | 结果                                                                                                                 |
| ---- | -------------------------------------------------------------------------------------------------------------------- |
| B1   | mailparser 3.9.23 → html-to-text 10.0.1 → deepmerge-ts 8.0.2，由上游兼容版本选择子依赖                               |
| B2   | ip-address 10.7.0；确认 socks 使用路径，回归前导零 IPv4 与映射 IPv6                                                  |
| B3   | 保留 node-imap 0.9.6／utf7；utf7 下 semver 固定 5.7.2，仅用于 Node 版本与 Buffer 分支判断                            |
| B4   | undici 7.29.1；沿用现有官网、代理及错误重试测试，另测本地 HTTP JSON                                                  |
| B5   | xlsx 仍为 0.18.5，具体官方来源方案待确认，审计未通过                                                                 |
| B6   | 保留 Express 4.22.2，body-parser 1.20.8；Express 下 qs 固定 6.16.0，覆盖嵌套输入、污染属性、无效 JSON、10 MB 限制    |
| B7   | 保留 Sequelize 6.37.8／node-cron 3.0.3，仅这两者下 uuid 固定 11.1.1；实际验证 CommonJS、v1/v4 默认值、事务及任务命名 |
| B8   | brace-expansion 依赖链更新至对应 1.1.18／2.1.4／5.0.9                                                                |
| B9   | browserslist 4.28.9                                                                                                  |
| B10  | js-yaml 4.3.2／3.15.2                                                                                                |

未使用 `npm audit fix --force`，未改宽安全扫描规则。后端全依赖从 12 高危／6 中危降至 1 高危，生产依赖从 9 高危／6 中危降至 1 高危；剩余均为 xlsx，两个 audit 均退出 1。审计分级不等于已证明应用可利用。

## 四、开发自测矩阵与命令

依赖安装在专用 `apple-order-security-20260909` 容器，镜像 `apple-order-mgr-dev-backend:local`、Node 22.22.0；不挂载真实 `.env`，不继承业务密钥。安装时更新仓库锁文件和该容器自身 node_modules，既有开发 API／Worker 的 node_modules 未更新。数据库阶段使用仅内部网络，无对外端口及互联网出口。

| 检查                                      | 实际结果                                                                                                                                            |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| mailparser、爬虫、代理、新依赖兼容 4 套件 | 57 项通过，退出 0                                                                                                                                   |
| 新依赖全量 Jest                           | 38 套件／266 项通过，3 套件／41 项按环境门禁跳过，退出 0                                                                                            |
| 付款 ACL 与生命周期隔离库                 | 2 套件／29 项通过，覆盖权限、并发、回滚和 150 单分配，退出 0                                                                                        |
| Migration                                 | 隔离库 21 项 up；生命周期用例内执行最新迁移 down → up，最终 21 条元数据                                                                             |
| 旧依赖反向回归                            | 新增兼容套件 5 失败／5 通过，退出 1：4 项版本约束和前导零 IPv4 行为；用于旧基线证据，不是新依赖失败                                                 |
| 后端 Lint                                 | 新依赖容器 quiet 模式退出 0，不代表存量 warning 清零                                                                                                |
| 前端 Lint／Vite                           | 既有开发容器退出 0，主包 856.12 kB，保留大包警告                                                                                                    |
| 文档检查                                  | 最终正常开发 API 容器 53/53 文档、19 模型通过；隔离容器最初因缺少 prod Compose 挂载失败；新增记录中的旧路径引用触发检查后移至归档定位记录，复测通过 |
| 后端全依赖／生产依赖 audit                | 均退出 1，仅剩 xlsx 1 高危                                                                                                                          |
| 前端只读 audit                            | 退出 1，6 高危／3 中危；未修改前端锁文件                                                                                                            |

实际安装命令在专用容器中执行：

```bash
npm update mailparser ip-address undici express body-parser qs brace-expansion browserslist js-yaml --package-lock-only --ignore-scripts --no-audit
npm install --package-lock-only --ignore-scripts --no-audit
npm ci --ignore-scripts --no-audit
npx jest test/dependencyCompatibility.test.js test/emailParser.test.js test/crawlerService.test.js test/proxyManager.test.js --runInBand --coverage=false
npm test -- --runInBand
npm run lint -- --quiet
npm audit --json
npm audit --omit=dev --audit-level=high
```

既有 Compose 中仅执行只读验证，不重装依赖：

```bash
docker compose -f docker-compose.dev.yml exec -T api npm run docs:check
docker compose -f docker-compose.dev.yml exec -T api npx jest test/dependencyCompatibility.test.js --runInBand --coverage=false
docker compose -f docker-compose.dev.yml exec -T frontend npm run lint -- --quiet
docker compose -f docker-compose.dev.yml exec -T frontend npx vite build
docker compose -f docker-compose.dev.yml exec -T frontend npm audit --json
git diff --check
```

隔离 PostgreSQL 17.5 使用专用内部网络、tmpfs 数据目录、无宿主机端口；只创建 `apple_order_mgr_lifecycle_test_20260909` 合成库。`trust` 仅限该临时隔离网络，不适用于开发或生产配置。测试用字段加密和 JWT 密钥在子进程内随机生成、不输出。迁移与测试环境固定 `DB_HOST=security-db`、`DB_USER=postgres`、`DB_SSL=false`、`NODE_ENV=test`，`DB_NAME` 和 `DB_NAME_TEST` 均为该测试库。

```bash
npx sequelize-cli db:migrate --config config/config.js --migrations-path migrations
# 仅新建合成库：付款套件前清空迁移生成的默认配置单例
psql -U postgres -d apple_order_mgr_lifecycle_test_20260909 -v ON_ERROR_STOP=1 -c 'TRUNCATE payment_dispatch_settings;'
# 在上述隔离环境内，注入临时随机测试密钥后执行
RUN_PAYMENT_INTEGRATION=true RUN_LIFECYCLE_DB_INTEGRATION=true LOG_TO_FILES=false \
  npx jest test/accessControlPayment.integration.test.js test/officialOrderLifecycle.integration.test.js --runInBand --coverage=false
```

未实测真实 IMAP、Apple、代理、生产 Node 20／linux/amd64 制品；声明的依赖 Node 要求与此前生产 Node 20.20.2 相容，但不替代目标运行时构建与测试。41 项默认跳过中，付款和生命周期 29 项已另行执行；其余真实环境或集成层级不因此算通过。本轮未新增浏览器验证。

## 五、仍需确认／执行的事项

1. **B5 来源选择**：SheetJS [官方安装文档](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/)说明 npm 发布停留在 0.18.5，官方发布为 0.20.3，并推荐 vendoring。建议将官方 `xlsx-0.20.3.tgz` 放入 `vendor/`，记录来源、Apache-2.0 许可和 SHA-256，使用 `file:vendor/xlsx-0.20.3.tgz` 并调整两个 Dockerfile 的安装前复制步骤。已仅下载到临时目录检查，SHA-256 为 `8dc73fc3b00203e72d176e85b50938627c7b086e607c682e8d3c22c02bb99fe8`；未安装或提交。保持 `require('xlsx')` 调用方式，确认后回归订单／取机人导入导出、模板、中文表头、前导零、空值、行数和文件大小限制、畸形文件、权限及公式相关既有行为。不要把换版本等同于不存在所有 Excel 风险。
2. **前端新发现**：brace-expansion、browserslist、js-yaml、nanoid、postcss、vite 为高危包，esbuild、react-router、react-router-dom 为中危包；共 9 个受影响包，不等于 9 个独立漏洞。前端 Vite／Router 的自动建议涉及主版本变化，应另行选择兼容方案和 UI 回归范围，不盲目强制升级。
3. **已发布历史**：本地增量已处理，远程已发布历史与轮换按[敏感资产处置记录](../../security/敏感资产处置记录.md)独立治理。未修改远程扫描结果，也未实际运行 GitHub CI。
4. **生产门禁**：先解决或明确处置剩余安全门禁，再新增交付提交、正常推送、精确提交制品构建、备份校验及隔离恢复迁移演练。当前代理选择和爬虫启用没有新增决定，不能从本次“同意调整”推导出启用授权。

开发自测结论：已执行的兼容性、单元和隔离库测试通过；安全审计与发布门禁未通过。已重建 8 个未推送本地历史提交，尚未新增工作区交付提交；未 push、测试部署、构建生产制品、上传、生产 Migration、真实数据写入、凭据轮换或既有服务重启。

收尾：指定文件 Prettier 检查及 `git diff --check` 退出 0。专用依赖容器 `apple-order-security-20260909`、专用数据库容器 `apple-order-security-db-20260909` 及同名内部网络已精确停止、删除并复核不存在；tmpfs 合成库已销毁，不可恢复但可重建。未清理既有开发服务、命名卷或真实数据；临时脱敏证据目录保留。
