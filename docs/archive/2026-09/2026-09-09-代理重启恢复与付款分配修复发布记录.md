# 代理重启恢复与付款分配修复发布记录

> 状态：已修复、已提交并部署生产，真实订单自动分配与浏览器复验通过。
>
> 日期：2026-09-09；时间均为北京时间。
>
> 代码：本地 main@512c2d3；生产发布 20260909T054341Z-04bce86b553f。

## 故障与根因

13:23:19／13:23:39 两笔新订单尾号 7921／3883 已入库并创建付款任务，首次官网任务 4028／4029 保持 pending，无抓取尝试。付款调度正常、人员容量充足，但无官网截止导致未分配和等待官网时间。

13:06 发布重启后内存代理为空，数据库保留 requested=yiyou_http、active=fanproxy_tunnel、switchStatus=failed。Worker 在失败保护分支直接返回 awaiting_new_switch_request，漏掉恢复原代理，循环只有心跳而不领取任务。隔离桩复现心跳 1 次、恢复和领取均 0 次。此前上线验收没有覆盖切换失败后的重启恢复，健康与心跳结果不足以证明新单抓取可用。

## 恢复与修复

- 按用户授权备份生产后，通过已鉴权管理接口重新请求网帆，候选检查成功后恢复既有队列。尾号 7921 自动分配 Test_1，官网截止 13:52:43；尾号 3883 首次抓取失败后重试成功，自动分配 admin，官网截止 13:53:08。未人工设置截止、分配负责人或执行付款。
- 冷启动失败状态优先恢复最后有效 Provider，并执行全局限流的候选健康检查；保留失败目标诊断。恢复失败不领取任务，一分钟内不反复恢复；新请求可结束冷却，旧恢复不得覆盖并发新请求。
- 新增 workerProxyReady／workerProxyErrorCode，将当前进程可用性与历史 Provider 和心跳分开。启动先标为未就绪，代理初始化成功才就绪；API 同时校验 20 秒心跳时效及暂停状态。
- 系统页显示就绪／阻塞原因、区分上次生效与当前生效，并允许重新选择尚未就绪的历史通道。两个付款页面优先显示官网已过期／已付款／取消阶段，不因截止缺失误显示等待官网时间。

## 验证

- 全量单元回归 403 项通过；前后端 Lint 无错误（保留既有 warning）；前端构建及付款阶段文案检查通过。
- 隔离 PostgreSQL 集成 16 项通过，覆盖失败后重启→恢复代理→领取新单→官网截止落库→自动分配，以及新 Migration down/up。外部网络使用合成响应，测试库已删除。
- 本地业务库原有 22 条迁移，备份后执行新增正式迁移至 23 条；没有启动新的本地邮件／爬虫 Worker。
- 生产浏览器复验：新订单负责人及真实倒计时正常，旧过期订单显示付款已过期，系统日志页显示当前网帆、爬虫代理已就绪。第三笔新订单尾号 2125 也已自动分配 admin。
- 生产正常重启后代理就绪为 true，抓取继续，邮件连接和最近扫描正常。failed 状态的重启回归在隔离环境验证，未在仍有待付款订单的生产环境人为制造故障。

## 发布与备份

- 完整提交：512c2d3de9ae1a05fa96b12ff55c6be6b369c7a9；tree：04bce86b553fbbaf0417df357e56409a31140784，构建时工作树干净。
- 发布目录：`/var/www/apple-order-mgr/releases/20260909T054341Z-04bce86b553f`，本地构建 linux/amd64 运行及迁移镜像；服务器校验全部制品 SHA-256 后加载。
- 临时恢复前备份：`/var/backups/apple-order-mgr/proxy-recovery-20260909`。
- 发布预检和最终备份分别位于 `/var/backups/apple-order-mgr/proxy-runtime-20260909-preflight`、`/var/backups/apple-order-mgr/proxy-runtime-20260909-final`。
- 两份发布备份均校验并在独立 PostgreSQL 15、network none、tmpfs 容器恢复；预检副本执行新增迁移成功，临时容器已删除。
- 13:45:38 至 13:45:48 完成停止、最终备份恢复验证、迁移和服务切换，约 10 秒。迁移 22→23，订单 105、邮件 25、付款任务 5 不变，全体订单全部字段摘要在停写迁移前后完全一致。
- HTTPS readiness 和首页制品一致性通过，API healthy、两个 Worker 运行；app.env、Apple Nginx 配置哈希以及共享服务活跃时间、其他容器 ID 均未变化。
- 上一发布 `20260909T050313Z-d45e11af3f2b` 保留，可回退应用。新增状态列不改变订单数据；生产回滚不默认删除列或恢复整库覆盖切换后的新业务数据。

本轮未执行真实付款，也不把候选首页检查当作亦优供应商故障已解决；当前生产使用网帆。
