# 订单表达误报修复生产发布记录

> 状态：已发布，历史误报告警已清理；生产鉴权查询和技术验收通过。
>
> 日期：2026-09-09；时间未另注时为北京时间。
>
> 范围：本地提交、linux/amd64 制品、生产切换、指定历史告警清理；未触发真实官网刷新或付款操作。

## 提交与制品

- 代码提交：`e655d620c2fd4fda6c73e1d1d1e05fbdbedef4b9`，本地 `main`。
- Git tree：`d45e11af3f2bd4f2bfa6d381fa8ad63654c2e6a0`；构建时工作树干净。
- 发布目录：`/var/www/apple-order-mgr/releases/20260909T050313Z-d45e11af3f2b`，current 已指向该目录。
- 运行及迁移镜像使用上述发布 ID，平台 `linux/amd64`；服务器只校验、加载并运行制品，没有安装依赖或构建源码。
- 上一版本：`20260908T221047Z-6bfcb0bb45a1`；镜像与目录保留，可回退应用。此次没有新增 Migration、环境配置或敏感字段转换。

## 变更与开发验证

- 微信／微信支付／WECHAT、支付宝／ALIPAY 分别等价，花呗保持独立；完整识别的 iPhone 名称允许容量单位和颜色排列差异，真实型号、颜色、容量、数量及未知表达继续告警。
- 多商品全局 SKU 优先，再作双方唯一的语义名称匹配；保留原始来源与官网文本。
- 历史工具默认只预览，指定订单后加 `--execute` 才更新；同事务锁行，只清理已证明等价的付款方式／名称冲突。
- 全量单元回归 43 套件、396 项通过；最终定向 41 项通过；另启用隔离 PostgreSQL 集成 14 项通过，涵盖解析合并落库、预览零写入、时间保持、幂等和整批回滚。语义模块行覆盖率 100%。
- 后端、前端 Lint 无错误（保留既有警告）；前端制品构建通过；提交前文档检查通过。Gitleaks 对提交差异扫描未发现敏感信息。

## 备份与切换

- 预检备份：`/var/backups/apple-order-mgr/expression-conflicts-20260909-preflight`。
- 停写后的最终备份：`/var/backups/apple-order-mgr/expression-conflicts-20260909-final`。
- 两份 pg_dump 均经 SHA-256 校验，并在独立 PostgreSQL 15、network none、tmpfs 容器成功恢复；临时恢复容器已删除。
- 13:05:51 开始暂停三个 Apple 应用服务，13:06:02 新 API 与两个 Worker 启动并完成切换，操作窗口约 11 秒。
- 迁移命令确认无需新增迁移；订单 102、邮件 22、付款任务 2、迁移记录 22 均不变。
- 对全体订单除 `validation_issues`、`validation_status`、`anomaly_detected_at` 外的所有字段生成排序摘要，清理前后完全一致；包含商品、来源快照、updatedAt、lastCrawledAt 和自动刷新设置。
- app.env 与 Apple Nginx 配置哈希未变；共享 Nginx／PostgreSQL 活跃时间、其他容器 ID 未变，没有重启或 reload 共享服务。

## 历史清理与生产验收

| 订单 | 清理前 | 清理后 | 保持的业务状态 |
| --- | --- | --- | --- |
| W1998270752 | 付款方式及名称共 2 条冲突 | valid，0 条告警 | payment_expired，自动刷新关闭 |
| W1982317865 | 付款方式及名称共 2 条冲突 | valid，0 条告警 | payment_expired，自动刷新关闭 |

- 生产全量异常订单仅上述 2 笔，共清理 4 条表达误报；没有清除其他真实异常。再次预览两笔均无待修改项。
- 最后官网更新时间保持原值：尾号 0752 为 12:41:16.498，尾号 7865 为 12:39:28.019；未借清理动作伪造官网更新时间。
- HTTPS readiness、首页与制品一致性检查通过；API 容器 healthy，两个 Worker 运行。
- 使用内存中两分钟短期令牌完成现有管理员的只读订单 API 验证，两笔返回 valid、空 validation_issues、付款已过期且自动刷新关闭。未重置密码或保存令牌；这不代表执行了用户密码登录测试。
- 邮件 Worker 已连接，13:06:35 成功扫描，最近扫描错误为空、连续失败 0；自动刷新计划和活动任务均为 0。
- 本轮未验证新真实邮件入库时效、实际付款或新增官网请求；这些不属于本次表达误报修复的生产操作。
