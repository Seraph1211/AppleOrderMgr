# 订单刷新修复生产发布记录

> 状态：发布准备中
>
> 日期：2026-09-09（北京时间）
>
> 授权：用户要求先提交本地改动，再部署生产；本轮不要求推送远程仓库。

## 发布范围与门禁

提交当前工作树的邮件来源过滤修复、联系邮箱校验修复、历史订单自动刷新资格修复、测试及配套文档。邮件来源过滤已在上一生产 tree 中发布，本轮将其纳入正式提交。已取消付款二维码功能，无新增二维码接口和页面。无新增 Migration，无凭据或生产配置变更。

本地全量测试在与当前锁文件 SHA-256 一致的隔离镜像中通过：39 套件／315 项，3 套件／42 项按环境门禁跳过。前后端 Lint、文档检查及 Vite 6.4.3 构建通过，保留大包警告。旧开发容器的 6 项依赖版本测试失败已用锁文件一致的环境复核，不改变测试预期。

## 生产切换前基线

只读核对：current 为 20260908T192217Z-0c4c67045dfd，三个 Apple 应用容器运行、API healthy。宿主机 PostgreSQL 15 与共享 Nginx 正常，磁盘余量约 14 GB。发布保持现有邮件来源配置、代理 Provider、自动刷新和付款分配设置。

## 执行与验收

待完成：本地提交、精确提交的 linux/amd64 制品、生产数据库及配置备份、隔离恢复校验、三个 Apple 应用容器更新、current 原子切换及技术验收。生产业务数据、敏感配置和完整容器 inspect 只存服务器备份目录，不进入仓库。停止窗口内最终备份后才更新应用；无结构差异时不重复敏感数据转换。

真实官网／邮件／付款端到端验收单独记录；健康检查不代表业务验收。新版本不会直接删除历史身份异常，已暂停订单须受控手动刷新成功后恢复。

## 首次发布与追加修复

- 本地提交 63794c91eeee1365737a8470e3f5b50f4bb887bc；从干净 tree f66174157e13d728e483f6b41a526a1cc7a9421e 构建 linux/amd64 制品 20260908T204608Z-f66174157e13。暂存文件快照 Gitleaks 零命中，全部制品 SHA-256 校验通过；未推送远程。
- 初次备份与最终停机备份分别位于服务器 /var/backups/apple-order-mgr/63794c9-refresh-preflight 和 /var/backups/apple-order-mgr/63794c9-refresh-final；两个备份均在无网络、无宿主机端口的临时 PostgreSQL 15 容器恢复通过，临时容器已删除。最终核心计数为 21 Migration、101 订单、542 Apple ID、30 取机人、21 邮件、1 付款任务；原库与恢复库一致。没有待处理 Migration，没有执行敏感数据转换。
- 北京时间 04:49:23–04:49:30 完成首轮更新，窗口约 7 秒；API healthy、两个 Worker 正常。配置文件和共享服务散列／启动时间未变，其他容器身份与启动时间未变，未 reload Nginx。HTTPS 首页及 JS 与制品一致，未登录订单接口 401，现有登录会话能读取付款调度。
- 历史订单后续自动计划已收敛至 0，旧任务执行前被跳过；联系邮箱校验在生产目标订单上通过。
- 追加发现：手动任务 3993 报 UNKNOWN，实际是 CrawlLog.source 不接受队列 trigger manual_single，成功抓取后的事务因日志校验回滚。补丁将 manual_single／manual_all 映射为 manual，保留 auto→scheduled 与 page_open 跳过；同时将非标准上游 HTTP 631 等记入 context.upstreamHttpStatus，避免 100–599 的日志字段校验掩盖原始错误，不改表结构。
- 追加开发回归：39 套件／322 项通过，42 项环境门禁跳过；新增手动／自动来源映射和真实 Sequelize 模型日志校验用例。补丁待本地提交与二次发布，首次版本保持运行。
