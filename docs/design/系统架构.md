# 系统架构与功能边界

> 状态：当前有效
>
> 最近核对：2026-09-06
>
> 基线：main@0fd7f80 与当前未提交工作树
>
> 验证范围：本地工作树静态核对；未验证真实数据库、邮箱、官网和生产环境

## 当前系统

React 管理台通过 Express API 管理用户、Apple ID、取机人、订单、渠道、Excel 导入导出、仪表板与系统日志。持久化使用 PostgreSQL 和 Sequelize；邮件、爬虫以独立 Worker 运行。

```text
浏览器 → Vite 开发代理 / 生产 Nginx → Express API → PostgreSQL
IMAP 邮箱 → 邮件 Worker → 格式解析 → 订单与邮件日志事务写入
爬虫 Worker → 筛选待刷新订单 → 代理访问 Apple → 短事务更新订单与日志
```

邮件入库与官网补全由不同进程承担；不能把历史“邮件到达立即同步触发爬虫”的图示当成独立 Worker 的可靠任务投递保证。

## 模块入口

| 能力         | 入口                                                                                                              | 说明                                                 |
| ------------ | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| HTTP API     | [app.js](../../src/app.js)、[路由目录](../../src/routes/)                                                         | 认证、强制改密、权限、错误处理                       |
| 前端         | [App.jsx](../../frontend/src/App.jsx)、[页面目录](../../frontend/src/pages/)                                      | 浅色管理界面，列表优先                               |
| 邮件监听     | [emailWorker.js](../../src/workers/emailWorker.js)、[emailService.js](../../src/services/emailService.js)         | IMAP IDLE 和 UNSEEN 批次处理                         |
| 邮件解析     | [当前解析器](../../src/services/emailParser.js)、[历史独立实现](../../services/emailParser.js)                    | 当前解析器直接处理 MIME 与字段，历史实现独立存在     |
| 订单保存     | [orderService.js](../../src/services/orderService.js)                                                             | 快照、基础数据匹配、事务                             |
| 官网同步     | [crawlerWorker.js](../../src/workers/crawlerWorker.js)、[crawlerService.js](../../src/services/crawlerService.js) | 运行中的同步入口                                     |
| 历史爬虫解析 | [order_parser.js](../../crawler/order_parser.js)                                                                  | 独立历史解析入口；修改前确认调用方，不能代替服务实现 |
| 数据         | [模型](../../src/models/)、[迁移](../../migrations/)                                                              | 六张当前模型表，详见数据库规范                       |

## 业务数据边界

- 商品数量以邮件为准；取消订单时官网数量为 0 不覆盖邮件数量。
- 订单状态、官网支付/取货状态、商品图片、官网金额来自官网；金额缺失不能用固定单价伪造。
- Apple ID 与邮件中取机人信息来自邮件；基础资料匹配用于填充订单快照。
- 渠道按订单、取机人的 tag 聚合，不存在独立渠道主表。
- 付款人、取机人、Apple ID、系统操作员是不同主体；内部结算尚在[规划阶段](../planning/付款人与内部结算方案.md)。

## 运行拓扑与限制

仓库 [Compose](../../docker-compose.yml)仅包含 api、email-worker、crawler-worker。Nginx 与 PostgreSQL 在该编排之外，示例 Nginx 对接宿主机 3000 端口；这描述仓库配置，不证明生产已迁移至此拓扑。

任务租约、邮件启动/定时回溯、持久化重试和跨进程暂停恢复仍未完成。浏览器、真实邮箱、官网、迁移及生产验收状态以[开发进度](../development/开发进度.md)为准。

## 专题入口

[API 契约](API设计.md) · [邮件解析](邮件解析器.md) · [邮件处理](邮件处理流程.md) · [爬虫](订单爬虫.md) · [数据库](../database/数据库架构.md) · [技术决策](技术决策.md)
