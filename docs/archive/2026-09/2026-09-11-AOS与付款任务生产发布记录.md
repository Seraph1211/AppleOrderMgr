# AOS 与付款任务生产发布记录

> 状态：生产技术发布完成，真实 AOS 业务待验收
>
> 日期：2026-09-11
>
> 代码基线：开发提交 `d700c71`，本地 `main@c5c1490`，tree `1793599a7359`
>
> 生产制品：`20260910T222510Z-1793599a7359`，`linux/amd64`，干净工作树构建

## 发布内容

- 上线 AOS 文件接收、持久化处理、来源切换、当天补录、跨日许可、订单来源留痕、设备管理、Windows x64 采集器及管理页面。
- AOS 解析兼容 15 列和 16 列，识别第 16 列取机人身份证后四位；第 17 列及以后只保留在加密原文，不阻断已知字段。既有订单继续执行“去重留痕”，不覆盖订单资料。
- 写入 Apple 中国大陆官网核对的 49 家直营店映射；`R502` 映射为“Apple 成都万象城”。
- 采集设备凭证增加 AES-256-GCM 密文保存和受审计查看／复制，SHA-256 摘要继续用于认证；采集器 HTTPS 根地址由 `AOS_COLLECTOR_PUBLIC_URL` 配置并在页面展示。
- 本人付款任务和管理员付款调度按订单下单时间倒序稳定分页；管理员单项刷新保持顶层 `{ jobId, status, created, merged }` 契约，批量刷新仍返回汇总。

## 提交与发布前验证

当前开发改动先提交为 `d700c71`，再与本地 `main` 已有的付款刷新修复合并为 `c5c1490`。提交与合并内容没有设备凭证、私钥、实际密码或本地环境文件；未推送远程仓库。

合并后全量 48 个测试套件、456 项测试通过，7 个环境门控套件／104 项跳过；后端 Lint 0 error、1613 个既有 warning，前端 Lint 0 error、41 个既有 warning。Vite 生产构建通过，保留约 975 kB 主包提示；文档检查通过，70 份文档均可达、29 个模型字段覆盖；`git diff --check` 通过。

发布脚本从干净 `main` 构建 `linux/amd64` 运行、迁移镜像和前端制品。`manifest.json` 记录完整 commit、tree 和 clean 状态，`SHA256SUMS` 在本地及服务器均通过。

## 备份与隔离演练

发布前备份、最终停机备份、旧容器 inspect、旧部署配置及计数证据保存在 `/var/backups/apple-order-mgr/aos-payment-20260910T222727Z/`，权限为 600，两个数据库 dump 的 SHA-256 均通过。备份时生产为 9 个账号、115 条订单、15 个付款任务、35 封邮件、4367 条刷新任务和 27 条 Migration。

服务器先在 internal Docker 网络和内存文件系统恢复完整备份，不发布宿主机端口。首次尝试使用缓存的 `postgres:15-alpine` 时，容器在初始化 WAL 阶段遇到宿主机存储兼容问题并在恢复前停止；临时容器、网络和环境文件由 trap 清理。改用历史已验证的 PostgreSQL 15.19 Debian 固定摘要后，恢复和四条待处理 Migration 通过。一次校验查询误用模型属性 `source`，实际数据库列为 `active_source`；该轮隔离资源同样清理，最终用正确列名重新恢复并完整通过。

最终隔离结果为 Migration 27→31，9 个账号、115 条订单、15 个付款任务、35 封邮件和 4367 条刷新任务保持一致；默认来源 `email`、AOS 设备 0、门店 49、`R502` 名称和城市正确、设备凭证密文列存在。隔离 API readiness 返回 200，AOS 设备接口匿名请求返回 401。

## 生产切换与技术验收

北京时间 2026-09-11 06:34 停止三个 Apple 应用容器，生成最终数据库备份后执行四条正式 Migration；31 条 Migration 全部登记，来源保持 `email`，没有自动启用 AOS。API、邮件 Worker 和爬虫 Worker 随后统一重建为新运行镜像；API 就绪后原子切换 `current`，Nginx 配置检查通过并 reload。新 API 在启动初期有三次连接未就绪重试，随后 readiness 通过，未触发回滚。

上线后确认：

- API healthy，回环及 HTTPS readiness 均为 200；匿名订单和 AOS 设备接口均为 401。
- 三个应用容器均使用新镜像，以 `node` 用户和只读根文件系统运行；API 仅绑定 `127.0.0.1:3001`，网络仍为 `172.28.240.0/24`。
- 生产配置文件保持 root:root、600，并仅新增稳定根地址 `https://apple.godp.me`；来源仍为邮件，AOS 记录和设备均为 0。
- 账号、订单、付款任务、邮件和刷新任务数量与备份一致；门店为 49 家，`R502` 映射为“Apple 成都万象城”。
- 邮件 Worker 已连接 IMAP 并持续扫描，0 条 error/fatal；爬虫 Worker 一个候选 Provider 检查失败后成功切换到可用 Provider并启动持久队列，0 条 error/fatal。
- 公网 HTML、JS 和 CSS 与制品逐字节一致；服务器 10 个容器运行、0 个 unhealthy。
- 已登录生产页面显示“订单数据源”、当前来源“邮件”、AOS 0 台设备和可复制的 HTTPS 地址；付款调度 15 条任务按下单时间倒序展示，付款任务页正常加载。

## 验收边界与回滚

本轮只完成生产技术发布和只读页面验收。没有登记生产采集设备、生成生产设备凭证、切换到 AOS、上传真实 AOS 文件、创建 AOS 订单、点击付款刷新或额外调用 Apple 官网；实际抢购软件、多机并发、开机／登录恢复、真实取机人关联和真实付款仍需业务验收。

应用回滚目标为 `/var/www/apple-order-mgr/releases/20260910T075455Z-d84370083ba0`。本轮 Migration 为新增表、字段及字典数据，旧应用不读取这些对象，短时应用回滚可保留新结构；若必须恢复数据库，应停止全部 Apple 应用并使用本次最终完整备份，且明确恢复后会丢失发布后的写入窗口。共享 PostgreSQL 未重启，Nginx 仅执行配置检查和 reload；未操作其他项目容器。
