# 刷新队列持续补位修复发布记录

> 状态：已部署生产，真实手动任务重叠执行验收通过
>
> 日期：2026-09-09

## 现象与根因

生产并发配置为 20，但 Worker 在整批任务 `Promise.allSettled` 结束前不领取下一批。15:43 的手动任务 4074 开始于 15:43:45.155；任务 4075 晚 21 毫秒入队，等待至 15:43:55.155 才执行，当时仅一个名额在使用。此前仅验证参数和会话池，漏掉了跨批次晚入队情形。

## 修复

- 领取循环与订单执行分开；每 5 秒以并发上限减去在途任务数计算领取数量。
- 在途任务持续占用名额直到持久化完成，错误路径有拒绝处理并释放内存名额。
- 每轮仅为当前 Worker 仍持有的在途任务续租，再恢复过期租约，避免慢任务被补位扫描重新领取。
- 暂停或请求切换 Provider 时不补位；排空在途后才验证代理，候选失败仍可保留原代理处理。
- 关闭时等待正在领取及执行任务；生产 crawler-worker 优雅退出窗口增加至 120 秒，强制退出仍按租约恢复。
- 保持网帆默认、20 会话槽、20 Worker 并发、5 RPS 和最多 3 次尝试；无数据库表结构变更。

## 开发验证

- 定向单元 27 项通过，新增持续补位测试 9 项；全量单元 413 项通过，55 项外部环境测试按门禁跳过。
- 隔离 PostgreSQL 集成 18 项通过，含慢订单未完成时另一订单已 running、本进程续租而其他过期租约仍恢复，以及此前新单截止与付款自动分配回归。
- 前后端 Lint 无错误。核心 Worker 定向覆盖率超过 70%；真实代理、生产任务执行与 100 单成功率不由这些测试代替。
- 测试开发期间发现并修正新增 Mock 的 require-await 规范问题，以及集成夹具缺少 products 的模型校验问题；上述结果均为修正后的通过结果。

## 发布与生产验收

代码提交 `7382a44`，制品 `20260909T080256Z-39093599464d`，tree `39093599464d887b79b2735a4a6897357b2d6598`，平台 `linux/amd64`；制品 SHA-256 校验通过。生产应用于 16:03:50–16:03:53 更新，配置保持 `20/20/5/3`，爬虫退出窗口实际为 120 秒。API healthy，网帆代理 succeeded/ready、未暂停。未执行 Migration，仍为 23 条；共享 PostgreSQL 和 Nginx 未重启。

备份 `/var/backups/apple-order-mgr/refill-20260909T080348Z/` 包含发布前数据库、SHA-256 和环境配置。隔离 PostgreSQL 恢复成功，确认 105 条订单、23 条迁移；部署前后 `app.env` 字节一致。回退使用上一发布 `20260909T072938Z-527e0b4aef41` 的 release.env 与 Compose 恢复三项应用服务，并原子恢复 current 链接，无需回退数据库结构。

通过浏览器手动操作用户刚才刷新的两笔订单，前两次均成功但没有形成重叠窗口；后续遇到慢请求时验证了另一笔订单可利用空闲名额执行。共 5 次人工验收任务，4 成功、1 次官网 HTTP 541 失败，未隐藏失败或清理日志。关键重叠证据：

| 任务 | 开始执行 | 完成 | 结果 |
| --- | --- | --- | --- |
| 4079（慢订单） | 16:05:33.120 | 16:06:25.173 | HTTP 541，failed |
| 4080（晚入队订单） | 16:06:18.028 | 16:06:23.713 | succeeded |

任务 4080 于 16:06:13.539 入队，等待约 4.5 秒后启动，并在慢订单结束前完成。生产证明空闲名额可持续补位；上游风控仍可能使个别订单失败，不能将本次调度验收写成官网成功率保证。检查时没有 pending/running 遗留，浏览器两行恢复手动刷新按钮。

仍可能存在一个调度周期的正常排队，以及全局限流、代理校验、数据库繁忙或满载引起的等待。自网帆扩容时间起点累计为 9 成功、1 失败，远不足 100 个有效任务，继续保持 5 RPS 并沿用原观测起点。
