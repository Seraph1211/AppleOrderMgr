# H5 与付款任务生产发布记录

> 状态：生产技术发布完成；真实手机和实际付款业务待人工验收
>
> 日期：2026-09-12（北京时间）
>
> 开发提交：`afb4a75`；合并基线：`main@c3d5221`；tree：`713ddb3fe9b3634361469fbe243f02746e521820`
>
> 制品：`20260911T160908Z-713ddb3fe9b3`；干净 main，linux/amd64

## 发布范围

合入本地分支 `codex/payment-tag-multiselect-ui`，包含此前 TAG 多选筛选和批量复制，以及登录／付款任务手机适配、电脑订单 ID 和金额、批量修改人工处理状态、手机菜单遮罩修复。无新 Migration、环境配置或权限规则变更。未推送远程。

## 发布前验证

- 后端 456 项测试通过、104 项环境门控跳过；专用临时数据库付款集成 21/21 通过，测试库已清理。
- 新增前端批量单测 2/2 通过；Chrome 合成 API 验证六种宽度、登录、复制、刷新、批量状态部分失败、草稿及权限，含 430×932 菜单切页和短视口滚动。
- 前后端 Lint 均 0 error，分别保留 1631、41 项既有 warning；前端构建通过，保留大包提示。文档检查通过。
- 合并结果与开发提交 tree 相同，无合并冲突；制品清单记录 workingTreeClean=true，本地和服务器 SHA-256 均通过。

## 备份和隔离演练

备份路径：`/var/backups/apple-order-mgr/h5-20260912/`，目录和文件受 root 权限保护，包含初始／最终数据库 dump、SHA-256、旧部署指针、配置、容器和共享服务基线。旧版本为 `20260910T222510Z-1793599a7359`。

完整备份恢复到不联网、无宿主机端口、tmpfs 数据盘的 PostgreSQL 15 容器；12 个账号、135 条订单、35 个付款任务、31 条 Migration 核对一致。新迁移镜像确认没有待执行迁移；新运行镜像对隔离库的付款列表、订单 ID、金额字段和 TAG 筛选验证通过。隔离容器已清理。

## 生产切换和验证

北京时间 00:11:22–00:11:31 完成切换，约 9 秒。停止三个 Apple 应用、生成最终备份并校验哈希、启动新 API、核对核心计数、原子切换 current、启动两个 Worker。没有执行生产迁移。脚本配置失败回旧镜像和旧指针，本次未触发回滚。

- API healthy，回环／HTTPS readiness 通过；匿名付款任务接口返回 401。
- 公网首页、全部 JS/CSS 与制品逐字节相同。
- API、邮件 Worker、爬虫 Worker 均使用新运行镜像；IMAP 已就绪并扫描，爬虫持久化 Worker 启动正常。
- API 启动后账号、订单、任务和 Migration 计数与最终备份前一致。
- 既有浏览器登录会话仍有效，生产付款页面读取成功，订单 ID、金额、TAG 和批量处理入口可见；没有点击真实复制、刷新、保存或批量修改。
- 其他项目容器 ID 和启动时间、共享服务启动时间、生产 app.env 内容与发布前一致；Nginx 配置检查通过，无需修改或 reload。

## 验收边界和回滚

本次为生产技术验收和已登录只读查询。没有重新登录或接管会话，也未执行真实付款、人工状态修改、供应商身份核验或额外官网刷新；日常 Worker 按原配置恢复运行。真实手机软键盘／剪贴板／触摸与完整付款业务仍需人工验收。

无数据库变更，应用回滚可恢复旧发布目录的三个镜像及 current 指针，保留切换后业务数据。最终备份仅作为必要恢复保障，禁止未经评估恢复数据库覆盖发布后写入。


## 当日增量：批量复制订单之间空行

北京时间 00:35:06，前端提交 `8496d15` 发布为 `20260911T163400Z-2d5960a2dfe0`，在相邻订单之间增加一个空行，首尾不额外添加空行。本地合成剪贴板、定向 Lint、构建和文档检查通过；干净 main 制品 SHA-256 在两端通过，公网 HTML／JS／CSS 与制品逐字节一致，健康检查通过。

本次只原子切换前端目录，API 和两个 Worker 继续运行 `c3d5221` 对应镜像，未重启任何容器、未操作数据库或 Nginx。全部容器 ID、启动时间、镜像与切换前一致。完整制品镜像已加载备用，但未切换后端。旧指针、容器基线和核验记录位于 `/var/backups/apple-order-mgr/copy-spacing-20260912/`，前端回滚目标为 `20260911T160908Z-713ddb3fe9b3`；没有触发真实订单复制、刷新或修改。未推送远程。
