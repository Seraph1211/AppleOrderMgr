# 服务器监控界面优化发布记录

> 状态：生产前端发布与已登录视觉验收完成
>
> 日期：2026-09-15（北京时间）
>
> 代码提交：`main@630f4abe94d40076cda6ee97785670e63f87459e`
>
> 制品：`20260915T105856Z-a6dd22b581a5`；tree：`a6dd22b581a566dfd4d17eaf2f8267edc520883f`；干净 main、linux/amd64

## 发布范围

根据生产页面反馈调整服务器监控入口和页面布局：侧栏入口移动到“订单数据源”之后；流量统计、实例监控和告警规则改为横向标签页；流量筛选使用带标题和重置入口的响应式面板，服务器选择改为可搜索多选下拉，支持已选数量、清空和全部服务器状态。未修改监控接口、权限、统计口径、告警规则、数据库或采集器。

## 验证

- 前端开发容器 Lint 0 error，保留 40 项既有 warning；Vite 生产构建成功，保留既有大包提示。
- 文档链接与模型覆盖检查通过，制品 `SHA256SUMS` 在本地和服务器全部通过。
- 已登录生产页面确认菜单顺序、三个标签页的选中及切换状态、服务器下拉搜索和多选、已选数量、清空和重置。选择 `US Server No.1` 后页面只读刷新为该服务器流量，再重置为全部服务器。
- 默认桌面视口和 390×844 临时窄屏视口均完成截图核对，筛选字段、标签页和表格可访问；浏览器控制台无 error。临时视口在验收后已恢复。

## 生产切换与回滚

北京时间 19:00:32 原子切换 `/var/www/apple-order-mgr/current` 到新制品。公网 `index.html` 已引用 `index-BrHUwQCZ.js` 和 `index-WhpsjRf1.css`，公网文件摘要与制品逐字节一致：

- JavaScript：`c36f02498f101bfd9659f352ce9a92398d3c8aaca14908c7a6736d2f61c1bacc`
- CSS：`5317e2015250ddc0e85db8c159107f7bf9967f3ccaf48924ede815ed9b35b34f`

本次只切换静态前端指针，API 和两个 Worker 继续运行 `20260915T060317Z-952a9da57b6d` 的原容器与镜像，容器 ID、运行时长和健康状态未变化；PostgreSQL、Nginx 主进程及生产 `app.env` 摘要未变化。未执行 Migration、数据库读写、Nginx reload 或采集器更新。旧静态资源合并到新目录，避免已打开页面请求旧哈希资源失败。

回滚目标为 `/var/www/apple-order-mgr/releases/20260915T060317Z-952a9da57b6d`，可通过原子恢复 `current` 指针回退。切换前后证据位于 `/var/backups/apple-order-mgr/server-monitor-ui-20260915T105856Z-a6dd22b581a5/`。本次未推送远程。
