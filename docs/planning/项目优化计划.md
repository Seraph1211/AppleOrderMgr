# Apple 订单管理系统优化计划

> 文档状态：执行中；最近核对 2026-09-06。业务验收未完成，文档治理批次完成（未提交、未部署）。
>
> 创建日期：2026-09-05
>
> 适用仓库：AppleOrderMgr
>
> 审查基线：`main` 分支，提交 `0fd7f80`
>
> 目标：在保留现有 MVP 成果的基础上，完成安全整改、业务闭环、架构收口和可验证交付

## 当前任务与历史基线

下方背景、功能盘点和旧验证记录以 2026-09-05 为基线，当前状态统一见[开发进度](../development/开发进度.md)。本轮完成 DOC-01、DOC-02、DOC-03、DOC-05 的本地文档整理与静态门禁部分，并建立 DOC-04 的决策记录入口；文件存储等具体决策尚未关闭。真实环境可执行性、完整 API/数据库契约验收仍未完成。付款人与结算细节统一维护在[专项方案](付款人与内部结算方案.md)；订单刷新业务规则、持久化任务、并发、代理和实施验收统一维护在[订单状态刷新与爬虫重构方案](订单状态刷新与爬虫重构方案.md)。

## 一、背景与总体判断

当前项目已经实现了后端 API、用户认证、Apple ID 与取机人管理、订单管理、邮件监听与解析、Apple 官网爬取、自动刷新、仪表板、渠道管理、Excel 导入、前端管理页面及基础部署文件。

项目现阶段的主要矛盾不是“功能数量不足”，而是以下问题尚未收口：

1. 仓库中存在真实敏感数据和硬编码凭据，安全风险高于普通功能缺陷。
2. 状态枚举、接口字段、数据库迁移和前端调用之间存在多处契约不一致。
3. 部分页面仍使用 Mock 数据或演示逻辑，不能代表真实业务结果。
4. 邮件和爬虫主链路已有实现，但缺少可靠补漏、失败重放、并发控制和持久化调度能力。
5. 测试、Lint、构建、依赖锁定、部署和文档没有形成统一质量门禁。

因此，本计划坚持“先止血、再修主链路、随后重构、最后扩展”的顺序。现阶段不建议推倒重写，也不建议在关键契约修复前继续增加大量页面或业务字段。

### 1.1 已实现功能盘点

| 领域          | 已有能力                                                                      | 完整度判断                                                                             |
| ------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 认证与用户    | JWT 登录、登出、改密、当前用户、用户 CRUD、角色与锁定                         | 基本可用；强制改密、锁定到期和本机密码依赖问题已在本轮修复，仍需 API 集成测试          |
| Apple ID 管理 | 列表、详情、新增、编辑、删除、Excel 导入、状态管理                            | 功能已接入；密码与密保存储、导入会话持久化和真实数据迁移未完成验收                     |
| 取机人管理    | 列表、详情、CRUD、导入/导出、批量生成联系方式/地址、批量绑定 Apple ID         | 主功能已接入；敏感字段校验和认证导出已修复，随机数据来源、并发绑定和动态标签仍待治理   |
| 订单管理      | 列表、真实详情页与详情弹窗、搜索、筛选、导出、付款人/截图更新、单笔与批量刷新 | 部分可用；独立 `/orders/:id` 已接真实 API，截图仍以 Data URL 进入 JSONB，不适合生产    |
| 邮件链路      | IMAP IDLE 监听、MIME/正文解析、多商品解析、来源白名单、入库、日志和重试框架   | 主链路来源校验、已读开关和重连已修；仍缺持久化幂等键、启动回溯和失败重放               |
| 官网爬虫      | axios + cheerio、代理、延迟、重试、541、身份校验、短事务、日志、自动刷新      | 已修 URL/响应身份与取消数量校验；仍缺租约锁、持久化调度和状态分级冷却                  |
| 统计与渠道    | 仪表板指标、趋势、商品/门店分布、渠道聚合和渠道订单                           | 主体已实现；本轮已移除默认 Mock 和每单 8000 元估算，仍需多币种与结算口径设计           |
| 前端管理台    | 登录、仪表板、订单、Apple ID、取机人、渠道、用户、改密、系统日志              | 页面齐全；权限按钮、请求竞态、通用错误态、真实详情和代码分包待收口                     |
| 部署与运维    | Dockerfile、Compose、Nginx、健康检查、Worker 启动入口和部署文档               | 文件已有；运行拓扑、构建上下文、端口、迁移职责和文档仍互相矛盾，不能视为已验收生产部署 |

### 1.2 当前审查结论

| 方向     | 结论                                                                   | 影响                                                               |
| -------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 业务功能 | MVP 页面和主链路已经有了，但多个“已完成”功能仍是局部实现或演示实现     | 不能直接按历史进度文档判定生产可用                                 |
| 数据契约 | 文档、Migration、Model、Controller 和前端字段名存在漂移                | 导入、筛选、统计、关联查询和敏感字段迁移容易产生错误数据           |
| 代码架构 | 分层目录已存在，但邮件和爬虫仍集中了获取、解析、校验、事务、调度和告警 | 难以独立测试，长时间网络操作会扩大数据库事务风险                   |
| 技术方案 | Express + React + PostgreSQL + Sequelize 仍适合当前规模，无需推倒重写  | 应优先增加持久化任务、对象存储、契约校验和可观测性，而不是更换框架 |
| 安全     | 当前树已开始脱敏和加密改造，但 Git 历史仍存在敏感文件记录              | 未清理历史、轮换凭据之前，不应导入新的真实数据或对外发布           |
| 质量     | 现有自动测试全部通过，但覆盖率仅约 38%，Lint 仍有数百个警告            | 需将“命令成功”与“业务验收”分开，核心失败分支仍缺保护               |

## 二、优化目标

### 2.1 核心目标

- 清除仓库中的敏感数据暴露，建立凭据、隐私字段和权限治理基线。
- 修复已确认会阻断新增、编辑、导入、导出、筛选和订单刷新的问题。
- 建立统一、可追踪的 API、状态枚举和数据库迁移契约。
- 使“邮件到达 → 解析 → 入库 → 官网同步 → 前端查询”形成可重试、可审计、可验证的闭环。
- 将 Web API、邮件监听和爬虫调度的运行职责逐步解耦。
- 建立可复现安装、自动测试、构建、迁移和发布门禁。
- 修订文档，使文档、代码和实际运行状态保持一致。

### 2.2 本计划暂不包含

- 未经需求确认的新业务模块。
- 大规模 UI 改版或品牌视觉重做。
- 移动端、小程序或多租户能力。
- 为追求技术新颖而全面替换 Express、React、PostgreSQL 或 Sequelize。
- 未经单独审批的生产数据迁移、生产凭据轮换、生产发布和历史数据清理。

## 三、执行原则

1. **安全问题优先**：P0 安全问题未关闭前，不继续导入真实账号、密码、身份证或邮件数据。
2. **权威文档先行**：涉及表结构或 API 契约的变更，先更新 `database/数据库架构.md` 或 `design/API设计.md`，再修改代码和测试。
3. **迁移优先于自动同步**：数据库结构只通过正式 Migration 演进，正常开发和部署不依赖 `sequelize.sync({ alter: true })`。
4. **单一契约**：状态枚举、字段命名、错误码和分页格式集中定义，前后端不再各自硬编码。
5. **小步交付**：每批变更只解决一个可独立验收的问题，避免安全整改、架构重构和业务扩展混在同一批次。
6. **证据分层**：单测通过、构建通过、接口联调、测试环境业务验收和生产验收分别记录，不互相替代。
7. **可回滚**：数据库迁移、凭据轮换、调度切换和部署变更都必须有回滚方案。

## 四、优先级定义

| 优先级 | 含义                                     | 处理规则                     |
| ------ | ---------------------------------------- | ---------------------------- |
| P0     | 已存在数据泄露、越权或可能造成不可逆损失 | 立即处理，阻断其他开发和发布 |
| P1     | 核心业务无法完成或会产生错误数据         | P0 后优先完成，阻断业务验收  |
| P2     | 可靠性、架构、性能和可维护性问题         | 在核心业务恢复后分批处理     |
| P3     | 文档、体验和一般工程优化                 | 不阻断业务，但纳入质量收口   |

## 五、分阶段实施计划

### 阶段 0：安全止血与资产确认

#### 阶段目标

停止新增风险，确认敏感数据暴露范围，为后续开发建立最低安全基线。

#### 任务清单

| 编号   | 优先级 | 任务                                                                                      | 主要产出                                   | 验收标准                                                                               |
| ------ | ------ | ----------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------- |
| SEC-01 | P0     | 盘点 Git 当前版本及历史中的数据库备份、Excel、环境配置和硬编码口令                        | 敏感文件清单、暴露范围、处置记录           | 当前树和完整历史均完成扫描；不在报告中输出秘密原文                                     |
| SEC-02 | P0     | 从仓库移除 `database_backup.sql`、`uploads/import/*.xlsx` 和真实环境配置，并清理 Git 历史 | 清理后的分支、历史清理方案、协作者同步说明 | 新克隆仓库无法检索到原敏感数据；远端分支和标签纳入处理范围                             |
| SEC-03 | P0     | 轮换数据库、JWT、邮箱、代理、Telegram、Apple ID 等可能暴露的凭据                          | 凭据轮换记录                               | 旧凭据失效；新凭据仅通过安全环境变量或密钥系统注入                                     |
| SEC-04 | P0     | 收紧敏感字段接口                                                                          | 脱敏 DTO、权限矩阵、访问审计               | 普通列表不返回密码、密保、完整身份证和地址；敏感导出仅限明确授权角色                   |
| SEC-05 | P0     | 设计敏感字段加密方案                                                                      | 加密设计、密钥版本策略、迁移方案           | Apple 密码和密保使用可轮换的可逆加密；身份证等字段按检索需求采用密文加盲索引或等效方案 |
| SEC-06 | P1     | 修复默认管理员和登录安全                                                                  | 安全种子、登录限流、强制改密               | 不再生成通用弱口令；首次登录必须改密；登录接口有 IP/账号双维度限流                     |
| SEC-07 | P1     | 建立最小权限模型                                                                          | 权限矩阵和路由授权中间件                   | 管理员、业务操作员、只读用户的读取、修改、导出、刷新权限清晰且有自动测试               |
| SEC-08 | P1     | 增加仓库敏感信息门禁                                                                      | Secret Scan 和提交检查                     | CI 能阻断数据库备份、私钥、环境文件和高置信度凭据进入仓库                              |

#### 阶段退出条件

- 2026-09-09 局部进展：`SEC-01/SEC-02/SEC-08` 已完成未推送 8 提交的归档脱敏和增量复扫，但已发布历史仍命中，完整历史治理未完成；`SEC-03` 仅形成轮换方案，未执行轮换。不得据此关闭本阶段；证据见[安全修复与门禁复核](../archive/2026-09/2026-09-09-本地安全修复与发布门禁复核.md)。
- 2026-09-09 后续：剩余 SheetJS 和前端依赖已修复，三类依赖审计零告警，固定制品校验与前端审计已纳入原 CI；`SEC-02/SEC-03` 仍受已发布历史维护窗口和外部账号轮换条件限制，未关闭。证据见[依赖风险收尾与构建回归](../archive/2026-09/2026-09-09-依赖风险收尾与构建回归.md)。
- 当前版本及历史中的真实敏感文件已完成处置。
- 可能暴露的生产凭据已轮换，旧凭据确认失效。
- 普通用户无法批量获得 Apple 密码、密保答案或完整身份证。
- 新提交具备敏感信息扫描门禁。

### 阶段 1：统一业务契约并修复核心阻断问题

#### 阶段目标

建立前端、API、模型和数据库之间的唯一契约，恢复新增、编辑、导入、导出、查询和刷新的完整功能。

#### 1.1 状态与字段契约

| 编号   | 优先级 | 任务                                                | 主要产出                         | 验收标准                                                                           |
| ------ | ------ | --------------------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------- |
| CON-01 | P1     | 集中定义 Apple ID、取机人、订单、校验和调度状态枚举 | 共享后端常量、API 文档、前端映射 | 控制器、模型、导入、筛选和前端不再分别硬编码状态                                   |
| CON-02 | P1     | 确定 API 字段命名规范                               | API 契约和兼容策略               | 建议 API 统一使用 `camelCase`、数据库保留 `snake_case`；迁移期旧字段有明确兼容期限 |
| CON-03 | P1     | 统一成功、错误和分页响应格式                        | 响应规范、错误码表、公共响应工具 | 所有业务路由使用同一结构；前端不再按页面猜测 `error`、`message` 或数据层级         |
| CON-04 | P1     | 为关键请求增加服务端 Schema 校验                    | 请求校验层                       | 新增、编辑、导入、批量刷新和筛选请求均在进入业务逻辑前完成校验                     |

#### 1.2 功能修复

| 编号   | 优先级 | 任务                                           | 主要产出                          | 验收标准                                                             |
| ------ | ------ | ---------------------------------------------- | --------------------------------- | -------------------------------------------------------------------- |
| FUN-01 | P1     | 修复 Apple ID 和取机人的默认状态与导入状态冲突 | Controller、Model、导入校验和测试 | 不传状态、传合法状态、传非法状态三类用例结果一致且可预测             |
| FUN-02 | P1     | 接通 Apple ID 和取机人新增弹窗                 | 前端真实 API 调用                 | 保存后数据库新增一条记录，列表自动刷新；失败时显示服务端校验信息     |
| FUN-03 | P1     | 修复取机人编辑字段错配                         | 统一 DTO 和编辑测试               | 姓名、身份证、联系方式、地址、状态、标签能够真实更新且回显正确       |
| FUN-04 | P1     | 修复 Excel 导入事务泄漏和重复校验缺失          | 导入会话、事务边界、文件清理      | 所有返回路径均提交或回滚；上传临时文件在成功和失败后都被清理         |
| FUN-05 | P1     | 重构 Excel 预览/执行协议                       | 服务端暂存或签名导入令牌          | 执行阶段不信任客户端篡改的预览数据；支持行数、大小和批次上限         |
| FUN-06 | P1     | 修复模板下载和取机人导出                       | 认证 Blob 下载                    | 不硬编码 localhost；Token 正确携带；生产路径和开发代理均可工作       |
| FUN-07 | P1     | 修复订单筛选、搜索和批量刷新协议               | 查询 DTO、真实筛选选项            | 页面展示的每个筛选项均由后端支持；指定订单批量刷新不会误刷新其他订单 |
| FUN-08 | P1     | 实现订单导出                                   | 导出接口和前端下载                | 导出内容服从筛选和权限规则；敏感字段默认脱敏；防止表格公式注入       |
| FUN-09 | P1     | 移除生产页面中的 Mock/演示逻辑                 | 真实仪表板、真实订单详情          | 默认展示真实 API；Mock 仅允许在测试或 Storybook 类环境使用           |
| FUN-10 | P1     | 修复渠道和取机人金额统计                       | 统一金额口径                      | 不再使用“每单 8000 元”或固定 0；金额来源、币种和缺失处理有明确规则   |

#### 阶段退出条件

- Apple ID、取机人可以通过页面新增、编辑、查询和删除。
- 两类 Excel 均能完成“模板下载 → 上传预览 → 执行 → 结果确认”。
- 订单筛选、导出、单笔刷新和指定批量刷新均通过接口测试。
- 仪表板、订单详情和渠道金额不再默认展示模拟结果。

### 阶段 2：数据库与文件存储收口

#### 阶段目标

消除模型、迁移和实际数据库之间的漂移，建立可重复创建和升级的数据库结构。

| 编号  | 优先级 | 任务                                                          | 主要产出                          | 验收标准                                                           |
| ----- | ------ | ------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------ |
| DB-01 | P1     | 以 `database/数据库架构.md` 为起点核对全部 Model 和 Migration | 差异清单、修订后的权威文档        | 文档、Model、全量迁移后的结构三者一致                              |
| DB-02 | P1     | 为 `payment_screenshot` 的 TEXT → JSONB 创建正式迁移          | 可回滚 Migration                  | 空值、单 URL、数组和历史异常值都有迁移测试；不再依赖一次性脚本     |
| DB-03 | P1     | 修复 `orders.tag` 长度等已知字段漂移                          | 正式 Migration                    | 新数据库和升级数据库的约束一致                                     |
| DB-04 | P1     | 停用正常流程中的 `sync({ alter: true })`                      | 迁移优先的初始化脚本              | 开发、测试、生产都从空库运行迁移得到相同结构                       |
| DB-05 | P2     | 评估并减少密码、身份证和地址快照冗余                          | 数据最小化方案                    | 仅保留有业务依据的快照；敏感数据变更和删除策略清晰                 |
| DB-06 | P1     | 重构付款截图存储                                              | 文件/对象存储接口、元数据表或字段 | 数据库只保存 URL、哈希、大小、类型和上传者；不再存储 Base64 大对象 |
| DB-07 | P2     | 建立备份恢复验证                                              | 脱敏测试备份、恢复脚本、记录      | 能在隔离环境完成备份恢复；备份加密并设定保留期限                   |

#### 数据迁移门禁

1. 迁移前生成备份并在隔离环境验证可恢复。
2. 对存量数据运行只读预检，统计异常值但不输出隐私原文。
3. 在测试数据库执行 `up → 校验 → down → up`。
4. 记录预计锁表时间和受影响行数。
5. 生产迁移与应用发布分别审批，失败时优先停止继续写入而不是强行推进。

### 阶段 2A：付款人与内部结算领域建模

业务边界、候选模型与待确认事项见[专项方案](付款人与内部结算方案.md)，未确认前不实施。

| 编号   | 优先级 | 任务                                           | 主要产出                              | 验收标准                                                                   |
| ------ | ------ | ---------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------- |
| PAY-01 | P1     | 确认付款人唯一性、归并和历史映射规则           | 业务规则与样例                        | 同名、更名、删除、禁用和历史订单的处理明确                                 |
| PAY-02 | P1     | 定义应结、部分结算、作废、争议、退款和冲正语义 | 状态机与金额公式                      | 每个状态转换都有前置条件、权限和审计记录                                   |
| PAY-03 | P1     | 更新数据库权威文档和 API 契约                  | 表、索引、约束、DTO 和错误码          | 文档评审通过后才创建 Migration                                             |
| PAY-04 | P1     | 设计结算交易对手主数据与付款责任关联           | Model、Migration、Service、API 和测试 | 与任务自由文本付款人分离，不把付款人建成登录账号，不按姓名自动猜测映射     |
| PAY-05 | P1     | 实现批次和明细台账                             | 结算领域服务、事务、幂等键和冲正机制  | 重复提交不重复结算，任一结果可追溯到原始订单和操作人                       |
| PAY-06 | P1     | 实现列表筛选、双状态详情和按付款人批量结算     | 前端页面和确认交互                    | 页面分开展示“Apple 支付”与“内部结算”，确认前展示订单数、币种、金额和异常项 |
| PAY-07 | P1     | 设计历史 `payer_name` 结算映射                 | 只读预检、候选映射和人工确认流程      | 无法确定的记录保持未映射，不自动合并同名付款人                             |
| PAY-08 | P2     | 建立结算对账和异常监控                         | 差异报表、指标和告警                  | 官网金额缺失、币种不同、超额结算、重复结算和取消后未冲正可被发现           |

### 阶段 2B：订单付款任务与逐用户权限（本地开发自测）

2026-09-07 第二轮反馈已明确：复用 users、不新增业务角色，由管理员逐用户分配细分权限；保留待处理／处理中／已完成／异常四态；不选择本人支付或外部分发；所有状态可登记订单付款人，第一版每单一名当前付款人且修正留痕；本人任务支持商品筛选、订单号搜索、逐行剩余付款时间。

付款流程、任务筛选/API 与 26 项业务验收见[付款任务方案](订单付款任务分配与处理方案.md)；现有全部模块权限拆分、管理员授权页面、端点门禁、存量映射和 13 项权限验收见[用户权限方案](用户权限分配与访问控制方案.md)。本阶段只维护订单上的外部付款人自由文本及变更审计，不建立付款人账号或候选主数据；完整资金与结算不纳入。

| 编号   | 优先级 | 任务                                          | 依赖与验收                                    | 状态                                       |
| ------ | ------ | --------------------------------------------- | --------------------------------------------- | ------------------------------------------ |
| ACL-01 | P1     | 权限目录、全路由矩阵和存量用户映射评审        | 用户复核细分能力与保留边界                    | 已定稿                                     |
| ACL-02 | P1     | 权威契约、权限表和 Migration                  | ACL-01；隔离 up/down/up、幂等迁移通过         | 开发自测通过                               |
| ACL-03 | P1     | 逐用户服务端授权、全部端点及字段范围门禁      | ACL-02；撤权、多副本和范围回归通过            | 核心实现与单库并发通过；多副本未验收       |
| ACL-04 | P1     | 用户权限配置、当前权限、导航／路由／按钮      | ACL-03；仅付款账号和跨模块账号流程通过        | 实现及构建通过；浏览器人工验收未执行       |
| ACL-05 | P1     | 权限矩阵、存量迁移和回退演练                  | ACL-03、ACL-04；不扩大授权、不恢复已撤销访问  | 存量映射与隔离回退通过；完整矩阵待独立验收 |
| PD-01  | P1     | 复核付款入口、时限、算法和付款人输入规则      | 四态、单人姓名和自由文本已确认，不重复确认    | 已定稿                                     |
| PD-02  | P1     | 数据库、API、订单付款人业务契约               | PD-01、ACL-01；旧付款人写入口兼容明确         | 已完成                                     |
| PD-03  | P1     | 配置、任务、订单付款人姓名与审计              | PD-02、ACL-02；正式 Migration 及历史兼容通过  | 开发自测通过                               |
| PD-04  | P1     | 任务生成、补偿、自动／手动分配                | PD-03；无漏单、重复、正常超额与撤权竞态       | 150 单与撤权竞态通过                       |
| PD-05  | P1     | 状态／备注、全状态付款人登记、链接与交接 API  | PD-03、ACL-03；关联、权限、幂等与版本冲突通过 | 核心开发自测通过                           |
| PD-06  | P1     | 本人任务、商品／订单号查询、逐行时间和调度 UI | PD-04、PD-05、ACL-04；实际浏览器闭环通过      | 实现及构建通过；浏览器人工验收未执行       |
| PD-07  | P1     | 功能、权限回归和峰值技术／业务演练            | PD-04 至 PD-06、ACL-05；证据分层记录          | 150 单技术演练通过；300 单和真实业务未验收 |
| PD-08  | P1     | 操作说明、发布及回退准备                      | PD-07；用户验收，发布按后续授权               | 当前文档已同步；发布未授权                 |

本阶段 PD-03 只承接订单外部付款人姓名和审计，不建立付款人实体；PAY-04 留待结算业务另行确认稳定交易对手模型。PAY-01 中每单单人自由文本规则已确认，历史姓名不自动归并；PAY-02 的资金语义不阻塞当前付款人登记。生产环境未执行 Migration 或业务数据写入。

### 阶段 3：邮件处理链路可靠性

#### 阶段目标

确保邮件不会因为短暂故障、进程重启、重复投递或解析失败而静默丢失。

2026-09-09 收信时效修复：MAIL-02、MAIL-03、MAIL-05、MAIL-08 已补充 UID 扫描、持久化进度、received 中断恢复、连接命令截止及扫描健康指标；本地单测、真实 node-imap 回环协议和隔离数据库验证通过，已于 2026-09-09 生产发布并验证真实 UID 回查和连续扫描；新到邮件时效验收尚无新样本。

2026-09-07 已完成 MAIL-01 至 MAIL-08 的本地开发实现：严格全量商品解析、标签边界和 `+08:00`，解析前加密留痕，复合 IMAP 幂等键，Message-ID/MIME 指纹，固定每分钟且最多 3 次的持久化重试，IMAP 确认重试，首次 24 小时回查与持久化 UID 续扫（2026-09-09 替换原 10 分钟补查为 30 秒兜底），独立 Worker 心跳与积压指标，以及 admin 专属的邮件人工处理页面/API。2026-09-09 生产真实邮件发现收件邮箱被误配为唯一精确 From 白名单，两个动态 `lanu.cn` 发件地址被错误忽略；本轮增加标准化完整域名精确白名单、稳定拒绝原因，并把主题命中但 From 未授权的疑似订单邮件保留为 `manual_review`。地址／域名任一命中即通过，两者均为空时接受任意标准化 From；严格订单结构和字段校验仍是建单门禁。实现、权限决策、自动化证据和未验证边界见[邮件解析与处理可靠性优化方案](邮件解析与处理可靠性优化方案.md)。

| 编号    | 优先级 | 任务                             | 主要产出                                    | 验收标准                                                                                                              |
| ------- | ------ | -------------------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| MAIL-01 | P1     | 建立可选发件人限制和严格内容门禁 | 邮件过滤策略                                | 地址／完整域名白名单任一命中；空白名单接受任意 From；拒绝原因可追踪，疑似订单误判可恢复；正文或字段不合法不能创建订单 |
| MAIL-02 | P1     | 修复批处理完成计数和标记已读策略 | 流程修复和回归测试                          | 每封邮件只完成一次；非订单邮件是否标记已读由配置和专用邮箱策略决定                                                    |
| MAIL-03 | P1     | 实现启动回溯和定时补漏           | 持久化回溯任务                              | 首次回查 24 小时；事件与 30 秒 UID 扫描均覆盖已读，重连／重启保留进度，失败不越过缺口，重复不重复建单                                        |
| MAIL-04 | P1     | 建立稳定幂等键                   | 邮箱、UIDVALIDITY、UID、Message-ID 组合策略 | 邮箱重建 UID 或重复投递时仍能识别已处理邮件                                                                           |
| MAIL-05 | P1     | 持久化重试和失败状态             | 重试字段、失败队列、人工重放接口            | 重启后重试次数不丢失；超过阈值的邮件进入可查询、可告警、可重放状态                                                    |
| MAIL-06 | P1     | 所有失败路径写入邮件日志         | 统一处理记录                                | MIME 解析失败、业务校验失败、数据库失败都能追踪原始邮件摘要和错误原因                                                 |
| MAIL-07 | P1     | 恢复 TLS 证书验证和启动配置校验  | 安全 IMAP 配置                              | 缺少必需配置时服务不启动；生产默认验证证书                                                                            |
| MAIL-08 | P2     | 增加邮件监听运行状态和指标       | 健康状态 API、指标                          | 可查看连接、心跳、最后收信／成功、积压和连续失败；扫描成功时间与心跳独立，超 90 秒或扫描错误不视为健康                                                          |

#### 必测场景

- 单商品、多商品、HTML 实体、换行变化、字段缺失和未知模板。
- 重复邮件、乱序邮件、邮件连接断开和服务重启。
- 数据库短暂不可用、解析永久失败和重试后成功。
- 非订单邮件、仅伪造主题的邮件、空白名单任意 From，以及配置白名单后的匹配与不匹配邮件。

### 阶段 4：爬虫与调度器治理

2026-09-09 来源冲突增量：按用户确认加入微信／支付宝别名、iPhone 名称语义比较与多商品唯一匹配；历史重校验仅清除已证实等价的付款方式／名称冲突。属于 CRAWL-07 后续优化，本地验证与生产边界见[开发进度](../development/开发进度.md)，规则见[订单爬虫](../design/订单爬虫.md#来源表达差异与真实冲突)。

2026-09-09 生命周期增量已完成本地实现和开发自测，补充 CRAWL-06 的合成结构 Fixture／JSON 优先解析，以及 CRAWL-07 的官网零数量权威与来源冲突新规则；没有把真实代理、持续时效或生产验收标为完成。证据见[本轮开发自测](../archive/2026-09/2026-09-09-订单生命周期改造开发自测.md)。

#### 阶段目标

降低官网结构变化、并发刷新、代理故障和高频调度造成的数据错误与风控风险。

2026-09-09 最新确认：未付款／支付待确认订单每分钟刷新；已付款订单仅手动刷新，打开页面不再访问官网。生命周期、官网字段权威和来源冲突按[生命周期方案](真实订单生命周期与官网数据映射优化方案.md)实施；不新增状态历史表。详细设计、任务拆分和验收标准见[专项重构方案](订单状态刷新与爬虫重构方案.md)。

| 编号     | 优先级 | 任务                                | 主要产出                                       | 验收标准                                                                   |
| -------- | ------ | ----------------------------------- | ---------------------------------------------- | -------------------------------------------------------------------------- |
| CRAWL-01 | P1     | 缩短数据库事务范围                  | 新事务边界                                     | 网络请求和重试不占用数据库事务；事务仅包裹最终校验和写入                   |
| CRAWL-02 | P1     | 增加订单刷新锁和幂等控制            | 行锁或租约锁                                   | 人工刷新与定时刷新不能同时更新同一订单                                     |
| CRAWL-03 | P1     | 引入刷新冷却、批次和并发上限        | 调度策略                                       | 不再每 10 秒无差别扫描并刷新全部活跃订单；不同状态使用不同刷新频率         |
| CRAWL-04 | P1     | 持久化调度暂停和风控状态            | 系统设置或调度状态表                           | 进程重启后暂停原因和恢复要求仍然存在                                       |
| CRAWL-05 | P1     | 验证返回页面身份                    | 订单身份校验                                   | 页面订单号或关键身份不匹配时禁止更新数据库并触发告警                       |
| CRAWL-06 | P1     | 拆分解析器并建立 HTML 样本回归库    | 解析适配器、脱敏 Fixture                       | JSON 主路径和 HTML 降级路径均有测试；官网结构变化能快速定位                |
| CRAWL-07 | P1     | 修正取消订单商品数量校验            | 状态感知校验规则                               | 官网有效零数量覆盖当前数量，导入差异留存并提示，不因普通冲突停止未付款轮询 |
| CRAWL-08 | P2     | 统一代理生命周期                    | 代理状态模型                                   | 失败次数、临时隔离、过期、刷新和 541 永久禁用策略一致且可测试              |
| CRAWL-09 | P2     | 拆分爬虫服务职责                    | Fetch、Parse、Validate、Persist、Schedule 模块 | 单文件不再同时承担全部职责，各模块可以独立单测                             |
| CRAWL-10 | P2     | 将邮件和爬虫任务从 Web API 进程解耦 | Worker 进程和持久化任务队列                    | API 重启不丢任务；Worker 可独立停止、恢复和扩容                            |

2026-09-07 实施进度：CRAWL-01、02、03、04、05、07、08、10 已完成开发实现并通过相应单元或隔离 PostgreSQL 验证；CRAWL-06 与 CRAWL-09 仍未闭环，Parse、Validate、Persist 仍位于兼容门面且缺少脱敏 HTML Fixture。真实订单、浏览器业务流程与生产验证继续以[专项重构方案](订单状态刷新与爬虫重构方案.md)中的 REFRESH-12 为独立授权边界。

#### 推荐状态刷新策略

刷新策略已明确为：

- 明确未付款每 60 秒刷新；未知支付仅待核实阶段且付款窗口内核实。历史空支付及付款后阶段停止，已排队任务执行前复核。2026-09-09 已修复并发布 8189a35，39 套件／322 项通过，旧自动队列收敛及指定订单手动刷新生产复验通过；规则与时间优先级见[爬虫说明](../design/订单爬虫.md#同步流程)。
- 已付款：停止定时刷新，仅手动刷新。
- 手动刷新：支持单个和全部订单，仍需经过任务去重、锁、权限、限速和审计。
- 页面打开：只读缓存，旧 page_open 入口返回空任务结果。
- 校验异常、连续风控、身份不匹配：保留旧数据，显示数据过期或异常并告警。
- 调度使用 PostgreSQL 持久化任务、批次和 Worker 租约；具体规则以[专项重构方案](订单状态刷新与爬虫重构方案.md)为准。

### 阶段 5：前端业务闭环与体验治理

#### 阶段目标

让页面只展示真实、可解释的数据，并正确处理权限、敏感信息和异常状态。

| 编号  | 优先级 | 任务                                 | 主要产出             | 验收标准                                                     |
| ----- | ------ | ------------------------------------ | -------------------- | ------------------------------------------------------------ |
| FE-01 | P1     | 移除生产默认 Mock 数据和演示保存逻辑 | 环境隔离的 Mock 方案 | 生产构建中无法切换到假数据；所有保存按钮产生真实请求         |
| FE-02 | P1     | 接通真实订单详情页                   | 详情 API 页面        | `/orders/:id` 展示真实订单，加载、无权限、404 和失败状态完整 |
| FE-03 | P1     | 修复筛选、搜索、导出和截图展示       | 页面与 API 联调      | UI 文案与实际搜索范围一致，多图可逐张查看                    |
| FE-04 | P1     | 实现安全文件上传                     | 上传组件和进度状态   | 文件类型、大小、数量、哈希和权限在前后端双重校验             |
| FE-05 | P1     | 敏感信息默认脱敏                     | 通用脱敏组件         | 页面不在 DOM 或接口响应中预先携带无权查看的明文              |
| FE-06 | P2     | 统一错误、加载、空状态和成功反馈     | 公共反馈组件         | 页面不再使用 `alert` 和大量 `console`；错误保留可追踪请求 ID |
| FE-07 | P2     | 修复 Hooks 依赖和请求竞态            | Hooks 重构           | 快速切换筛选不会由旧请求覆盖新结果；Lint 无 Hooks 警告       |
| FE-08 | P2     | 恢复前端设计规范一致性               | 样式检查             | 移除遗留深色主题类，列表页、按钮、徽章和表单遵循现有设计规范 |

### 阶段 6：工程质量、部署与文档收口

#### 6.1 测试与 CI

| 编号  | 优先级 | 任务                   | 主要产出               | 验收标准                                                                 |
| ----- | ------ | ---------------------- | ---------------------- | ------------------------------------------------------------------------ |
| QA-01 | P1     | 调整 ESLint 范围       | `.eslintignore` 和脚本 | 不扫描 `frontend/dist`、依赖和明确归档的实验脚本；活跃代码 0 error       |
| QA-02 | P1     | 提交并维护依赖锁文件   | 根目录和前端 lockfile  | 全新环境使用确定性安装命令可复现依赖树                                   |
| QA-03 | P1     | 建立后端单元和集成测试 | Jest + 隔离 PostgreSQL | 认证、邮件、导入、订单和迁移主路径有自动测试；核心业务覆盖率不低于 70%   |
| QA-04 | P1     | 建立前端组件和接口测试 | 前端测试框架           | 新增、编辑、筛选、导入、导出、权限和异常状态均有测试                     |
| QA-05 | P1     | 建立端到端测试         | 脱敏/合成数据 E2E      | 登录 → 导入 → 邮件入库 → 订单查询 → 模拟官网同步主流程可重复执行         |
| QA-06 | P1     | 建立 CI 门禁           | CI 工作流              | 格式、Lint、单测、迁移、前端构建、依赖审计、Secret Scan 全部通过才可合并 |
| QA-07 | P2     | 治理高风险依赖         | 升级或替换计划         | 高危无修复依赖有替代评估和迁移测试，不直接执行破坏性自动升级             |

#### 6.2 部署与运行

| 编号   | 优先级 | 任务                            | 主要产出                  | 验收标准                                                          |
| ------ | ------ | ------------------------------- | ------------------------- | ----------------------------------------------------------------- |
| OPS-01 | P1     | 确定唯一生产运行拓扑            | 部署架构决策              | Docker、PM2、Nginx 的职责不再重叠；端口和目录统一                 |
| OPS-02 | P1     | 修复后端、前端和 Nginx 端口契约 | 配置和自动检查            | Compose、应用监听、前端 API 和 Nginx upstream 完全一致            |
| OPS-03 | P1     | 分离 liveness 与 readiness      | 健康检查接口              | 数据库不可用时 readiness 返回非 2xx；进程存活和业务可用状态可区分 |
| OPS-04 | P1     | 建立显式迁移任务                | 发布流程                  | 应用启动不自动变更表结构；迁移成功后才切换应用版本                |
| OPS-05 | P1     | 规范异常退出和优雅关闭          | 进程生命周期策略          | 未捕获异常记录后安全退出，由编排系统重启；邮件和任务停止可验证    |
| OPS-06 | P2     | 增加可观测性                    | 指标、日志、告警和请求 ID | 可定位邮件积压、爬虫失败率、代理耗尽、DB 连接和 API 错误率        |
| OPS-07 | P2     | 建立测试环境发布与回滚          | 发布清单、回滚演练        | 每次生产发布前在测试环境完成迁移、冒烟和回滚验证                  |

#### 6.3 文档治理

| 编号   | 优先级 | 任务                                   | 主要产出                | 验收标准                                                     |
| ------ | ------ | -------------------------------------- | ----------------------- | ------------------------------------------------------------ |
| DOC-01 | P1     | 重写根 README 的阶段、路径和启动说明   | 最新 README             | 新成员按 README 可以完成安装、迁移、启动和测试               |
| DOC-02 | P1     | 修复文档一致性脚本和 Pre-commit 路径   | 可用检查脚本            | 权威文档缺失或字段不一致时返回非 0 退出码                    |
| DOC-03 | P1     | 清理“完成”与“待开发”互相矛盾的进度记录 | 当前状态摘要 + 历史归档 | `development/开发进度.md` 只保留当前结论，旧记录进入 archive |
| DOC-04 | P2     | 建立架构决策记录                       | ADR 目录                | API 命名、加密、任务队列、文件存储和部署拓扑均有决策依据     |
| DOC-05 | P2     | 自动检查失效链接和过期路径             | 文档 CI                 | 不再引用已不存在的 `SCHEMA.md`、旧 API 文档或错误脚本路径    |

## 六、目标架构

建议在不改变主要技术栈的前提下逐步演进为：

```text
邮件服务器
    │
    ▼
邮件 Worker ──► 持久化任务/失败队列 ──► 订单领域服务
                                         │
Apple 官网 ◄── 爬虫 Worker ◄─────────────┤
                                         │
                                         ▼
前端 ──► Nginx ──► Web API ─────────► PostgreSQL
                         │               │
                         ├──────────────► 文件/对象存储
                         └──────────────► 指标、日志与告警
```

职责边界：

- **Web API**：认证、授权、查询、人工操作和任务提交，不直接承担长期监听任务。
- **邮件 Worker**：连接邮箱、过滤、解析、幂等入队和失败重放。
- **爬虫 Worker**：代理、限速、抓取、解析、校验和结果提交。
- **订单领域服务**：统一执行状态转换、数据权威性和写入规则。
- **持久化任务层**：保存任务状态、重试次数、租约和失败原因。
- **文件存储**：保存付款截图等二进制文件，数据库只存元数据。
- **可观测性层**：统一请求 ID、任务 ID、结构化日志、指标和告警。

## 七、测试矩阵

| 层级              | 覆盖重点                                             | 是否阻断发布       |
| ----------------- | ---------------------------------------------------- | ------------------ |
| 单元测试          | 邮件字段解析、状态映射、商品匹配、金额解析、权限判断 | 是                 |
| 数据库集成测试    | Model、Migration、事务、唯一约束、加密读写           | 是                 |
| API 契约测试      | 请求字段、响应结构、权限、分页、错误码               | 是                 |
| Worker 集成测试   | 邮件重试、任务幂等、爬虫锁、暂停恢复                 | 是                 |
| 前端组件测试      | 表单校验、敏感展示、筛选、上传和异常状态             | 是                 |
| 端到端测试        | 登录、导入、订单入库、查询和刷新                     | 是                 |
| 真实邮箱/官网验收 | 脱敏真实样本、代理和官网字段适配                     | 上线前必须人工确认 |
| 生产验收          | 实际邮件、订单、告警和回滚                           | 单独授权后执行     |

覆盖率不是唯一验收条件。核心业务逻辑目标不低于 70%，同时必须覆盖失败分支、权限边界、重复处理、事务回滚和状态转换。

## 八、推荐实施批次

### 第一批：建立安全且可开发的基线

建议首先完成：

1. `SEC-01`～`SEC-04`：敏感数据与权限止血。
2. `CON-01`～`CON-03`：统一状态、字段和响应契约。
3. `FUN-01`、`FUN-03`、`FUN-04`：修复确定性数据错误和事务问题。
4. `DB-01`～`DB-04`：恢复迁移可信度。
5. `QA-01`、`QA-02`、`DOC-01`、`DOC-02`：建立可复现开发环境。

第一批完成前，不建议开发新业务功能。

### 第二批：恢复前后端业务闭环

建议完成 `FUN-02`、`FUN-05`～`FUN-10`、`FE-01`～`FE-05`，并补齐对应 API 集成测试和前端测试。

完整内部结算工作先执行 `PAY-01`～`PAY-03` 的资金需求与契约确认；未确认应结口径、退款责任和审批权限前，不创建结算表或结算历史映射脚本。最小付款人实体及订单关联按阶段 2B 的 PD-01 至 PD-03 复核后实施，不依赖完整结算语义；历史姓名仍不得自动归并。

### 第三批：增强自动化可靠性

建议完成邮件和爬虫 P1 任务，先保证不丢邮件、不重复建单、不并发覆盖、不高频失控，再进行 Worker 解耦。

### 第四批：架构、部署和长期治理

完成 Worker 拆分、文件存储、可观测性、部署收口、依赖治理和文档历史归档。

## 九、风险与前置决策

以下事项需要在对应开发开始前确认：

| 决策         | 需要确认的内容                                       | 默认建议                                                           |
| ------------ | ---------------------------------------------------- | ------------------------------------------------------------------ |
| API 字段规范 | 是否兼容已有外部客户端                               | 前后端统一 `camelCase`，数据库保持 `snake_case`，提供短期兼容层    |
| 敏感字段检索 | 哪些字段必须支持精确查询                             | 密文存储，必要字段增加标准化哈希/盲索引                            |
| 用户权限     | 已确认复用账号且不新增角色；具体功能权限和数据范围   | 见用户权限方案；管理员逐用户授权，存量角色仅作兼容，权限明细待复核 |
| 文件存储     | 本地磁盘、S3 兼容存储或云对象存储                    | 优先选择支持私有对象和临时签名 URL 的对象存储                      |
| 任务队列     | 数据库任务表或独立队列                               | 规模较小时先用 PostgreSQL 持久化任务表，保留后续替换接口           |
| 刷新频率     | 不同订单状态的时效要求和代理预算                     | 按状态分级，不采用全量 10 秒扫描                                   |
| 部署拓扑     | 数据库是否与应用同机、Nginx 是否宿主机运行           | 选择一种生产拓扑并形成唯一文档，不混用 PM2 与容器进程管理          |
| 历史数据     | 订单快照和原始邮件的法定/业务保留期限                | 数据最小化并设置自动到期清理                                       |
| 付款人唯一性 | 同名、更名、平台账号和联系方式哪一项可作为唯一依据   | 使用内部主键，不仅依赖姓名自动合并                                 |
| 结算口径     | 应结金额、部分结算、佣金/费用、退款/冲正和多币种规则 | 独立结算台账，不从 Apple `paymentStatus` 自动推导                  |
| 结算权限     | 创建、确认、作废、冲正、导出是否需要不同角色或复核   | 至少将“编辑”和“确认/冲正”拆成不同权限                              |

## 十、完成定义（Definition of Done）

每项优化只有同时满足以下条件才能标记完成：

- 需求、数据来源和权限边界已确认。
- 受影响的权威文档先于或同步于代码更新。
- 实现代码遵循 `development/编码规范.md` 和前端设计规范。
- 对应单元、集成或端到端测试已新增并通过。
- Lint、格式检查、测试和构建通过。
- 数据库变更具备正式 Migration、回滚和存量数据预检。
- 日志不包含密码、密保、完整身份证、完整订单 URL 或代理凭据。
- 已验证失败场景，而不仅是成功场景。
- 测试环境验收结果与生产验收结果分开记录。
- `development/开发进度.md` 已记录完成内容、关键决策、遗留风险和下一步。

## 十一、当前基线验证记录

本计划最新一次本地验证基于 `main@0fd7f80` 及当前未提交工作区：

- 后端 Jest：12 个测试套件、49 个测试全部通过。
- Jest 覆盖率：Statements 36.29%、Branches 30.25%、Functions 34.29%、Lines 36.66%，仍显著低于核心业务 70% 目标。
- 后端 ESLint：0 error、686 warning；命令成功不等于警告已治理。
- 前端 ESLint：0 error、56 warning。
- 全仓库 Prettier 检查仍有 97 个文件不符合格式；本轮已修复“Prettier 自动双引号与 ESLint 单引号规则冲突”，但历史文件尚未做批量格式化。
- 前端 Vite 生产构建成功；主 JS 包约 792.37 kB（gzip 226.95 kB），仍有超过 500 kB 的分包警告。
- 文档一致性检查通过，已识别 6 个模型。
- 在 arm64 本机已可加载全部 Sequelize 模型；密码库改用纯 JavaScript 的 `bcryptjs`，避免之前 x86_64 原生模块阻断启动。
- 根目录 `npm audit`：18 项（6 moderate、12 high），其中 `node-imap` 和 `xlsx` 当前无直接修复版本，需替代方案评估。
- 前端 `npm audit`：9 项（3 moderate、6 high），包含 Vite、React Router、PostCSS 等升级项。

本轮已在本地工作区实施但尚未进行真实数据库迁移或业务联调的修复：

| 项目           | 本地结果                                                                                  | 仍需验证                                         |
| -------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------ |
| 取机人敏感字段 | 从解密后明文校验身份证，派生后四位和盲索引；安全迁移中移除旧明文触发器                    | 备份后在隔离数据库执行迁移和回滚演练             |
| Sequelize 关联 | 关联统一使用 camelCase 模型属性，不再隐式生成重复 snake_case 属性                         | 真实数据库关联查询与导入/刷新联调                |
| 认证边界       | 业务 API 统一执行认证和强制改密检查；过期临时锁定在签发 token 前恢复；系统日志限管理员    | 登录、改密、三角色全路由 API 契约测试            |
| 取机人导出     | 使用统一 API 客户端携带 JWT 下载 Blob，移除硬编码 localhost                               | 浏览器手工下载、大数据量和敏感导出权限           |
| 仪表板与渠道   | 移除默认 Mock 和每单 8000 元估算，使用 `official_order_amount` 和官网支付状态             | 实际数据口径、金额缺失、币种和大数据量查询性能   |
| 统计 SQL       | 修正产品聚合引用不存在的 `orderId` 字段                                                   | PostgreSQL 集成测试                              |
| 邮件监听       | 可选发件人限制、显式已读开关、单次完成回调、异常重连和主动停止语义已修复                  | 真实专用邮箱、断线恢复、回溯和失败重放           |
| 订单爬虫       | 校验 Apple URL 与官网返回订单号；网络请求移出事务；锁行并拒绝并发旧结果；取消数量特殊处理 | 脱敏 HTML 样本、代理、并发刷新和 PostgreSQL 联调 |
| Worker 控制    | 外置 Worker 时只返回日志观测状态，恢复接口明确返回 409，不再伪造内存控制成功              | 持久化租约、心跳和跨进程控制                     |
| 前端详情       | `/orders/:id` 已接入真实详情 API，补齐加载、错误、空数据和脱敏展示                        | 浏览器联调、组件测试和代码分包                   |
| 工程格式       | ESLint 允许 Prettier 为避免转义而选择双引号，修复 format 后 lint 必然失败的问题           | 仍有 97 个历史文件需要独立格式化批次             |

上述结果只能证明当前仓库的静态与本地自动测试状态，不代表真实数据库、邮箱、Apple 官网、代理、Telegram 或生产业务已经验收。

## 十二、计划维护方式

- 本文档负责记录总体阶段、优先级、依赖和验收标准。当前文档治理结果见[整理记录](../development/文档整理记录.md)。
- `development/开发进度.md` 负责记录每次实际开发后的结果和证据。
- `database/数据库架构.md` 是数据库结构的唯一权威来源。
- `design/API设计.md` 应升级为 API 契约的唯一权威来源。
- 任务开始时从本文档选择编号；完成后在开发进度中引用同一编号。
- 若任务范围、业务规则或技术决策发生变化，应先修改本计划或对应权威文档，再开始实现。
