# 桌面紧凑布局生产发布记录

> 状态：生产前端发布与只读技术验收完成
>
> 日期：2026-09-12（北京时间）
>
> 代码提交：`main@5d39cdac8a0a37dd7a6eadcc36379a0bdef981dc`
>
> 制品：`20260911T211347Z-ca57d7c44bbe`；tree：`ca57d7c44bbe9a52d17c4bd50e9ff99bbdbaa5d5`；干净 main、linux/amd64

## 范围与验证

用户授权提交和部署。8 个文件完成暂存范围、未跟踪文件和敏感凭据模式核对后提交；未推送远程。公共字体、控件、导航与页面留白收紧，本人付款任务使用自适应表格和窄桌面行详情，不涉及业务接口、权限或数据库变化。

- 提交前 Jest 488 项通过，117 项环境门控跳过；前后端 Lint 均无错误，保留 1717／41 项提示性警告。
- 合成浏览器十种宽度通过，覆盖表格展开前后无溢出、操作可见、跨断点草稿、备注／付款人保存、只读权限、手机导航和单项／批量操作。真实业务 API 均被拦截。
- 本地标准脚本构建完整制品，两端 SHA-256 校验通过；前端 Vite 构建通过，保留已有大包提示。

## 备份、切换与回滚

备份目录：`/var/backups/apple-order-mgr/compact-ui-20260911T211347Z-ca57d7c44bbe/`。保存旧 current 指针、旧前端归档及 SHA-256、部署清单、全部容器 ID／镜像／启动时间、共享主进程、生产环境文件摘要、健康及公网资源核验结果；归档目录清单可读性检查通过。

仅切换 Nginx 已有静态目录指针，不改 Nginx 配置，不重启 API、Worker、数据库或其他项目。完整制品中的镜像归档只上传备用，未加载或切换后端。保留旧前端 assets 的内容哈希文件，支持仍打开旧页面的浏览器；继承文件不属于新制品清单，新清单中的文件均已逐项验证。

首次切换后，公网校验脚本因服务器 Python 默认 ASCII 编码无法读取含中文的 HTML，触发预设自动回滚。明确使用 UTF-8 后重试，于北京时间 **05:16:04** 成功切换并通过全部检查。该过程仅切换静态指针，服务和数据库未操作。

回滚目标：`/var/www/apple-order-mgr/releases/20260911T180234Z-856e6f84be07`。回滚只需原子恢复 current 指针。API 和两个 Worker 仍使用该旧制品镜像（代码 `61afd6c`），不能依据新前端目录的 release.env 推断当前后端版本。本次未执行数据库备份、恢复或 Migration，也不需要数据库回滚。

## 生产只读验收

- 回环和 HTTPS readiness 健康；公网 HTML、JS、CSS 与新制品逐字节一致。
- JS：`index-eEzH3ogE.js`，CSS：`index-B_9bchxN.css`；摘要保存在备份目录的 `public-assets.sha256`。
- 全部容器 ID、镜像、启动时间、共享主进程与生产 app.env 摘要均与切换前一致。
- 独立 Chrome 标签页沿用现有管理员会话，读取本人付款任务成功。未重新登录、接管会话或点击保存、复制、刷新、批量操作。
- 生产实测：1366／1440／1920 CSS 像素视口下页面宽度分别等于视口；表格 clientWidth 与 scrollWidth 分别同为 1124／1198／1678px。侧栏 208px、主标题 24px，右侧操作可见，已查看真实页面截图。验证后恢复浏览器尺寸。

本轮完成前端技术发布和只读视觉核对，不替代真实付款、移动端软键盘和业务验收。合成截图与执行日志位于本机 `/tmp/apple-compact-qa/`；长期复现入口见[测试指南](../../testing/测试与验收指南.md#h5-登录与付款任务回归)。
