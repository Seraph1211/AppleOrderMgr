# 快速运维手册

> 状态：当前有效
>
> 最近核对：2026-09-06
>
> 适用环境：`/var/www/apple-order-mgr/current`

## 日常只读检查

```bash
cd /var/www/apple-order-mgr/current
docker compose --env-file release.env -f docker-compose.prod.yml ps
docker compose --env-file release.env -f docker-compose.prod.yml logs --tail=100 api
docker compose --env-file release.env -f docker-compose.prod.yml logs --tail=100 email-worker
docker compose --env-file release.env -f docker-compose.prod.yml logs --tail=100 crawler-worker
curl -i http://127.0.0.1:3001/api/health/live
curl -i http://127.0.0.1:3001/api/health/ready
curl -I https://apple.godp.me/
```

`live` 只说明 API 进程存活，`ready` 只额外验证数据库连通。两者都不能证明邮件、代理和 Apple 官网同步正常。

## 服务操作

只重启明确受影响的服务：

```bash
docker compose --env-file release.env -f docker-compose.prod.yml restart crawler-worker
```

更新 Compose 服务时使用：

```bash
docker compose --env-file release.env -f docker-compose.prod.yml up -d api email-worker crawler-worker
```

不要运行不带项目范围的 Docker 清理、停止或重启命令。PostgreSQL 和 Nginx 是宿主机共享服务，不能随应用 Compose 操作。

### 网帆套餐扩容

先在供应商控制台选中目标套餐，核对并发、粘性时长与该套餐的接入账密。新旧套餐可同时有效，不能仅修改并发数就认为已经切到新套餐。对应参数与提速验收条件见[订单爬虫](../design/订单爬虫.md#网帆-20-并发套餐配置)。

备份权限为 `600` 的 `shared/app.env` 后，更新账密及 `20/20/5/3` 四项参数。环境变量更新需要重新创建 Worker，单纯 `restart` 不会重新读取 Compose 的 `env_file`：

```bash
docker compose --env-file release.env -f docker-compose.prod.yml up -d --no-deps --force-recreate crawler-worker
```

只核查脱敏配置、启动日志中的会话槽数和并发数、系统代理就绪状态及真实任务结果；不要输出完整环境或代理 URL。失败时恢复配置备份，再重新创建同一 Worker。配置调整不需要重建镜像、迁移数据库或重启共享服务；若 API 进程也需要读取新的代理参数，应单独核对其用途再决定更新范围。

## 常见症状

| 症状          | 优先检查                                                                                                                           |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 前端打不开    | `current` 链接、静态文件、Nginx 站点配置和证书                                                                                     |
| API 502       | `api` 容器、`127.0.0.1:3001`、Nginx upstream                                                                                       |
| readiness 503 | PostgreSQL 连接、环境变量、Migration 状态                                                                                          |
| 邮件未入库    | `email-worker` 日志、IMAP 网络、授权码、精确地址／域名白名单、`SUBJECT_NOT_ALLOWED`／`SENDER_NOT_ALLOWED`、主题/正文格式、未读状态 |
| 官网未更新    | `crawler-worker` 日志、自动刷新开关、代理、风控暂停状态                                                                            |
| 登录失败      | 用户状态、JWT 配置、是否要求首次改密                                                                                               |

## 日志与磁盘

应用日志写入 Docker stdout/stderr，并由 `json-file` 限制为每个容器最多 5 个 10 MB 文件。查看磁盘前先限定项目：

```bash
docker compose --env-file release.env -f docker-compose.prod.yml ps -q
docker system df
find /var/www/apple-order-mgr/releases -maxdepth 2 -type f -printf '%s %p\n' | sort -n | tail
```

发布目录和数据库备份的删除必须先列出版本、确认当前 `current` 指向和回滚保留策略，再单独执行。

## Nginx 与数据库

站点配置变更后：

```bash
nginx -t
systemctl reload nginx
```

数据库备份、恢复和迁移遵循[数据库初始化与迁移指南](../database/数据库初始化与迁移指南.md)。不要通过删除表、Migration 记录或数据库目录来处理迁移故障。
