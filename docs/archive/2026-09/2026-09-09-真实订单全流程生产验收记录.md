# 真实订单全流程生产验收记录

> 状态：执行中；邮件来源误判已修复并发布，等待新邮件事件完成真实业务复验
>
> 日期：2026-09-09（北京时间）
>
> 生产代码基线：`main@615aa8d` + tree `0c4c67045dfd2768b0453d323372e652407c5547`，发布 `20260908T192217Z-0c4c67045dfd`
>
> 本地记录基线：`main@615aa8d` 与本轮未提交文档变更
>
> 目标邮箱：`188****4636@163.com`（仅记录脱敏地址）
>
> 验证范围：正常线上真实订单和真实邮件的接收、持久化、严格解析、订单事务、首次刷新任务、官网刷新、付款任务和前端展示；已执行邮件根因修复、完整回归、生产备份恢复验证、版本发布和 Worker 运行验证，尚需新邮件事件验收真实建单

## 一、目标、授权与安全边界

本轮属于正常生产业务，不使用模拟或可丢弃测试数据。使用用户新建的真实订单验证生产全链路，重点发现真实数据、时序、配置和人工操作下的问题。每个检查项分别记录“用户操作事实、生产系统事实、外部官网事实和人工页面体验”，健康检查或 HTTP 200 不代替业务验收。

当前授权包括：

- 用户自行在 Apple 官网创建真实订单，并向目标邮箱发送测试邮件。
- 在本仓库维护本次脱敏验收记录。
- 根据用户后续提供的时间点和证据检查全流程问题。
- 启用生产代理、自动刷新、爬虫 Worker 和付款调度，允许这些正常业务模块产生预期的生产数据写入。
- 为上述生产变更创建数据库与配置备份、执行隔离恢复验证、重建 API 和启停精确的 crawler-worker。
- 按推荐方案修复动态 From 域名白名单和误判恢复逻辑，构建并发布新生产版本，更新邮件来源配置并重建本系统三个应用容器。

当前未授权：

- Codex 代替用户创建、付款、取消、退款、取货或发送邮件。
- 执行新 Migration 或操作代理供应商账号后台；本次修复无表结构变更。
- Codex 代替用户开启接单或自行决定任务容量；用户已在生产页面开启自动分配并配置人员容量。
- 回滚、提交或推送本轮记录。

测试邮件可能包含 Apple ID、密码、订单链接、身份证号、联系方式和地址。原始邮件、完整订单号、完整链接、密码、验证码及个人信息不得写入本文件、聊天结论或 Git；只记录时间、状态、计数、脱敏尾号、错误码、关联 ID 和必要散列。

## 二、执行前生产基线

以下执行前快照已于 2026-09-09 02:03（GMT+8）从生产只读复核。

| 检查项         | 已知基线                                             | 对本次测试的影响                                      |
| -------------- | ---------------------------------------------------- | ----------------------------------------------------- |
| 生产版本       | `61499d0`，21 项 Migration                           | 本次业务结果应绑定该版本和发布 ID                     |
| API            | readiness 通过，仅监听回环端口                       | 只证明技术存活，不证明登录后业务链路                  |
| 邮件 Worker    | 已连接，心跳无连续失败；`IMAP_MARK_SEEN` 默认 `true` | 具备接收条件，真实订单邮件尚未验收                    |
| 邮件来源限制   | 已配置 1 项允许来源                                  | From 不匹配可能进入 `ignored`，发送前需关注实际发件人 |
| 邮件数据       | 16 条日志、100 个订单、0 个付款任务                  | 测试后用脱敏计数差和关联事实核对                      |
| 代理与自动刷新 | 执行前均关闭                                         | 用户随后明确授权全部启用                              |
| 爬虫 Worker    | 执行前未启动                                         | 启动后立即消费存量到期调度                            |
| 付款调度       | 执行前关闭、版本 0、无范围                           | 02:11:36 起启用手动模式并建立本场范围                 |

### 环境限制说明

邮件处理成功的事务会同时提交订单、邮件终态和首次刷新任务。生产 `PROXY_ENABLED`、`AUTO_ORDER_REFRESH_ENABLED` 已改为 `true`，付款调度已启用；但 crawler-worker 首次启动后，`yiyou_http` 对真实订单页连续 24 个任务返回 HTTP 403，剩余 37 条排队、8 条在停止时处于租约运行态。为避免继续无效重试，已于 02:13:50 临时停止精确的 crawler-worker。

`fanproxy_tunnel` 使用代码当前格式、历史观测的全国混播格式以及 Chrome 128 请求头访问既有真实订单页均为 HTTP 403；`kdl_private` 能取得 1 个代理，但生产主机访问返回 HTTP 407。按用户要求，02:42 已使用 `fanproxy_tunnel` 恢复 crawler-worker；活动 Provider、代理切换和 Worker 心跳正常，但存量真实订单页仍出现 HTTP 403 和少量 TLS 断连。本次新订单必须单独观察，不能用首页健康检查或存量失败推断其结果。

## 三、全流程检查表

| 阶段              | 检查重点                                                     | 通过标准                                           | 状态                    |
| ----------------- | ------------------------------------------------------------ | -------------------------------------------------- | ----------------------- |
| T0 执行前快照     | 版本、Worker 心跳、Migration、订单/邮件/任务计数、待处理队列 | 只读快照完整，未泄露敏感值                         | 通过                    |
| T1 用户创建订单   | 创建时间、订单类型、商品数量、付款方式、测试目的             | 用户确认创建成功；只保存脱敏业务特征               | 用户确认已创建          |
| T2 邮件投递       | 发送时间、主题命中、实际 From、目标邮箱、IMAP UID            | Worker 在合理时间内取得邮件；不记录正文和完整身份  | 已收到，From 白名单失败 |
| T3 邮件持久化     | `received → parsing/processing → succeeded` 或明确失败态     | 状态、尝试、错误码和 IMAP 确认可追踪               | `ignored/non_order`     |
| T4 解析与订单事务 | 严格字段、商品全有或全无、订单号/URL 一致、主数据关联        | 新订单只创建一次；邮件终态与首次刷新任务同事务提交 | 未进入；订单仍为 100    |
| T5 幂等与重复     | 回溯、IMAP 重扫、重复 Message-ID/MIME/订单                   | 不重复建单；重复邮件独立留痕并为 `superseded`      | 待执行                  |
| T6 官网刷新队列   | 首次任务、优先级、租约、重试、失败分类                       | 失败可控、不会无限重试或冒充成功                   | Worker 已恢复，观察中   |
| T7 官网真实解析   | Apple 身份校验、原始状态、金额、币种、截止时间、商品         | 真实字段按保留/冲突规则写入                        | 阻塞：订单页 403/407    |
| T8 付款任务       | 本场范围、生成条件、截止时间、官网已付排除、单负责人         | 仅启用时刻后的合格订单生成一条任务                 | 已启用，等待新订单      |
| T9 前端与权限     | 订单、邮件处理、刷新反馈、付款任务、敏感字段、不同角色       | 页面与 API 一致；无权数据不可见；错误状态可操作    | 待执行                  |
| T10 后续生命周期  | 等待付款、已收款、处理中、可取货、已取货及手动刷新           | 各维度独立、真实状态可复核、已付款后不自动刷新     | 待执行                  |

## 四、时间线与结果

| 时间（GMT+8）          | 阶段  | 操作或系统事件                          | 预期                      | 实际                                                                                                                                           | 证据                                                              | 结论                       |
| ---------------------- | ----- | --------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | -------------------------- |
| 2026-09-09 02:03       | T0    | 生产只读快照                            | 确认执行前事实            | 100 个订单、16 条邮件日志、0 个刷新任务、0 个付款任务；API/邮件 Worker 运行                                                                    | 生产只读查询                                                      | 通过                       |
| 2026-09-09 02:05       | 备份  | 备份配置与生产数据库                    | 可恢复、可校验            | 备份 1,764,432 字节并生成 SHA-256                                                                                                              | `/var/backups/apple-order-mgr/20260908T180552Z-enable-live-flow/` | 完成                       |
| 2026-09-09 02:07       | 恢复  | 隔离 PostgreSQL 15 恢复                 | 核心计数一致              | 100 个订单、16 条邮件日志、21 项 Migration                                                                                                     | 临时容器恢复结果                                                  | 通过；临时容器与网络已清理 |
| 2026-09-09 02:11       | T8    | 启用付款调度                            | 从当前时刻纳入新订单      | `enabled=true`、`mode=manual`、版本 1                                                                                                          | 调度设置与审计事件                                                | 通过，尚无新任务           |
| 2026-09-09 02:11       | T6/T7 | 启用代理、自动刷新并启动 crawler-worker | 正常消费官网刷新任务      | 生成 69 个刷新任务；24 个失败、37 个待处理、8 个停止时运行                                                                                     | Worker 日志与任务聚合                                             | 失败，已止损               |
| 2026-09-09 02:13       | 止损  | 停止精确的 crawler-worker               | 阻止持续 403 重试         | API/邮件 Worker 保持运行，爬虫停止                                                                                                             | Compose 状态                                                      | 完成                       |
| 2026-09-09 02:14-02:23 | T7    | 三条 Provider 与请求格式门禁            | 至少一条真实订单页成功    | `yiyou_http` 403；`fanproxy_tunnel` 403；`kdl_private` 407                                                                                     | 脱敏单次请求                                                      | 未通过                     |
| 2026-09-09 02:42       | T6/T8 | 恢复 crawler-worker 并核对自动分配      | 等待新订单进入完整链路    | `fanproxy_tunnel` 活动、心跳正常；调度为 `auto`，2 名人员均开启自动分配且容量各 10                                                             | Worker 状态、调度设置与人员配置                                   | 已运行，等待新订单         |
| 2026-09-09 03:00:19-40 | T2/T3 | email-worker 收到 2 次新邮件事件        | 识别订单邮件并提交事务    | 新增记录 20、21 均立即转为 `ignored/non_order`，已读确认成功；邮件日志 16→19，订单仍为 100                                                     | 脱敏 Worker 日志、邮件状态与计数                                  | 失败；来源过滤未通过       |
| 2026-09-09 03:07-03:08 | T2/T4 | 离线核对用户提供的两封原始 EML          | 区分主题、From 与正文解析 | 两封主题均命中全部 3 个关键词，当前版本均成功解析 1 个商品及必填订单字段；两封均来自 `lanu.cn`，但标准化 From 彼此不同且均不匹配生产唯一白名单 | EML 离线 MIME/解析结果与 From SHA-256 比对                        | 根因确认：精确 From 白名单 |
| 2026-09-09 03:17-03:22 | 修复  | 实现域名白名单、稳定原因码和可恢复拒绝态 | 仅接受完整允许域名        | `lanu.cn` 精确命中，子域名和相似后缀均返回 `SENDER_NOT_ALLOWED`；主题未命中返回 `SUBJECT_NOT_ALLOWED`                                      | 25 项定向测试、13 项隔离 PostgreSQL 集成测试、两封 EML 回归 | 通过                       |
| 2026-09-09 03:22       | 质量  | 全量回归与发布门禁              | 无高危阻断项              | 后端 39 套件/288 测试通过，3 个环境套件/42 项跳过；后端 Lint、文档检查、前端 Lint 和构建通过；前端生产依赖留有 2 个中危通告 | 洁净一次性容器、精确隔离库、`npm audit --omit=dev`                 | 通过；中危单独追踪       |
| 2026-09-09 03:23-03:25 | 备份  | 最终生产备份与恢复验证          | 可恢复且计数一致          | 配置、容器 inspect、旧 manifest 和 1,818,087 字节数据库备份均通过 SHA-256；恢复库为 21/100/19                         | `/var/backups/apple-order-mgr/20260908T192317Z-20260908T192217Z-0c4c67045dfd/` | 通过；临时库已删除       |
| 2026-09-09 03:26-03:31 | 发布  | 更新白名单、重建应用并切换版本    | 服务健康且共享服务不重启    | 精确地址白名单清空，域名白名单为 `lanu.cn`；API healthy，邮件/爬虫 Worker 运行，首页/readiness 200，未鉴权订单 API 401，PostgreSQL/Nginx active | 生产容器、状态表、HTTPS 和配置读取                              | 技术发布通过               |
| 2026-09-09 03:31       | T6/T7 | 爬虫恢复消费存量刷新任务          | 代理请求可用且错误可稳定留痕  | Worker 未暂停、代理成功切换；订单页持续出现 HTTP 403，少量代理返回 631 又被模型 100-599 约束拒绝记录                                      | 爬虫 Worker 脱敏聚合日志                                          | 运行中；新增问题 LIVE-07     |

## 五、问题清单

问题等级：P0 表示可能造成跨单覆盖、敏感数据泄露、重复付款或系统不可用；P1 表示核心流程失败；P2 表示可绕行的功能/体验问题；P3 表示低影响展示或记录问题。

| 编号    | 等级 | 发现时间         | 阶段 | 现象                                                                              | 预期                                 | 影响                                                         | 状态             | 证据与后续                                                                         |
| ------- | ---- | ---------------- | ---- | --------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------------------- |
| LIVE-01 | P1   | 2026-09-09 02:12 | T7   | 代理首页健康检查为 200，但三条线路访问既有真实订单页均失败                        | 上线门禁应验证实际订单页             | 官网数据无法同步                                             | 打开             | 新订单产生后先执行单次真实链接门禁                                                 |
| LIVE-02 | P1   | 2026-09-09 02:12 | T6   | 连续 HTTP 403 未触发自动暂停，Worker 继续生成和消费任务                           | 连续风控/拒绝应有界暂停              | 产生大量无效请求和失败任务                                   | 打开，已恢复运行 | 24 个失败后曾人工停止；按用户要求恢复，仍需补 403 分类和暂停策略                   |
| LIVE-03 | P1   | 2026-09-09 02:12 | 日志 | 生产爬虫日志输出完整订单号和代理 IP:端口                                          | 日志应使用脱敏摘要                   | 敏感标识扩散到容器日志                                       | 打开             | 本轮对外记录已脱敏；需修改日志字段并重新发布                                       |
| LIVE-04 | P1   | 2026-09-09 02:21 | T7   | `kdl_private` 提取成功但生产请求返回 HTTP 407                                     | 代理应在生产主机完成认证             | 该 Provider 不可用于生产                                     | 打开             | 核对供应商认证或生产出口白名单                                                     |
| LIVE-05 | P1   | 2026-09-09 03:00 | T2   | 两封合格业务邮件因 From 地址不匹配唯一精确白名单而被判定为 `ignored/non_order`    | 合格业务邮件进入严格解析             | 订单、刷新及付款任务均未创建                                 | 已修复发布，待业务复验 | 生产已使用 `lanu.cn` 完整域名白名单；原邮件为终态，需新邮件事件复验                 |
| LIVE-06 | P1   | 2026-09-09 03:00 | T2   | `ignored` 后立即清除主题、From、Message-ID、MIME 指纹和原文，且不保存未命中的规则 | 在不保留敏感正文的前提下记录过滤原因 | 无法从系统证据区分主题不匹配或白名单不匹配，也无法人工重处理 | 已修复发布       | 主题未命中记录 `SUBJECT_NOT_ALLOWED` 后最小化；From 未授权记录 `SENDER_NOT_ALLOWED` 并进入人工队列 |
| LIVE-07 | P2   | 2026-09-09 03:31 | T6   | 代理返回非标准 HTTP 631 时，爬虫日志模型的 100-599 校验拒绝保存                              | 代理非标准状态仍能脱敏分类留痕       | 订单刷新任务可记录失败，但该次详细爬虫日志丢失                 | 打开             | 需在入库前将非标准代理状态归一化为可存储错误码，并补 631 回归测试                         |

环境限制不直接登记为产品缺陷；只有当系统实际行为与已确认配置、契约或页面提示不一致时，才转为问题。

## 六、每次更新必须补充的证据

- 用户侧：关键操作北京时间、是否成功、页面提示；订单只给脱敏尾号或由系统内部 ID 关联。
- 邮件侧：接收时间、状态流转、错误码、尝试次数、IMAP 确认状态；不复制原始 MIME。
- 数据侧：前后计数差、关联关系、状态与时间字段；查询仅输出脱敏值。
- Worker 侧：心跳、任务领取、重试、终态和相关日志时间窗；日志不得包含订单链接或密码。
- 官网侧：请求结果、代理/传输/解析/身份错误分类、原始枚举和脱敏指纹。
- 页面侧：角色、入口、筛选、状态、时间、操作反馈和敏感字段权限。
- 结论侧：分别写已完成、已验证、失败、阻塞、未验证、人工验收和生产业务验收。

## 七、当前结论与下一触发点

生产代理和自动刷新配置已经启用，付款调度从 2026-09-09 02:11:36（GMT+8）建立新订单范围；用户已将调度切换为 `auto`，2 名人员均开启自动分配、容量各 10。生产已切换到 `20260908T192217Z-0c4c67045dfd`；API healthy，邮件 Worker 已连接且连续失败 0，crawler-worker 未暂停、活动 Provider 为 `fanproxy_tunnel`、心跳正常。存量订单页持续 HTTP 403，并新发现非标准 HTTP 631 无法写入详细爬虫日志。

原两封邮件已在 03:00:19-03:00:40 时间窗被旧版 Worker 判定为 `ignored/non_order`，订单计数仍为 100。本轮修复已把生产完整地址白名单从误配的收件邮箱地址清空，改为完整域名 `lanu.cn`；生产分类器实测接受 `@lanu.cn`，拒绝 `@sub.lanu.cn` 和 `@evil-lanu.cn`。原邮件已进入不可恢复的旧 `ignored` 终态，不能自动重处理；下一触发点是用户重新发送一封合格的真实订单邮件，然后依次核对邮件终态、订单唯一性、首次刷新任务、官网同步、付款任务和自动分配。

## 八、历史订单刷新与新订单身份异常只读核对

2026-09-09 约 04:00（北京时间）根据用户截图核对生产数据库和运行容器。以下为本次读数，替代上节“等待新订单”的后续状态；未写生产数据、恢复任务、请求订单官网、停止 Worker 或发布代码。

### 历史订单持续刷新

- 历史 100 单全部缺失 `payment_status`，全部使用 `test…@example.com` Apple ID；因此早前将这一批笼统称为“存量真实订单”不准确，至少具有明确的测试数据特征。未进一步追溯每单的原始创建操作。
- pending 12、processing 20、ready_for_pickup 14、shipped 15，共 61 单仍启用自动刷新且未标记终态。`refreshPolicy.isAutoRefreshEligible` 把所有非 paid/refunded 的支付值（包括 NULL）视为可自动核实，未区分历史空值与新订单待核实，也未用 processing/ready_for_pickup 阶段补充停止判断。
- 截图圈出的两单（内部 ID 75、42）均有持续的 auto 失败记录，主要错误为 HTTP_403，读取时分别累计连续失败 43 和 42 次。另一次全量快照为历史 auto 失败任务 2,613、pending 37、running 4；Worker 持续运行，计数会变化。不是手动全部刷新，也不是只有 UI 残留。
- 待确认方案：历史支付空值默认不进入周期轮询；新单保留独立、有界的首次核实机会；已确认付款阶段停止；入队与执行资格一致，失去资格的自动任务标记 skipped，不伪造官网成功时间。历史数据停用及队列收敛属于生产写入，尚未执行。HTTP 403 有界暂停及 631 日志问题继续沿用 LIVE-02/LIVE-07 跟踪。

### 新订单身份校验

- 内部 ID 108（尾号 0752）创建于 03:49:10.947，已有 pending 付款任务。自动任务 03:49:35.581 开始、03:49:35.832 失败；手动任务 03:54:40.853 开始、03:54:41.141 失败，均为 IDENTITY，摘要为“订单 URL 来源或身份与本地订单不一致”。
- 生产运行容器只读比较证实：HTTPS、www.apple.com.cn、空端口、xc/cn/vieworder 路径、5 段路径和订单号均符合要求；链接末段与本地 Apple ID 均为邮箱，但解码／不区分大小写／去除首尾空白后仍不同。该单没有关联 Apple 账号，因此不是关联账号优先级引起。未输出两个邮箱的明文。
- `validateOrderUrl` 在网络请求前比较链接邮箱与 Apple ID，故此单失败不属于代理 HTTP 403，也没有证据证明官网返回了错误订单。异常处理已设置 `auto_refresh_enabled=false`、`auto_refresh_stop_reason=order_identity`、下一自动时间为空。截图中的“待处理（历史）”是 pending 兼容标签，不能据此判断新单创建于历史时期。
- 当前邮件解析器分别提取正文 Apple ID 和订单链接，仅检查链接订单号，不在该阶段核对两种邮箱语义。Apple 的[订单帮助](https://www.apple.com.cn/shop/help/viewing_changing_orders)明确支持订单号加电子邮件的访客查看方式，不能仅因邮箱不同就断定用户提供了错误账号；还需核实本单来源字段及真实官网身份后确定修复。不得通过自动改写 Apple ID 或直接删除安全校验处理。

### 付款二维码需求与验证边界

- 用户提出付款任务每行新增“查看付款码”，点击弹窗展示官网二维码。现有 `getOwnPaymentLink` 只返回 `order.orderUrl`，官网解析仅提取 Pay Now 金额等业务字段，没有付款二维码获取接口。
- 拟实现：保留原复制链接操作；新增按需获取接口，校验本人任务归属和当前订单身份／未付款资格；弹窗显示订单号、官网金额、付款方式、可用截止、二维码、加载／错误／重试状态；已付款、过期、取消或身份异常时不展示可付款码。官网确认收款后停止展示，人工任务四态仍独立。
- Apple 的[付款帮助](https://www.apple.com.cn/cn/shop/help/payments)确认桌面端微信支付使用二维码，但不证明现有爬虫可直接提取，也未提供本系统可调用的公开付款码契约。必须用有效待付款订单验证官网实际获取流程、会话依赖和有效期；不把普通订单链接生成的二维码冒充官方付款码，不在日志或长期存储保留付款令牌。本轮未访问付款入口、生成付款会话或执行付款。

本轮结论：两个截图问题的数据库与代码原因已定位；生产修复与真实付款码验证尚未完成。仅记录事实及待确认方案，未修改现行身份或自动刷新规范。


## 九、2026-09-09 订单刷新修复发布后的后续结果

前节待确认项随后经用户授权完成实施，付款二维码功能取消。最终生产已发布 8189a35／20260908T205416Z-2e3447d2e083，27 条旧自动任务已跳过，活动自动任务与后续计划为 0。目标订单尾号 0752 手动刷新成功，保存 payment_expired，原身份异常解除，普通来源差异保留。发布复验另发现并修复 manual_single 日志来源校验导致事务回滚；LIVE-07 的非标准状态码日志兼容已本地测试并发布，尚未重新观察到真实 631。备份、窗口、计数和当前验证边界见[订单刷新修复生产发布记录](2026-09-09-订单刷新修复生产发布记录.md)。本节是后续时点结果，前节读数保留为当时证据。
