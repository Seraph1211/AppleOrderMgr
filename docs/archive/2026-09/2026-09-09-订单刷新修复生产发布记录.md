# 订单刷新修复生产发布记录

> 状态：发布准备中
>
> 日期：2026-09-09（北京时间）
>
> 授权：用户要求先提交本地改动，再部署生产；本轮不要求推送远程仓库。

## 发布范围与门禁

提交当前工作树的邮件来源过滤修复、联系邮箱校验修复、历史订单自动刷新资格修复、测试及配套文档。邮件来源过滤已在上一生产 tree 中发布，本轮将其纳入正式提交。已取消付款二维码功能，无新增二维码接口和页面。无新增 Migration，无凭据或生产配置变更。

本地全量测试在与当前锁文件 SHA-256 一致的隔离镜像中通过：39 套件／315 项，3 套件／42 项按环境门禁跳过。前后端 Lint、文档检查及 Vite 6.4.3 构建通过，保留大包警告。旧开发容器的 6 项依赖版本测试失败已用锁文件一致的环境复核，不改变测试预期。

## 生产切换前基线

只读核对：current 为 20260908T192217Z-0c4c67045dfd，三个 Apple 应用容器运行、API healthy。宿主机 PostgreSQL 15 与共享 Nginx 正常，磁盘余量约 14 GB。发布保持现有邮件来源配置、代理 Provider、自动刷新和付款分配设置。

## 执行与验收

待完成：本地提交、精确提交的 linux/amd64 制品、生产数据库及配置备份、隔离恢复校验、三个 Apple 应用容器更新、current 原子切换及技术验收。生产业务数据、敏感配置和完整容器 inspect 只存服务器备份目录，不进入仓库。停止窗口内最终备份后才更新应用；无结构差异时不重复敏感数据转换。

真实官网／邮件／付款端到端验收单独记录；健康检查不代表业务验收。新版本不会直接删除历史身份异常，已暂停订单须受控手动刷新成功后恢复。
