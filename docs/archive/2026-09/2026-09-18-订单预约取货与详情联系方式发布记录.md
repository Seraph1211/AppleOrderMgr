# 订单预约取货与详情联系方式发布记录

> 日期：2026-09-18
>
> 代码提交：`09297b52dd666e2bc8870e7e40b325053985ae03`
>
> 生产制品：`20260917T172904Z-507ac19ace67`
>
> 验证边界：完成自动化回归、正式制品校验、生产备份和技术发布检查；未触发真实官网刷新或执行人工业务验收

## 修复内容

官网履约提示中的“今天／明天”不再按页面打开时间解释，而以该次官网成功观测时间 `official_status_observed_at` 的北京时间日期换算，缺失时才回退 `last_crawled_at`。订单列表、详情及日期筛选使用同一规则，原始官网履约文本继续保留供核对。

订单详情新增订单入库快照中的下单手机号和下单邮箱；预约取货日期、时间段改为由官网履约提示派生，不再错误读取通常为空的业务实际取货字段。手机号继续遵循现有脱敏权限。无数据库字段、历史数据转换或业务规则迁移。

## 开发验证

- 定向 Jest 2 套／33 项、全量后端 62 套／624 项通过；14 套／182 项外部环境测试按门禁跳过。
- 开发 PostgreSQL 只读验证绝对日期及“今天／明天”日期筛选 SQL；未发起真实 Apple 请求。
- 前后端 Lint 均 0 error，Vite 构建、文档检查通过。
- 合成浏览器覆盖订单列表、弹窗详情、独立详情页及 390px 窄屏，验证绝对取货时间、下单手机号／邮箱、预约日期／时段和官网原文；全部业务 API 使用合成响应。

## 生产发布

正式发布包全部 SHA-256 校验通过，运行和迁移镜像均为 linux/amd64。服务器发布目录为 `/var/www/apple-order-mgr/releases/20260917T172904Z-507ac19ace67`，旧版本 `/var/www/apple-order-mgr/releases/20260917T162159Z-288e54e1dd7c` 保留。

受保护备份目录 `/var/backups/apple-order-mgr/20260918T0133-pickup-detail` 保存发布前和停写后的 PostgreSQL 完整备份及 SHA-256、旧 `current` 指针、环境文件副本与摘要、容器基线、核心计数、readiness 和发布后日志。目录权限为 700，敏感文件为 600。因本次无 Migration 或数据转换，不需要对生产结构执行恢复库迁移演练；迁移命令确认数据库已是最新状态，Migration 数保持 38。

北京时间 01:32:19—01:32:27 停止三个 Apple 应用、生成最终备份、启动新 API、等待 readiness、原子切换 `current` 并恢复两个 Worker，约 8 秒，未触发自动回滚。发布前后核心计数一致：38 条 Migration、37 条订单、14 个用户、1,926 个 Apple ID、1,862 个取机人、37 个付款任务。`app.env` SHA-256 未变；未执行共享 PostgreSQL、Nginx 或其他项目容器的重启或配置变更。

生产内网与公网 readiness 均返回 healthy/db=ok。API、邮件 Worker、爬虫 Worker 均使用新镜像且 restart=0，API health 为 healthy；启动后目标错误日志检查无命中。公网首页 SHA-256 与制品一致，正式 JS 和 CSS 资源可访问。

## 回滚与验收边界

如需应用回滚，使用旧发布目录的 `release.env` 和 Compose 文件重建三个 Apple 应用，并将 `current` 原子切回 `/var/www/apple-order-mgr/releases/20260917T162159Z-288e54e1dd7c`。本次无结构变更，不需要数据库 down；完整备份仅作恢复兜底，禁止未经业务窗口评估直接覆盖发布后数据。

本次没有手动刷新真实订单、付款、编辑或改写业务记录，也没有使用真实“今天／明天”订单做人工页面验收。健康检查和合成浏览器证明技术链路可用，不等同于真实官网返回及业务人员展示确认。代码和发布记录均仅本地提交，未 git push。
