# 网帆二十并发扩容记录

> 状态：配置已在生产生效；百单稳定性验收待真实任务积累
>
> 操作时间：2026-09-09 14:44:17–14:44:19（北京时间）
>
> 生产代码：`512c2d3`，发布 `20260909T054341Z-04bce86b553f`

## 原因与操作

用户已购买 20 并发、1 分钟粘性套餐，并授权通过浏览器配置和优化爬虫。网帆控制台仍同时列出旧的 5 并发套餐，新套餐尾号为 `1504`。浏览器确认新套餐规格，并读取该套餐接入参数；新旧套餐的基础账号和密码均不同，仅调高旧配置的并发数无法切换套餐。

生产原先显式配置 5 会话槽，并发使用代码默认 8。现有 Provider 支持有界会话池，Worker 直接读取并发配置，无需修改通用代码默认值。生产 `shared/app.env` 已换用新套餐账密，并显式设置：

```ini
FANPROXY_TUNNEL_SESSION_MODE=sticky_pool
FANPROXY_TUNNEL_SESSION_POOL_SIZE=20
CRAWLER_WORKER_CONCURRENCY=20
CRAWLER_REQUESTS_PER_SECOND=5
CRAWLER_MAX_RETRY=3
```

1 分钟粘性由供应商套餐控制；应用使用 `acc/cty/sid` 参数，不拼接其他供应商的时长参数。环境配置以原子替换写入，权限为 `600`；只重新创建 `crawler-worker`。API、邮件 Worker、共享 PostgreSQL、Nginx 未重启，生产镜像和数据库结构不变。

## 验证证据

- 新账密在生产网络上通过已有全局限流与候选代理健康检查，Apple 返回 HTTP 200。
- 实际 Worker 启动日志记录 `sessionPoolSize=20`、`sessionMode=sticky_pool`、`concurrency=20`，其配置读取结果为 `20/20/5/3`。
- 14:45:05 数据库显示 `active=fanproxy_tunnel`、`switchStatus=succeeded`、`workerProxyReady=true`、`isPaused=false`，心跳为 14:45:03。
- API 保持 healthy，API 与邮件 Worker 容器持续运行。手动刷新与自动入队仍由独立爬虫 Worker 执行。
- 新增 20 槽回归：首轮 20 个用户名互不重复，随后 100 次取用仍仅复用这 20 个槽。代理及 Worker 定向测试共 18 项通过；后端 Lint、文档检查和差异检查通过。
- 没有前端或运行时代码变化，本次不重建应用制品，也不运行数据库迁移。

## 稳定性边界与后续观测

扩容后截至 14:45:05 尚无新完成的真实刷新任务。HTTP 200 健康检查和配置生效不等于真实订单成功率达到 95%，也没有完成 20 个真实订单并发压测。

变更前最近 100 个已完成有效任务为 72 成功、28 失败，分布于当天多个阶段，不能当作同一时段的 HTTP 重试尝试基线。保持 5 RPS，不用历史已付款或终态订单凑样本。

已建立每 15 分钟一次的只读百单验收跟进，起点为 `2026-09-09T06:44:17Z`。正常且样本不足时不通知；达到至少 100 个有效任务时报告成功率、403/541 证据及提速建议，再暂停跟进。该跟进不自动改配置；基线或重试日志不足时明确保留 5 RPS。分阶段提速门槛见[爬虫说明](../../design/订单爬虫.md#网帆-20-并发套餐配置)。

## 备份与回退

配置备份、SHA-256 和脱敏变更元数据位于 `/var/backups/apple-order-mgr/fanproxy20-20260909T064417Z/`，目录权限为 `700`，配置备份为 `600`。必要时恢复该目录中的 `app.env` 到 `shared/app.env`，再按[运维手册](../../deployment/快速运维手册.md#网帆套餐扩容)重新创建爬虫 Worker；这会恢复旧套餐及旧并发。回退前需核实旧套餐仍有效。没有执行生产数据库数据修正。

## 后续：网帆统一为默认线路

用户随后明确指定网帆作为默认线路。代码默认常量、环境示例、本地环境和系统页首选标记统一为 `fanproxy_tunnel`，其他 Provider 仍支持手动切换。代码提交 `ae6b92b`，发布 `20260909T072938Z-527e0b4aef41`；三项应用服务于 15:30:58–15:31:01 更新完成，共享服务未重启。

全量单测 404 项通过，53 项外部环境测试按门禁跳过；前后端 Lint 无错误、前端构建和文档检查通过。正式制品为 `linux/amd64`，全部 SHA-256 校验通过。发布备份位于 `/var/backups/apple-order-mgr/default-fanproxy-20260909T073055Z/`，隔离 PostgreSQL 恢复验证为 105 条订单、23 条迁移；未执行 Migration，生产 `app.env` 与发布前一致。

浏览器确认当前生效和目标均为“网帆隧道代理（首选）”，爬虫已就绪、邮件监听正常。新 Worker 确认代码默认与环境配置均为网帆，会话槽 20、并发 20、5 RPS、最多 3 次尝试；代理切换状态 succeeded、未暂停，API healthy。百单观测继续使用原扩容时间起点，不因本次默认标记发布重复计数。此处是默认配置与页面验收，不新增真实订单成功率结论。
