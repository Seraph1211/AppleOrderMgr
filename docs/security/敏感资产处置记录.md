# 敏感资产处置记录

> 状态：执行中；生产存量字段加密已完成，Git 历史与外部凭据轮换仍未闭环。
> 文档引用核对：2026-09-09。

> 处置日期：2026-09-05
>
> 基线提交：`0fd7f80`
>
> 适用范围：本地工作树与可读 Git 历史

## 一、处置结论

当前工作树中的数据库备份、真实环境配置和导入 Excel 文件已移除，并增加忽略规则与 CI Secret Scan 门禁。历史扫描确认敏感路径和对象仍存在于 Git 历史；在未获得远程历史改写和凭据轮换的明确授权前，不得将本记录视为泄露处置已闭环。

## 二、当前工作树处置

| 类型         | 路径/范围                         | 状态           | 说明                                |
| ------------ | --------------------------------- | -------------- | ----------------------------------- |
| 数据库备份   | `database_backup.sql`             | 已从当前树移除 | Git 历史仍可恢复，需单独改写历史    |
| 导入临时文件 | `uploads/import/*.xlsx` 共 7 个   | 已从当前树移除 | 保留 `.gitkeep`，新文件默认不追踪   |
| 真实环境配置 | `backend/.env.production`         | 已从当前树移除 | `.env*` 仅允许示例文件入库          |
| 硬编码口令   | Compose、默认管理员、JWT fallback | 已移除         | 改为必填环境变量和启动校验          |
| 证书验证     | PostgreSQL、IMAP、价格爬取        | 已收紧         | 默认验证 TLS 证书，仅可显式配置例外 |

`.gitignore` 已覆盖数据库备份、SQL 压缩包、导入文件、私钥格式和后端环境文件。CI 增加 Gitleaks 扫描，用于阻断后续高置信度凭据进入仓库。

## 三、Git 历史扫描

- 已对全部本地 refs 扫描备份、Excel、环境配置和凭据相关路径，未在本文档中记录任何秘密原文。
- 扫描发现至少 2 个历史提交涉及敏感路径，并确认 `database_backup.sql`、部分导入 Excel 和真实环境配置曾进入历史。
- 截至初次处置未改写本地或远程分支/标签；2026-09-09 仅重建未推送的 8 个本地提交，见第十二节。远程历史未改写，未强制推送，未要求协作者重新克隆，因此 `SEC-02` 的“新克隆不可检索”退出条件尚未满足。

## 四、凭据轮换清单

以下操作会修改外部系统或生产环境，本次未执行：

- PostgreSQL 账号和密码；
- JWT 签名密钥并使旧 Token 失效；
- IMAP 邮箱应用密码；
- 代理服务 API 凭据；
- Telegram Bot Token 和 Chat ID；
- 曾进入备份、日志或导入文件的 Apple ID 凭据。

轮换后应保留“凭据类型、轮换时间、执行人、旧凭据失效验证”，不得记录新旧凭据原文。

## 五、历史清理建议

获得单独授权后，应在停止合并的维护窗口中执行：

1. 冻结远程分支和标签范围，完成加密备份。
2. 使用 `git filter-repo` 按已确认路径清理所有 refs，再次扫描 Git 对象。
3. 完成凭据轮换，验证旧凭据不可用。
4. 经授权强制更新远程分支和标签，通知协作者废弃旧克隆。
5. 在全新克隆中重跑 Secret Scan，并将结果作为处置验收证据。

## 六、验收状态

| 项目                   | 状态                      |
| ---------------------- | ------------------------- |
| 当前树敏感文件移除     | 已完成，待提交            |
| 新增文件忽略与 CI 门禁 | 已实现，本地未执行远程 CI |
| Git 历史全量清理       | 未执行，需明确授权        |
| 外部/生产凭据轮换      | 未执行，需明确授权        |
| 新克隆仓库验收         | 未执行，依赖历史清理      |

## 七、2026-09-06 生产字段加密

- 发布前生产数据库已完整备份，并在隔离 PostgreSQL 容器完成恢复、结构迁移和敏感数据转换演练。
- 生产已执行字段类型、角色约束、敏感字段长度与盲索引 Migration，并转换 Apple ID、取机人、订单和邮件日志的存量敏感字段。
- 转换后核心表行数与备份一致；受检非空字段无明文残留，取机人身份证盲索引无缺失；应用模型解密读取通过。
- 新字段加密密钥仅保存在权限为 `600` 的生产环境文件，并随最终停机备份配套保存，未写入仓库或发布制品。
- 本次没有轮换 PostgreSQL、JWT、IMAP、代理、Telegram 或历史 Apple ID 凭据，Git 历史清理和外部凭据轮换状态保持未完成。

## 八、2026-09-07 本地验收展示边界

- 数据库继续保存加密的 Apple ID 密码、取机人身份证号以及完整手机号；未通过修改或伪造后台数据取消脱敏。
- 页面所需明文仅在 `NODE_ENV=development`、`ALLOW_LOCAL_SENSITIVE_DISPLAY=true` 且当前用户为 admin 三项同时满足时由 API 返回。
- 本地例外只覆盖 Apple ID 密码、取机人身份证号、手机号与详细地址、订单手机号；订单身份证号、Apple 订单链接和其他敏感字段仍按原契约隐藏。
- production 环境即使误配开关也保持脱敏；生产 Compose 未增加该配置。

## 九、2026-09-07 邮件人工处理授权边界

- 邮件处理模块采用独立的 admin 专属规则，不继承普通订单、Apple ID 或取机人页面的默认脱敏响应；`operator`、`readOnly` 和未认证用户由后端拒绝整个 `/api/email-processing` 模块。
- admin 可查看完整订单邮件 MIME，并可在人工草稿中填写 Apple 密码、完整身份证号、联系方式、地址和订单内部状态。这是为快速修复失败邮件而确认的受控能力，不代表其他 API 获得相同权限。
- 原始 MIME 使用加密 TEXT，自动解析、人工草稿、最终数据、处理尝试和管理员操作审计使用 AES-256-GCM JSONB 密文包装。运行日志和错误响应不得包含上述明文。
- 查看完整详情、重新解析、保存草稿、确认入库和人工关闭写入操作人 ID、时间、稳定操作名及结果引用；审计中不复制密码、身份证号或 MIME。
- 原始 MIME、解析结果、人工草稿和最终数据保留 180 天后清除，状态、稳定错误码、订单关联、处理尝试和操作审计继续保留。自动识别的非订单邮件立即清除可识别元数据和正文，只留最小状态审计。
- 本节是应用契约；当前没有因此执行生产 Migration、发布、邮箱访问或真实数据核验。历史凭据轮换和 Git 历史处置状态不变。

## 十、2026-09-09 推送前安全检查

- 基线：本地 `main@57859e7`；从远程更新引用后，`origin/main..HEAD` 含 8 个尚未推送的提交。扫描工具为官方 Gitleaks 容器，镜像摘要 `sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f`。
- 使用 `git /repo --log-opts='origin/main..HEAD' --redact --no-banner` 扫描，退出 1、命中 3 处；报告仅保留完全脱敏值，没有回显凭据原文。
- `9d13c20` 中的 `docs/archive/2026-07/开发进度记录.md` 第 1507、1508 行包含代理 `PROXY_SECRET_ID/PROXY_SECRET_KEY`，未见占位标记，未请求外部服务验证其有效性；同段代理 URL 也应一起排查。另一命中为 `docs/archive/2026-09/整理前接口设计.md` 第 91 行带省略号的 Token 示例，不将其计作已确认有效凭据。
- 本轮暂停提交、推送和发布制品构建，没有删除证据、修改历史、强制推送、轮换凭据或改变生产环境。仅修订当前归档不能清除未推送提交中的副本；当前树脱敏、未推送历史处置、远程历史治理及外部凭据轮换需分别确定范围。
- 待授权处理后重新扫描；本节不代表历史泄露已闭环，也不代表上述代理凭据仍有效。

## 十一、2026-09-09 已批准的本地安全修复范围

- A1/A2：脱敏两份归档中的代理配置与 Token 示例，同步重建 `origin/main..HEAD` 的 8 个未推送提交；只改变对应文档内容及必然变化的提交哈希，保留其他文件、提交顺序、作者和提交说明。切换本地分支前核验新旧树差异、工作区文件校验值及敏感信息扫描结果，不改写远程历史、不强制推送。
- A3：更早的远程历史与外部凭据轮换独立调查、单独制定操作方案；本轮不轮换凭据，不将增量扫描通过写成全历史泄露处置完成。
- B1/B2/B4/B6/B8/B9/B10：仅更新受影响依赖及其必要子依赖，在现有主版本约束内优先修复。B3 先核对 `utf7` 的实际调用，优先修复其 `semver` 子依赖而不更换 IMAP 库；B7 保留 Sequelize 6 与 node-cron 3，核对实际 UUID 用法后验证兼容修复，禁止盲目使用 `npm audit fix --force`。
- B5：先确认 SheetJS 官方发布来源及现有导入导出调用，形成明确版本、校验和及回归范围的方案；更换来源或库之前仍需具体方案确认。
- 环境边界：依赖安装与回归在不加载真实 `.env` 的本地隔离容器进行，不重装现有运行容器的依赖、不启动邮件／爬虫、不访问真实邮箱／Apple／代理，不迁移当前开发库或生产库。生产恢复演练、服务切换及爬虫代理选择仍按独立门禁执行。

## 十二、2026-09-09 局部处置结果与后续方案

- A1/A2 已执行：两份归档的代理配置与 Token 示例改为明确占位符，仅重建 `origin/main..HEAD` 的 8 个本地提交；新 HEAD 为 `405ed22e3af2f5b09cc2ada4cfe2ac4219560b80`。逐提交仅两份目标文档树内容变化，其他工作区文件校验一致。新提交区间 Gitleaks 退出 0、零命中，未放宽规则。
- 远程 `origin/main@0fd7f80` 保持不变。对该已发布历史的只读 Gitleaks 扫描退出 1、7 个提交中命中 3 处：`f1ed27f` 的 `docs/design/API设计.md:50` 为 Token 示例命中；`ff69d52` 的历史开发进度文件第 215/216 行为代理字段命中（原路径见归档报告）。未验证任何凭据有效性，未读取历史数据库备份或环境文件内容。
- 历史敏感路径查询仍检出 `fc6ae8c`、`7719734`，涉及备份、环境配置与导入路径。上述检查不覆盖托管平台缓存、其他克隆及未取得的引用。全历史 CI 仍可能因已发布历史失败，不能用本地增量零命中宣称全历史通过。
- 旧本地提交对象仍可从 reflog 恢复，未新建指向它们的备份分支；这是可恢复性保留，不是安全擦除。未删除远程资产、未轮换凭据。详细证据与依赖风险见[本地安全修复报告](../archive/2026-09/2026-09-09-本地安全修复与发布门禁复核.md)。

### A3 独立治理方案（尚未执行外部写操作）

1. 由资产责任人确认仓库分支、标签、协作者克隆、fork、缓存与制品的调查范围；仅记录路径、提交、凭据类型和责任人，不复制敏感内容。对旧凭据是否仍有效，不擅自调用外部服务验证。
2. 分别确认数据库、JWT、IMAP、代理、Telegram、历史 Apple ID 的轮换窗口、影响与验收人。JWT 更换会使现有登录失效；邮箱和代理变更可能中断 Worker；数据库变更需协调连接切换。没有证据表明字段加密密钥泄露时，不把它混入普通口令轮换，避免破坏已有密文可读性。
3. 每类凭据逐项授权后，由安全渠道注入新值，验证新链路，再验证旧值已失效；仅记录非敏感证据。失败时停止相关操作，不将已暴露旧凭据作为默认恢复方案。生产备份、迁移、服务操作继续遵循部署指南。
4. 远程历史改写另行确认维护窗口、加密备份、全部目标 refs、协作者重克隆与托管方缓存处置；经授权执行后，以全新克隆全量扫描及旧凭据失效验证闭环，不增加扫描忽略项来绕过问题。

## 十三、2026-09-09 剩余依赖风险处理

- 用户已确认继续处理剩余风险，B5 的具体官方制品方案及前端依赖升级已实施，不再处于“等待版本来源确认”。SheetJS 0.20.3 固定公开制品、SHA-256 校验脚本和 CI 校验生效；前端审计增加到现有 CI。
- 后端全依赖、生产依赖及前端全依赖审计均为零告警；保持原审计与 Gitleaks 规则，没有增加漏洞／凭据忽略项。测试、构建和失败处理详见[本轮报告](../archive/2026-09/2026-09-09-依赖风险收尾与构建回归.md)。
- 只读 `git ls-remote --heads --tags origin` 再次确认远程仅返回 `refs/heads/main@0fd7f80584aca4f69ebfb087262629bcbab4ecc2`，没有返回标签；这不覆盖隐藏 PR refs、fork、其他克隆及托管平台缓存。仍需确认强制更新该已发布分支的维护窗口和协作者安排，未实际改写远程。
- 外部账号密码不能仅在项目 `.env` 中更换：必须由对应邮箱、代理等服务端撤销旧值，再安全注入新值并验证；数据库／JWT 还涉及连接和登录切换。本轮没有取得这些账号的具体操作范围、维护窗口与旧值失效证据，未轮换凭据。不会把用户的风险治理目标转成未经核对的停服或批量账号操作。

## 十四、2026-09-09 发布授权与延期风险

- 用户明确要求继续本地 commit、普通 push，并基于提交后的本地分支打包部署生产；同时明确暂不强制改写远程分支、暂不轮换外部凭据。
- 已发布历史中的敏感资产与凭据有效性风险仍未关闭。本次发布保留该例外记录，不关闭、放宽或忽略 CI Secret Scan；全历史扫描失败与新增提交扫描结果分别报告。
- 发布仍须通过当前代码质量、依赖审计、增量秘密扫描、精确制品校验、生产备份与隔离恢复迁移演练。保留现有生产代理／自动刷新关闭状态，不自行启用爬虫或更换账号。
