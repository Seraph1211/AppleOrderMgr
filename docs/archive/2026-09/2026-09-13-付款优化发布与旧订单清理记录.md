# 付款优化发布与旧订单清理记录

> 日期：2026-09-13；范围：用户明确授权提交、生产部署及删除截图对应 9 月 12 日订单。

## 发布结果

代码提交 `730396b`，本地 main 构建 linux/amd64 制品 `20260913T102115Z-bb103e5dfaaa`，sourceTree `bb103e5dfaaa`，构建时工作树干净。北京时间 18:23:10–18:23:18 完成生产切换；前端、API、邮件 Worker、爬虫 Worker 同版。未推送远程。覆盖日期筛选、官网状态多选与标签统一、复制数量合并、TAG 专属分配和保留首次刷新／取消周期刷新；包含前序已发布但尚未提交的全局待付款概览。

迁移 `20260913000001-add-initial-refresh-trigger.js` 已执行，SequelizeMeta 32→33。已在完整生产备份恢复库验证迁移与新镜像付款查询，后执行生产迁移。未主动调用真实官网或进行付款。

## 删除范围与保护

按北京时间来源下单时间 `order_date >= 2026-09-12 00:00:00+08 AND order_date < 2026-09-13 00:00:00+08` 核对为 9 条，与截图一致；实际事务进一步限定订单 ID 143–151，断言目标数量恰为 9。没有按入库日期扩大范围。

生产原有 41 条，删除 9 条订单、9 个付款任务、22 条任务事件、9 条订单来源关联，解除 9 条终态 AOS 回执关联并保留去重记录；相关刷新任务／日志按既有外键处理。其余 32 条订单逐行内容以及用户、Apple ID、取机人和 TAG 规则整表摘要在事务内核验一致，任何校验失败都会回滚。清理后北京时间 9 月 12 日订单为 0。先在隔离恢复库完整演练，再停止三个 Apple 应用、生成最终备份后执行生产事务。

## 备份与回滚

服务器目录 `/var/backups/apple-order-mgr/payment-opt-20260913/` 保存初始和停写后的最终完整数据库 dump、SHA-256、删除 SQL、备份恢复／迁移／运行镜像验证、生产执行日志、旧指针、配置摘要与容器基线。备份目录受限，dump 文件采用 600 权限；专用恢复容器已删除。

旧版本 `/var/www/apple-order-mgr/releases/20260912T113544Z-f5a0dd9b2880` 保留。部署脚本在切换失败时恢复旧三个应用与前端指针；本次未触发。代码回滚不能恢复已提交的删除，数据恢复必须使用备份并评估发布后新增数据。回滚旧 Worker 还需评估新 initial 队列兼容，不能直接删除迁移或覆盖整库。

## 验证与边界

- 发布前已有 517 项后端回归、19 项隔离集成、2 项复制测试及四页合成浏览器操作验证；前后端 Lint、构建与文档检查通过。
- 生产 release 全部 SHA-256 通过，公网 HTML、JS `index-jTwbigUO.js` 和 CSS `index-Dom0jjEQ.css` 与制品逐字节一致。
- 生产 API readiness 返回 healthy／db ok，API 容器健康；三个应用镜像均为新版本，两个 Worker 在运行。
- 其他项目容器 ID／启动时间／镜像、共享 Nginx 进程及生产 app.env 摘要一致。共享 PostgreSQL 主进程 PID 13086、启动时间 2026-07-15 13:47:23 保持；首次宽泛进程匹配捕获核验 shell 导致假差异，检查后确认是核验命令自身，不是数据库重启。
- 技术发布与数据清理验证完成；真实用户登录后的筛选、复制、TAG 分配及付款业务由用户验收。
