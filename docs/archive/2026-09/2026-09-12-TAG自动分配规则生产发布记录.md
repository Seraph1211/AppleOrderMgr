# TAG 自动分配规则生产发布记录

> 状态：生产技术发布完成；真实规则及订单分配由用户在生产验证
>
> 日期：2026-09-12（北京时间）
>
> 代码提交：`main@61afd6caf2df97403e1b9fd7a5fe4c5c5cd75273`
>
> 制品：`20260911T180234Z-856e6f84be07`；tree：`856e6f84be076468ef5bcfd03963f9cf3aa7c29d`；干净 main，linux/amd64

## 发布范围

按用户「提交代码并部署到生产环境」授权发布 AOS TAG 自动分配规则：多 TAG、多系统账号完整匹配，规则内负载分配、容量和资格不足等待、未命中默认回退，以及规则管理和等待原因展示。新增 Migration `20260912000001-create-payment-tag-rules.js`。前端、API、邮件 Worker、爬虫 Worker 使用同一制品；未变更生产环境变量、权限配置、Nginx 或共享数据库服务。提交仅在本地 main，未推送远程。

## 提交和制品验证

- 待提交 23 个文件，暂存范围、无删除项及敏感文件／凭据模式扫描通过；功能提交 `61afd6c` 后工作树干净。
- 本地 488 项回归通过，新增 TAG 规则隔离集成 13 项、原付款流程隔离集成 21 项通过；32 项新单元测试包含在全量回归中。组合规则／策略行覆盖率 95.83%。前后端 Lint 无错误，保留提示性警告；构建、文档和差异检查通过。
- 合成浏览器覆盖新增、多 TAG、多账号、停用、版本冲突与重载、删除确认、错误重试、权限入口、等待原因，375／430／768／1024px 弹窗边界和保存按钮可达性通过。
- 本地构建运行／迁移镜像和前端，清单 `workingTreeClean=true`，两个镜像架构均 linux/amd64；制品两端 SHA-256 校验通过，服务器未安装依赖或构建源码。

## 备份及恢复演练

备份：`/var/backups/apple-order-mgr/tag-rules-20260912/`，目录 700、文件 600，保留初始和最终完整数据库 dump 及 SHA-256、部署配置、容器清单、旧指针、环境文件摘要、共享进程基线、发布脚本和验证日志。

将初始备份恢复到 PostgreSQL 15 隔离容器：无网络、无宿主机端口、tmpfs 数据盘。恢复后 12 个账号、135 条订单、35 个任务、31 条 Migration 与生产基线一致。新迁移镜像只执行本次 Migration，变为 32 条，规则表为空。新运行镜像在恢复库通过规则查询／新增／修改／删除与付款列表查询；用户、订单和付款任务整表内容摘要保持一致。演练数据仅在隔离库，容器完成后已删除。

## 生产切换和技术验收

北京时间 **02:06:37–02:06:46**，约 9 秒：优雅停止三个 Apple 应用、生成最终完整备份、迁移、启动新 API、等待 readiness、原子切换 current、启动两个 Worker。发布脚本在失败时恢复原应用及前端指针，本次未触发回滚。

- Migration 31→32，仅新增 `payment_tag_rules` 空表；迁移前后用户、订单、付款任务数量及整表内容摘要一致。发布后核对仍为 12／135／35，规则 0 条。
- API healthy，回环与 HTTPS readiness 均成功；匿名规则接口返回 401。三个应用均运行目标镜像，重启计数为 0。
- 邮件 Worker 和持久化爬虫 Worker 启动标记已核对，生产页面显示邮件监听正常。
- 公网 HTML、JS、CSS 与制品逐字节一致。
- 既有管理员登录会话仍有效；在新浏览器标签页只读打开付款调度台和「TAG 分配规则」，列表成功显示 35 条任务和空规则列表。保留该页面供用户验证，没有新增规则、修改接单配置、点击立即扫描或触发真实刷新／付款／状态修改。
- 其他项目容器 ID、启动时间、镜像未改变，PostgreSQL 主进程和共享 Nginx 启动时间未改变，生产 app.env 摘要保持一致。Nginx 配置检查通过，未修改或 reload。

## 回滚及业务验收边界

旧后端：`20260911T160908Z-713ddb3fe9b3`；旧前端指针：`20260911T163400Z-2d5960a2dfe0`。两者必须分别恢复，不能根据旧前端目录中的 release.env 推断实际旧后端镜像。

本次仅增加独立规则表，旧应用可忽略该表；应用回滚保留新增表、迁移记录、规则及审计，避免删除用户发布后配置。不默认执行 down 或恢复数据库覆盖发布后业务写入。完整备份作为必要恢复保障，使用前仍须评估数据窗口。

本次完成技术发布及已登录只读验证，未进行真实 TAG 规则配置、实际自动分配与付款闭环。日常 Worker 按原配置恢复运行；用户将在生产环境完成业务验收。
