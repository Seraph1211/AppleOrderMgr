# 测试与验收指南

> 状态：当前有效
>
> 最近核对：2026-09-07
>
> 基线：main@79e83e0 与当前未提交工作树
>
> 验证范围：本地工作树、隔离 PostgreSQL、代理连通及用户授权的 83 条真实订单只读 A/B；未验证真实订单写库、邮箱、完整切换业务流程和生产环境

## AOS TAG 自动分配验收

- 单元测试：`test/paymentTagRules.test.js`，覆盖完整匹配、AOS 来源限定、输入边界、规则内负载选择与当前等待原因。
- 隔离数据库：`RUN_TAG_RULE_INTEGRATION=true` 启用 `test/paymentTagRules.integration.test.js`；`DB_NAME` 必须符合 `^apple_order_mgr_.+_test_\d+$`，`DATABASE_URL` 必须为空，CLI 迁移同步设置 `DB_NAME_TEST`。先创建专用空库并运行全部 Migration，再执行；用例会清空该隔离库用户、订单、配置及关联数据，严禁指向业务库。
- 集成用例：多 TAG 多账号负载比例；账号满载、停用、撤权、关闭接单和软删除时不回退；容量恢复；跨扫描批次不阻塞；非 AOS／无 TAG／未命中默认分配；规则修改不迁移已分配任务；管理员人工转派；启停／删除及审计快照；冲突与并发版本；模式及订单付款资格；真实路由 CRUD 状态码和普通账号拒绝。HTTP 用例注入合成身份，使用正式权限中间件，不代表真实登录验收。
- `npm run test:browser:tag-rules`：`PAYMENT_BROWSER_WS` 指向专用临时 Chrome，全部 API 合成拦截。覆盖规则新建、多选与手工 TAG、多账号、停用、冲突保留草稿和重载、删除确认、错误重试、等待原因、配置入口权限和响应式；截图写到运行环境 `/tmp/tag-rules-desktop.png`、`/tmp/tag-rules-mobile.png`。
- 在隔离库演练 Migration `20260912000001-create-payment-tag-rules.js` 的 down/up 并核对规则表与迁移记录；本地开发库仅执行 up，保留既有业务数据。不创建真实 TAG 规则、不触发真实订单分配，生产迁移与业务验收单独授权执行。

## 验证层级

### 商品聚合、订单批量刷新与基础资料（2026-09-17）

- `node --test frontend/test/productDisplay.test.mjs`：同名同型号合计、不同商品、未知／0 数量及原数组不变。
- API 容器 `npx jest test/profileReadAndBatch.test.js test/selectedRefresh.test.js --runInBand --coverage=false`：三类角色按 read 权限明文读取、无权限拒绝、空电话／非法输入、批量 ID 严格校验、去重、丢失订单与单项异常隔离。
- `test/profileCleanup.integration.test.js` 仅在 `RUN_PROFILE_CLEANUP_TEST=true`、空 `DATABASE_URL`、`DB_NAME` 匹配 `^apple_order_mgr_profiles_test_\d+$` 的专用空库运行，先执行正式 Migration。验证密文落库、空手机号、错误指纹／未知外键拒绝、清理仅删除基础资料且保留订单和付款任务。不得指向开发业务库或生产库。
- `PLAYWRIGHT_MODULE=<本机已安装模块路径> node test/browser/orderProfilesAcceptance.cjs`：本地 5173 前端，全部业务 API 拦截为合成数据；1440／375px 三页面聚合、当前页选择、入队／合并／部分失败／整体失败、权限隐藏、电话清空与明文展示。没有真实官网请求或业务数据写入。
- 生产清理须额外具备当次授权、备份校验、隔离恢复和同脚本演练；记录前后聚合数量与事务内快照摘要一致性，不输出个人信息或秘密。

| 层级           | 可证明                                     | 不能证明                       |
| -------------- | ------------------------------------------ | ------------------------------ |
| 文档检查       | 链接、索引、旧路径、命令及模型字段文档覆盖 | 业务正确性、数据库实际结构     |
| Lint/构建      | 语法与部分规范、打包结果                   | 浏览器业务流程                 |
| Jest 单测      | 当前用例中的逻辑与桩交互                   | 真实数据库、邮箱、官网、代理   |
| 隔离数据库集成 | Migration、事务、API 数据契约              | 生产环境配置及业务验收         |
| 浏览器 E2E     | 指定环境的端到端流程                       | 未覆盖的场景及线上同版本已发布 |
| 生产/业务验收  | 指定版本和范围内的真实结果                 | 其他版本、角色及未测流程       |

## 当前命令

在仓库根目录：

```bash
npm run docs:check
npm run lint
npm test -- --runInBand
npm --prefix frontend run lint
npm --prefix frontend run build
```

`npm test` 默认包含覆盖率。历史 test/test_*.js 中存在会连接数据库、发送邮件或访问官网的脚本，不作为无副作用单测入口批量运行。

## 隔离数据要求

专用数据库、合成数据和明确的生命周期。CLI 与模型都必须显式指向同一个临时 `DB_NAME`；验证完成后只删除精确命名的测试库，不能将当前开发库或生产库当作隔离库。

测试数据应覆盖三个角色、不同状态、缺失关联、金额缺失、失败导入、重复订单与渠道特殊字符。历史“6 个渠道、100 个订单”仅为一次生成快照，不保证现在存在。

本地页面验收可使用 `npm run db:seed:local-mock` 对应的受保护脚本。它只在显式设置 `ALLOW_LOCAL_MOCK_DATA=true`、`LOCAL_MOCK_USER_PASSWORD`、`NODE_ENV=development` 且数据库主机为本地目标时运行，生成 8 个 Apple ID、20 个取机人、48 个订单、3 个 Mock 工作人员和 30 个付款任务，并为部分订单填写合成的外部付款人姓名；其中 16 个取机人状态为“使用中”或“未使用”，付款任务覆盖四种处理状态、24 个已分配和 6 个待分配场景。付款人不建立登录账号；脚本可重复执行且不读取生产数据，只重置 `mock_pay_*` 合成员工账号的密码，不能在生产或共享数据库运行。完整 Compose 命令见[本地开发指南](../development/本地开发指南.md)。

## 核心验收矩阵

| 功能            | 最少场景                                                                                                                                                                                                                                                                          |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 认证            | 正常登录、错误密码、锁定/解锁、本人改密、管理员重置、三设备并发登录、三个角色权限                                                                                                                                                                                                 |
| Apple ID/取机人 | CRUD、枚举、敏感字段不泄露、导出鉴权、已有联系方式/地址的二次覆盖确认                                                                                                                                                                                                             |
| 导入            | 非法文件、超限、错误行、重复行、过期/跨用户/重复令牌、事务失败与临时文件清理                                                                                                                                                                                                      |
| 邮件解析        | HTML/纯文本/Base64/实体/换行、多商品全有或全无、标签边界、`+08:00`、非法字段                                                                                                                                                                                                      |
| 邮件状态机      | 解析前留痕、复合 UID、Message-ID/MIME 重复、精确地址／精确域名来源过滤、相似后缀拒绝、主题拒绝最小留痕、发件人拒绝可恢复、1 分钟 3 次重试、中断恢复、IMAP 确认                                                                                                                    |
| 邮件扫描恢复    | 已读新邮件、mail 通知丢失、30 秒兜底、UIDNEXT=0、UID 范围返回旧值、UIDVALIDITY 变化、重启续扫、分片／部分持久化失败、SEARCH/FETCH/正文/STORE 超时、旧连接迟到事件、停机有界、最近扫描健康与心跳分离                                                                               |
| 邮件人工处理    | admin 门禁、完整字段、查看审计、重解析、草稿乐观锁、重复订单、批量逐条结果                                                                                                                                                                                                        |
| 邮件事务        | 双 Worker `SKIP LOCKED`、同订单并发、订单/邮件终态/首次刷新任务同时提交或回滚                                                                                                                                                                                                     |
| 官网同步        | URL 来源及订单号、联系邮箱不同于 Apple ID／无关联账号、返回身份、541/代理失败、官网零数量、并发旧写、五阶段与付款过期映射、动态键变化、金额／截止缺失保留、来源冲突、微信／支付宝别名、iPhone 容量单位与属性顺序、多商品唯一匹配及真实差异保留                                    |
| 刷新队列        | 重复合并、优先级提升、双 Worker `SKIP LOCKED`、租约过期、批次部分失败、重启保留                                                                                                                                                                                                   |
| 刷新策略        | 首次入库 initial 及原付款窗口校验、后续全部手动、旧周期任务跳过、首次成功或失败不再生成周期任务、旧排队任务跳过且不访问官网、页面打开不触发官网请求、人工单笔/全量、90 秒过期展示                                                                                                 |
| 代理与限流      | 541/429/441/407/517 分类、四种 Provider 鉴权脱敏、网帆有界 `sid` 会话槽、亦优短效池 TTL/格式/官方域名校验、未配置 `PROXY_PROVIDER` 时默认网帆、Provider 未知值失败关闭、候选失败保留旧 Provider、并发切换串行化、坏 IP 冷却、全局时隙、重试仍受限流；真实代理使用同订单集交错对照 |
| 仪表板          | 日期边界、过滤条件、空值、缺失金额、不同币种、异常响应                                                                                                                                                                                                                            |
| 渠道            | 聚合、改名级联、权限、特殊字符、并发，见专项清单                                                                                                                                                                                                                                  |
| 付款任务        | 只提交实际变化、状态／备注／付款人行级原子保存与回滚、幂等／版本冲突、任意状态复制本人订单链接、他人任务拒绝、订单入库 TAG 展示与下拉多选精确筛选、刷新排队／运行／终态反馈、最后爬数时间格式                                                                                     |
| 列配置          | 三页面可见性与顺序保存、跨页返回、恢复默认、损坏数据和新增列                                                                                                                                                                                                                      |
| 文件            | 上传限制、权限、公开路径、截图存储和导出公式注入                                                                                                                                                                                                                                  |

## 结果记录

### 服务器监控日志解析回归

采集器 Core 回归需覆盖标准时间戳记录、多文件和跨午夜、追加半行、截断重写、普通坏行、编码错误及去重。抢购软件在时间戳记录后输出多行 XML 时，最多 100 行结构化续行应被跳过且不参与关键词计数；续行跨两次增量扫描仍保持相同行为。文件开头出现 XML、任意普通无时间戳行或超过续行上限时，实例必须显示日志解析异常，不能用不可信结果驱动告警或恢复。

### 依赖更新回归

SheetJS 来源更换时执行 `npm run security:verify-vendor` 与 `test/excelDependency.test.js`，验证真实库的模板／解析／导出行为及上传、会话、权限边界。该套件使用临时文件、回环 HTTP 和模型桩，不证明真实数据库事务回滚；数据层另按隔离数据库层级验证。

前端路由和构建依赖更新时，可在专用空白 Chrome 上运行 `npm run test:browser:lifecycle`。脚本使用 Playwright Core，要求显式 `RUN_LIFECYCLE_BROWSER=true`、专用 `LIFECYCLE_BROWSER_WS`；`LIFECYCLE_BROWSER_URL` 默认为 `http://127.0.0.1:5173`，仅接受 HTTP 回环地址。所有业务 API 被合成响应拦截，其他外部请求阻断，禁止连接用户日常浏览器。它验证页面、路由和权限展示，不代表后端鉴权、真实登录或官网验收。

每次验收记录日期、版本、环境、用例、通过/失败/阻塞、证据位置和未验证范围。可复用用例保留在本目录；有日期的执行结果进入 archive，开发进度链接最新证据。HTTP 200 和静态通过不能替代人工或生产验收。

[渠道验收清单](渠道管理验收清单.md) · [开发进度](../development/开发进度.md)

### 收信时效修复的分层测试

- `emailScanner.test.js` 验证计时、UID 进度与连接取消；`emailServiceLifecycle.test.js` 验证可靠接收、业务处理和已读确认边界。
- `emailScannerProtocol.test.js` 使用真实 node-imap 和仅监听 127.0.0.1 的合成 IMAP 服务器，验证 UIDNEXT=0、已读通知、BODY.PEEK 和真实正文事件顺序，不访问真实邮箱。
- `emailScanProgress.integration.test.js` 通过 RUN_EMAIL_DB_INTEGRATION=true 显式启用，要求 DB_NAME 以 test_email_scan_ 开头的专用可丢弃库；验证迁移 down/up、并发游标、健康指标及 received 恢复。不得指向开发主库或生产。
- 真实邮箱另测立即已读、正常未读和断线补回；正常服务下到邮箱至入库 60 秒为目标，核对每单只生成一个订单、首次刷新任务和付款任务。

### 订单列表优化回归（2026-09-09）

- `orderControllerFilters.test.js`：无关联档案的邮件快照回退、关联优先、手机号脱敏及姓名/关键词筛选。
- `crawlerService.test.js`：完整逐台副本保留总数量；不同商品行、不同状态、缺失节点或证据不误合并。
- `npm run test:browser:orders`：通过 `ORDERS_BROWSER_WS` 连接专用临时 Chrome，所有 API 使用合成拦截，验证旧列配置、字段、成功时间、排队/执行/成功/失败重试、窄屏和只读权限。真实接口与生产业务另行验收。

### 订单管理筛选与取货时间回归（2026-09-17）

- `orderPickupTime.test.js` 验证从官网履约提示提取 `YYYY/MM/DD HH:mm – HH:mm`、多商品提示去重、非法日期／时间拒绝和日期筛选格式转换；不把无结构的履约文案推算成取货时间。
- `orderControllerFilters.test.js` 验证订单状态、商品完整名称和门店多选，同维度 OR／跨维度 AND、取货日期只匹配官网预约提示日期、非法数组／状态／日期拒绝，以及列表派生字段不改变详情原始履约提示。
- `npm run test:browser:orders` 另验证主表移除校验状态和 Apple ID、异常图标保留且整行不标红、取货时间列、多选和取货日期请求参数、商品信息筛选框及选项完整展示 `iPhone 18 Pro Max 512GB 勃艮第酒红色`、详情仍显示原有 Apple ID 内容。浏览器使用合成 API，不证明真实历史订单均有可解析预约提示；无提示或格式不明确时列表显示 `-`。

### 来源表达差异回归

- `test/orderComparison.test.js`：微信／微信支付／WECHAT、支付宝／ALIPAY 等价；花呗不并入支付宝；iPhone 颜色与容量顺序、G／GB、T／TB 同义；型号、代际、颜色、容量、数量、未知词语和有歧义的多商品继续告警。来源快照和官网原文保持不变。
- `test/officialOrderLifecycle.integration.test.js`：在门控隔离库验证历史重校验默认只读，执行只改校验字段、保留所有业务数据和时间字段，重复执行无变化；批量后续写入失败时全部回滚。执行方式沿用该文件的 `RUN_LIFECYCLE_DB_INTEGRATION` 与固定隔离库保护，不连接业务库运行。

### 代理重启与付款分配回归

- 切换失败后冷启动：有最后有效 Provider 时恢复并验证，保留候选失败诊断并继续领取；无可恢复配置／恢复失败时公开阻塞、保留队列、限制一分钟内重试；并发新切换请求不可被旧恢复覆盖。
- workerReady 必须同时满足当前实例初始化、心跳有效且未暂停；仅有历史 activeProvider 或心跳不足以显示就绪。
- `officialOrderLifecycle.integration.test.js` 覆盖恢复到领取、官网截止落库、自动分配及正式 Migration 回退升级。真实生产新单与官网重试结果单独记录。
- 付款已过期但 deadlineAt 为空的两张付款页面显示“付款已过期”；待付款且无截止仍等待官网时间，不假定邮件时间是付款截止。

## 刷新队列持续补位回归

`test/refreshWorkerConcurrency.test.js` 覆盖慢任务期间晚入队任务可启动、20 个名额用满后不超领、释放后只补空位、重叠 tick 不重复领取、持久化失败释放内存名额、暂停与代理切换排空边界，以及退出时等待领取与持久化完成。`test/officialOrderLifecycle.integration.test.js` 在限定隔离 PostgreSQL 中验证两个任务同时 running、本 Worker 续租及其他过期租约恢复。真实生产验收需记录第二单开始早于第一单结束的时间证据；只看配置值或健康检查不足以通过。

## 付款页面分页与批量刷新回归

`npm run test:browser:payments` 使用 `PAYMENT_BROWSER_WS` 指向专用临时 Chrome；所有 API 使用合成数据拦截，不能指向日常浏览器。`PAYMENT_BROWSER_OUTPUT` 可指定截图目录。验证两页分页、10/20/100 条切换、页码跳转、筛选归一、TAG 展示及可搜索下拉多选精确筛选、空结果、当前页全选与翻页清空、官网下单时间及未知值、最后爬数时间、本人权限隐藏、付款任务 `orders.id || 商品信息 || 支付方式 || 订单链接` 单项／批量复制格式、列表顺序、换行、缺失值 `-` 兜底及复制／刷新独立权限、排队／执行／终态、批量部分失败与重试、草稿保留、刷新或保存期间翻页及总数缩减退页、375px 横向滚动。它不证明生产 API 鉴权或真实官网成功率。

`accessControlPayment.integration.test.js` 在既有门控隔离库中新增 150 单分页覆盖、组合筛选总数、AOS 来源 TAG／普通订单 TAG、TAG 多选 OR、候选值权限范围、TAG 长度与数量边界、末页无重漏、空页及非法参数、本人任务归属和下单时间；不得在业务库运行。开发容器若仍绑定旧镜像，应通过 `docker compose -f docker-compose.dev.yml run --no-deps --rm -T api ...` 使用与锁文件一致的当前开发镜像执行，不能将旧依赖失败当作本次业务回归结果。

付款调度单项刷新必须在隔离数据库中校验真实服务返回 `{ jobId, status, created, merged }`，批量刷新继续返回含 `results[]` 的汇总；浏览器合成响应必须分别遵循单项和批量契约，不能用一种结构代替另一种。

### 下单时间回归

`test/orderTime.test.js`、`test/orderControllerFilters.test.js`、`test/dashboardOrderTime.test.js` 验证来源精度、日期筛选与导出；官网生命周期隔离库用例验证“解析→合并→持久化→恢复→再次刷新”不丢失时分秒。付款浏览器脚本支持 `PAYMENT_BROWSER_TIMEZONE=UTC`，验证跨时区仍展示北京时间且日期值不补造时间。真实历史恢复仅依据来源快照，不代表官网精确下单时间验收。

### 账号管理与单设备登录回归（2026-09-09）

使用 `RUN_ACCOUNT_INTEGRATION=true` 执行 `test/accountManagement.integration.test.js`，`DB_NAME` 必须匹配 `apple_order_mgr_accounts_test_数字`，且先执行正式 Migration。测试仅用合成账号与独立 HTTP 服务，不启动邮箱或爬虫。最后的 Migration down/up 会清空新增会话和操作记录，禁止使用开发主库或生产。

- 首次登录及历史强制改密标记不阻断访问；首页与导航顺序一致，零权限进入个人设置。
- 每账号最多 3 个有效会话；第 4 台须确认，取消保留三端；并发初次登录最多成功 3 次，同一确认的并发接管只成功一次；过期／重放确认凭证不能绕过确认。同一会话续签不额外占名额；到期释放名额；单台退出不影响其他设备；改密、重置与管理员锁定清空全部会话。
- 退出撤销服务端会话，迟到的旧端退出不撤销新端；旧版本 Token 和到期会话拒绝访问。
- 管理员、操作员和只读账号均可修改本人昵称、密码；资料接口拒绝角色／身份等额外字段；管理员可配置昵称和重置密码，普通账号不能重置他人密码或读取日志。
- 重置／本人改密后旧 Token 失效，账号列表不暴露密码、哈希及会话 ID；操作日志保存身份快照、中文动作、结果、IP 和时间，不包含密码、令牌、正文或查询值。
- 正式 Migration down/up 保留原用户 ID、登录名、密码和角色；回退删除本轮日志，须备份后操作。
- 可选 `ACCOUNT_BROWSER_WS` 连接专用临时 Chrome；测试需要挂载可写 `/tmp/account-artifacts`。页面来自本地 5173，所有 `/api/` 请求转入该隔离 HTTP 服务，验证真实页面与真实后端，不使用生产账号。覆盖双浏览器弹窗／旧端退出、昵称、个人改密、管理员编辑重置、操作日志及 390px 窄屏。

系统日志检查中文类型、级别、事件、结果；原技术代码和上下文保留在详情。操作记录只覆盖到达 API 的请求，不记录本地草稿输入和鼠标动作，健康检查及 `/auth/me` 心跳排除。反向代理后的真实客户端 IP 和真实员工业务使用需要部署后的单独验收。

### 过期任务手动分配验收（2026-09-09）

付款隔离集成覆盖过期不自动分配、无原因手动分配／转派、缺交接确认拒绝、状态与截止时间保持、审计原因可空、原负责人失去范围；已付／退款／取消／状态待核实／缺时限／身份异常仍拒绝。混合批次任一不合格仍全部回滚。弹窗长错误文字和重载按钮分行，按钮不得挤压换行。

### 人员统一保存与软删除回归（2026-09-09）

- 调度主页仅保留人员配置入口，弹窗表格可滚动，底部统一保存；无改动禁用保存，取消丢弃草稿，保存失败保留草稿。
- 批量成功更新多人的容量和开关；其中一人版本冲突时全部配置与业务事件回滚；无配置权限返回 403，重复人员拒绝。
- 有初始授权记录或已完成付款任务的账号可软删除；历史记录保留，默认账号／人员列表隐藏，全部旧会话失效，关闭自动接单，原用户名不可复用。
- 锁定账号有待处理、处理中、异常任务仍需先交接；禁止删除自己和最后有效管理员。
- `test/accountManagement.integration.test.js` 在专用隔离 PostgreSQL 通过真实 HTTP 验证上述后端路径及迁移 down/up；迁移回退会移除删除标记，生产恢复应优先保留新增列并按备份处理，不能将测试回退当作生产恢复步骤。

## 身份核验验收

单测入口 `test/identityVerification.test.js`：大陆身份证日期与校验位、文本精度、公式／数字拒绝、1000 行限制、重复行、原文文本导出、供应商布尔判定、未知结构、额度／鉴权和超时不自动重试。所有供应商请求均为桩，合成号码不发送到真实服务。

`test/identityVerification.integration.test.js` 使用真实 PostgreSQL 与 HTTP、真实 JWT 会话及权限，供应商回包注入合成结果。运行必须同时设置 `RUN_IDENTITY_INTEGRATION=true` 和名称匹配 `apple_order_mgr_identity_test_数字` 的专用数据库；测试会清理该库中的全部核验批次／明细，禁止使用业务数据库。

```bash
# 先在本地 PostgreSQL 创建专用库，然后仅迁移该库
# 以下名称仅为示例，重复执行前确认仍是本轮可清理的测试库

docker compose -f docker-compose.dev.yml exec -T -e DB_NAME=apple_order_mgr_identity_test_20260909 api npm run db:migrate
docker compose -f docker-compose.dev.yml exec -T -e DB_NAME=apple_order_mgr_identity_test_20260909 -e RUN_IDENTITY_INTEGRATION=true api npx jest test/identityVerification.test.js test/identityVerification.integration.test.js --runInBand --coverage --collectCoverageFrom='src/services/identity*.js'
```

集成覆盖：单人并发幂等与内容冲突；管理员／本人／其他人读取、执行和导出；逐动作撤权与旧 JWT；原文响应及数据库密文；预览不外呼、同组合一次请求并对应全部行；额度暂停与仅继续 pending；遗留 processing 转 unknown；在途停止、第二执行器不重复领取；全局 500ms 间隔；草稿过期、未配置与最多 10 个活动批次。正式 Migration 的 up/down 单独在可清理隔离库演练。

浏览器验收使用实际打包页面、隔离数据库和合成供应商结果，检查单人、错误提示、模板、上传预览、开始、刷新恢复、结果筛选、历史、原文下载和窄屏布局。真实供应商联调另需明确测试姓名／证件和最多次数；记录真实结果与额度差异，不能以合成测试或配置就绪代替。生产发布另按部署流程授权执行。

## AOS 文件采集与来源切换验收

范围：本地开发分支；自动技术验证、Windows 实机与真实业务验收分别记录，不互相替代。

- `aosParser.test.js`：历史 15 列、当前 16 列、未知尾部扩展、身份证后四位、空列、BOM、毫秒和北京时间、严格商品／Apple 链接、邮箱和订单号一致、错误不含密码或原始行。
- `aosIngestion.integration.test.js`：仅允许 `aos_ingestion_test_` 专用可丢弃 PostgreSQL 库；真实事务／HTTP 验证事件重传、内容冲突、多来源只创建一次、任务登记失败回滚、设备禁用、跨日许可、草稿日期、凭证摘要认证与密文查看隔离、服务器扫描回执核对和固定补录范围。测试会清空该库的合成业务表，不得指向开发主库或生产。
- `emailScanner.test.js`：来源切回覆盖已读旧 UID，正常游标不倒退，接收失败不确认补录。`emailProcessing.integration.test.js` 使用当日合成下单时间，保留旧邮件流程回归。
- `collector/AosCollector.Tests`：UTF-8／GB18030、半字符尾行、SQLite 加密与重启、追加／重写／重命名、回执丢失、Retry-After、跨午夜重启后旧尾行恢复、未登记历史文件不自动入库。
- 管理页面：来源预览／生效／等待扫描、筛选、空数据、错误提示、敏感查看、草稿保存与入库分离、来源链接、TAG 差异、窄屏横向表格；普通员工和设备认证不能进入管理员处理入口。

```bash
docker compose -f docker-compose.dev.yml exec -T -e DB_NAME=aos_ingestion_test_20260910 -e RUN_AOS_DB_INTEGRATION=true -e RUN_EMAIL_DB_INTEGRATION=true api npx jest test/aosIngestion.integration.test.js test/emailProcessing.integration.test.js test/aosParser.test.js test/emailScanner.test.js --runInBand
```

先创建专用数据库并执行全部正式 Migration；测试时不得同时启动使用同一测试库的 API 调度器。Windows 测试要求记录具体版本、x64、GUI 配置目录、服务身份可读权限、断网／恢复、服务强停／重启、托盘退出、开机启动、DPAPI 文件无明文、安装升级及保留队列卸载。

真实软件验收需验证实际追加或替换方式和当天跨日行为；以完整行稳定写入到订单事务提交计时，目标通常 5 秒、兜底 10 秒，官网补全单独计时。用 3–4 台电脑、每台一个本机目录和峰值每分钟几十至百单验证；合成测试通过不代表已达到真实环境时延或吞吐目标。

### H5 登录与付款任务回归

批量处理的前端单测：

```bash
docker compose -f docker-compose.dev.yml exec -T api node --test frontend/test/paymentTaskBatch.test.mjs
```

[合成浏览器脚本](../../test/browser/h5Acceptance.cjs)需要本地 Playwright 与 Chrome、已运行的本地前端（5173）。使用 `PLAYWRIGHT_MODULE` 指定已有 Playwright 模块绝对路径后执行 `node test/browser/h5Acceptance.cjs`；截图默认输出 `/tmp/apple-h5-acceptance`，可用 `H5_ARTIFACT_DIR` 指定目录。脚本在独立浏览器拦截全部业务 API，合成登录、任务和写入结果，不访问真实业务服务。检查 375/430/768px 菜单实际点击、切页自动关闭、按钮与遮罩关闭、430×500 长菜单滚动，再检查 320／375／390／430／768／1024／1366／1440／1600／1920 十种 CSS 像素宽度、手机无详情入口及附加字段、完整商品信息多选参数、复制、刷新、批量部分失败和独立权限；桌面另检查系统订单 ID／独立商品列／数据更新时间、查看付款码／复制订单信息／刷新订单状态／修改备注四项操作、表格无横向溢出、中等宽度辅助行不出现输入框、处理备注只读文本、弹窗保存、状态选择自动保存、付款人入口移除、只读权限，以及顶部提示同步产生居中自动关闭 Toast，并查看个人设置公共样式。这不替代服务端权限和真实手机验收。

人工验收还需覆盖 iOS Safari、Android 浏览器软键盘、登录失败与设备上限弹窗、剪贴板拒绝、弱网、长商品名、分页、单项及批量空备注异常、恢复、完成、状态冲突及任务转派。官网状态不随人工处理状态修改。生产操作另行授权。

### 全局待付款概览（2026-09-12）

全局待付款概览：验证未创建任务／未分配任务、多人和同名账号、零单／停用账号、人工 completed 但官网待付款仍计入、已付／退款／过期排除；筛选和分页不改变概览；无权限不能访问；加载、失败重试、空数据、关闭恢复焦点及窄屏滚动。

## 付款调度优化回归（2026-09-13）

- `test/paymentTagRules.integration.test.js` 增加 TAG 专属池隔离、容量释放后匹配补单、多启用绑定全部解除、已有非 TAG 归属保持、管理员复制及普通账号拒绝、链接缺失与审计脱敏、首次刷新 Migration down/up。
- `test/initialRefresh.test.js` 覆盖首次任务标记、清空旧调度、旧 auto/page_open 不执行官网、首次和人工成功／失败后不再自动调度。`refreshJobRepository.test.js` 验证人工批量提升旧自动任务。
- `test/browser/paymentDispatchAcceptance.cjs` 使用 `PLAYWRIGHT_MODULE` 或 `NODE_PATH` 指向本机 Playwright，在独立 Chrome 中拦截全部业务 API、阻断外部请求。覆盖完整商品信息多选请求、系统订单 ID、独立商品列、处理备注列、数据更新时间、查看付款码／复制订单信息／刷新订单状态／修改备注四项行操作，验证备注弹窗请求、顶部提示与居中自动关闭 Toast，以及单条／七条批量复制、顺序和缺失值、五并发、失败保留剪贴板、TAG 专属人员、375／768／1440px、复制与刷新独立权限及人工刷新结果；图片输出 `/tmp/apple-payment-dispatch-acceptance`。不替代真实手机和生产付款业务验收。

- `test/accessControlPayment.integration.test.js` 在门控的一次性隔离 PostgreSQL 中验证 `productNames` 精确多选、组内 OR／跨维度 AND、本人归属隔离、候选项不受本维度已选值限制、旧商品参数兼容和非法 JSON／数量／类型／长度边界；同时覆盖管理员备注修改的版本递增、加密审计、幂等重放和旧版本拒绝。测试库必须同时以 `DB_NAME` 和 `DB_NAME_TEST` 传入符合安全正则的名称，完成后删除；不得指向开发或生产数据库。

本轮另在专用 AOS 测试库启用 `RUN_EMAIL_DB_INTEGRATION=true` 和 `RUN_AOS_DB_INTEGRATION=true`，执行邮件与 AOS 两套入库测试，新增断言首次任务 trigger 为 initial、调度 nextAutoRefreshAt 为空。只使用合成记录，不连接邮箱或官网。

## 付款官网状态多选回归（2026-09-13）

2026-09-17：`test/orderStatus.test.js`、`frontend/test/orderStatus.test.mjs` 验证 12 个官网状态、pending 显示“待处理”、unknown 原样显示、移除订单 completed 与非法值兜底；`orderControllerFilters.test.js` 验证读取和筛选一致。付款权限隔离集成补充空备注的异常／恢复／完成、备注清空、管理员空原因重开保留备注、非法存量 unknown 分页，以及入库 pending 与 initial 任务同事务登记。`test/browser/orderStatusAcceptance.cjs` 验证订单页桌面／390px；付款状态浏览器脚本验证两页桌面／375px 的文案、12 项筛选和状态列宽；H5 脚本补充空备注直接改状态。全部浏览器使用合成 API，不访问真实订单或官网。

`paymentStatusFilter.test.js` 验证合法多选 OR、去重、空数组、旧单值兼容和非法类型／枚举／数量。`paymentTagRules.integration.test.js` 增加两页官网状态在分页前过滤、总数、TAG 交集及本人归属隔离，使用既有专用测试库门控。`test/browser/paymentStatusFilterAcceptance.cjs` 通过 NODE_PATH 或 PLAYWRIGHT_MODULE 指向本机 Playwright，独立 Chrome 中拦截全部 API 并阻断外部请求；验证桌面及 375px 中文搜索、多选、Escape、筛选归第一页、清空重置、空结果后仍可选择、两页标签 class 一致、TAG 原文兼容。截图目录 `/tmp/apple-payment-status-acceptance`，不能替代生产接口和真实手机付款验收。

## 下单日期及复制数量回归（2026-09-13）

- `test/orderDateFilter.test.js` 验证北京时间全日起止、单边范围、旧参数兼容、非法日期、数组输入和反向范围。`test/paymentTagRules.integration.test.js` 在专用隔离库验证午夜边界、空时间排除、四列表过滤、分页总数、本人任务范围和渠道统计；沿用既有集成测试门控，不对业务数据库执行。
- `docker compose -f docker-compose.dev.yml exec -T api node --test frontend/test/paymentCopy.test.mjs` 验证同名同型号商品聚合、非相邻数量累加、不同商品保留、输出顺序与源记录不变。
- `test/browser/orderDateAcceptance.cjs` 通过 `NODE_PATH` 或 `PLAYWRIGHT_MODULE` 指向本机 Playwright，独立 Chrome 拦截所有 API、阻断外部请求，使用合成订单验证四页日期传参、清空、单边范围、375px 日期输入可读宽度、渠道统计、订单导出、两付款页单条和批量合并复制。截图 `/tmp/apple-order-date-acceptance`；这是合成浏览器技术验证，不替代真实手机和付款业务验收。

## AOS 付款码与统一更新验收（2026-09-13）

- `test/paymentCodeValidation.test.js`：PNG 完整性、大小与外链拒绝、发布签名和产品／版本／平台／队列兼容限制。
- `RUN_PAYMENT_CODE_DB=true` 启用 `test/paymentCode.integration.test.js`；仅允许 `DB_NAME=aos_payment_code_test_*` 的独立数据库、`DATABASE_URL` 为空。CLI 同时设置 `DB_NAME_TEST` 后执行 Migration。测试会清空该隔离库相关表；验证补码不改订单、去重、乱序历史、待订单、身份冲突、来源暂停、转派／管理员权限、支付宝仅提示、签名制品和更新任务归属／幂等／终态保护。
- `/tmp/aos-dotnet10/dotnet run --project collector/AosCollector.Tests` 为本机已验证 SDK 路径；其他机器使用安装的 .NET 10 `dotnet`。Core 新增 17 项，包含 16 列日期、成功记录尾行、午夜关联、同号身份冲突、支付宝链接排除、补码队列重启／重传和签名篡改。
- `PAYMENT_BROWSER_WS` 指向专用临时 Chrome 后运行 `node scripts/testPaymentCodesBrowser.js`；全部 API 合成拦截。覆盖付款任务／付款调度 375px 和 1440px 弹窗、微信字段与图片、支付宝仅提示、终态可查看、缺码／转派错误、权限隐藏及关闭交互；截图 `/tmp/payment-code-<页面>-<宽度>.png` 在运行脚本的容器内。合成图片无真实付款载荷。
- 新 Migration 在隔离库 down/up；不在真实开发／生产业务库运行测试或清空表。Windows 引导安装、计划任务、真实手机长按保存和相册识别、无人登录、断电、更新组件退出协调、失败回滚及真实支付分别验收，不能以跨平台编译代替。

## 首次官网刷新日志与事务回归（2026-09-13）

`test/initialRefresh.integration.test.js` 仅在 `RUN_INITIAL_REFRESH_DB_TEST=true` 且 `DB_NAME=aos_initial_refresh_test_20260913`、未设置 DATABASE_URL 时执行。先在此专用可丢弃库完成 Migration；测试会清空该库订单相关数据。测试 Mock 外部网络、代理、限流与通知，但保留真实 Worker、解析器、Sequelize 模型和事务，覆盖首次刷新成功后金额及观察时间持久化、失败日志与原始错误保留、金额警告、手动刷新和无周期调度。禁止连接生产或开发业务库。

## 服务器监控验收

- 邮件停用：关闭总开关立即取消延迟、重试及已领取任务，重启用不补发；仅关闭恢复通知不影响异常告警。正文准备期间关闭再开启仍跳过旧任务，发送失败遇到关闭不再重试，普通失败保持退避。网站显示已保存状态、更新时间、未保存提示；关闭后测试邮件按钮禁用。全部发送测试使用模拟SMTP，不向真实邮箱发送。

- 已移除实例：默认列表隐藏，开启“显示已移除实例”后可只读查看，关闭后详情一并关闭；仅剩旧实例时默认空态，离线和解析异常实例仍可见。旧实例不计入待核实及规则同步统计，不能新增到规则范围；原有绑定保留并标注。直接提交旧实例处理动作应返回409。发送前移除的实例，其告警／恢复／到期提醒均跳过，合并邮件仍投递其他有效实例；历史继续按90天保留。

- 规则策略：`test/monitorPolicy.test.js`，字面任一／全部／排除、范围、输入边界、独立权限、跨日日期及有效检测连续恢复。
- 独立数据库：新建名称以 `aos_monitor_test_` 开头的空 PostgreSQL 库，在该库运行正式迁移；设置 `DB_NAME` 指向该库，`RUN_MONITOR_DB=true` 后运行 `npx jest test/serverMonitor.integration.test.js --runInBand`。测试会清空该专用库的合成表并执行监控 down/up，禁止指向共享／生产库。覆盖 HTTP 权限／设备认证、去重和重叠区间、实例静默与乐观锁、过期报告与规则版本、自动恢复、多实例与迟到配置、跨午夜流量分摊、90天清理及迁移往返。
- 采集器：.NET 10 执行 `dotnet run --project collector/AosCollector.Tests -c Release --no-restore`。先按项目及目标 RID 完成锁定还原，Core 锁文件含 win-x64，Windows 还原需显式 `-r win-x64 --locked-mode`。`MonitorTests.cs` 覆盖跨午夜、增量读取、半行、重写、去重、实例隔离、规则重算、原始日志样例与截断、持久化与过期计数、网卡变化、采样中断及上传阻塞不影响采样。可选 `MONITOR_SAMPLE_PATH` 对用户附件进行本地回放，附件不纳入仓库。
- 浏览器：启动专用临时 Chromium，设置 `MONITOR_BROWSER_WS`，运行 `npm run test:browser:monitor`。脚本拦截所有业务 API，以合成数据验证1440px流量展示、实例处理、规则试匹配／冲突／保存、接口失败、空数据、375px布局和权限撤销；不向真实业务接口写入。
- Windows 必验：LocalService 能读取实际目录、默认／指定网卡统计与系统计数对照、两个实例、多文件跨日、目录改名与移除、重启／断网恢复、规则下发及静默到期；不得将 macOS 编译或合成测试当作实机验收。
- 业务验收：先一台，再10台连续一天以上；核对覆盖分钟数、系统更新与远程桌面等额外流量，确认代理平台 GB／收发方向／北京时间账单边界后对比服务器合计。告警阈值须由用户根据实际运行调节。

### 采集器1.2.1升级与退出回归

- 20并发制品校验不整体读取EXE；签名、大小、同长度篡改、软链接、失败后恢复仍正确拒绝或恢复。独立768MB容器使用真实签名118MB制品验证并发内存。
- Windows同桌面旧1.1.0 GUI引导升级，设备身份和本地队列保留，主程序与Updater固定副本均1.2.1。
- 新版GUI保持运行时从网站下发升级，SYSTEM跨会话安全退出托盘并恢复，任务succeeded、心跳版本和监控样本同时核对。
- 一键退出后SCM为Stopped且GUI进程退出；等待一次更新轮询仍Stopped，手动启动后继续上传。服务停止失败或更新锁占用时保留窗口，不误报退出成功。

## 取机人与 Apple ID 管理（2026-09-17）

方案见[取机人与 Apple ID 管理实施方案](../planning/取机人与AppleID管理实施方案.md)。单测 `test/profileManagement.test.js` 验证跨表去重、冲突裁定、账号占用、腾讯位置映射、状态映射和订单身份歧义；`test/excelDependency.test.js` 保留真实上传路由、10MB 限制、令牌重放／跨用户、敏感导出权限与公式保护回归。

`test/profileManagement.integration.test.js` 仅在 `RUN_PROFILE_MANAGEMENT_TEST=true` 且库名符合 `apple_order_mgr_profiles_test_数字`、无 DATABASE_URL 时运行；会清空该专用库的合成档案、用户和订单，不能连接常规开发库或生产库。须先通过正式 Migration 初始化库。覆盖当前关系唯一、并发抢占、版本冲突、删除历史、批量分配不改状态、资料完整编辑和生成、密保权限、事务导入、过期预览、历史订单确认关联、逐行 TXT 录入信息导出、Migration down/up 及存量冲突阻断。回退测试会移除本次新增字段，不能用于真实库验收。

浏览器 `test/browser/profileManagementAcceptance.cjs` 全量拦截业务 API，只使用合成数据，覆盖默认列顺序、TAG 下拉多选及请求参数、姓名／身份证／Apple ID 搜索入口、桌面／375px 表单、复姓、地址／真实电话／备注保存、换绑预期值、先预览裁定后导入、历史关联确认、敏感勾选 TXT 导出、密保回显及默认状态；不是腾讯文档真实导入、官网请求、手机短信或可投递地址的业务证明。`frontend/test/columnConfig.test.mjs` 使用三套真实列定义回归跨页后顺序恢复。既有 `test/browser/orderProfilesAcceptance.cjs` 随下单电话字段名调整继续回归。

真实验收必须先做只读全量预览，核对错误行、现有关系及字段冲突，再由用户确认执行。生产 Migration、真实资料导入和部署均另行授权。
