# 监控原始日志与邮件通知发布记录

> 状态：已发布生产
>
> 发布时间：2026-09-15 20:03（北京时间）
>
> 代码：`main@7fc94d1`，未推送远程

## 发布范围

- 采集器1.2.5在日志规则命中时上报未经脱敏的完整原始行、文件名、行号和截断标记；每条最多4,000字符，每条规则每次最多3个最新样例。
- 服务器监控的当前窗口和告警历史展示原始日志；旧采集器数据继续显示命中关键词。
- 新增监控邮件通知设置、持久化投递队列和投递历史。严重告警立即发送，警告按服务器在10分钟内合并，提醒按服务器下一整点汇总，恢复通知按服务器等待10分钟合并且可关闭。
- 通知默认关闭，收件地址为空。发件配置优先使用独立 `SMTP_*`，未配置时复用订单邮箱账号和授权码；邮件只含摘要和网站入口，不包含原始日志。

## 制品

- 应用发布目录：`/var/www/apple-order-mgr/releases/20260915T114835Z-52ea8d88f6f1`
- Runtime：`apple-order-mgr-runtime:20260915T114835Z-52ea8d88f6f1-amd64`
- Migrator：`apple-order-mgr-migrator:20260915T114835Z-52ea8d88f6f1-amd64`
- 采集器发布目录：`/var/www/apple-order-mgr/shared/collector-updates/releases/1.2.5`
- `AosCollector.exe`：118,679,580字节，SHA-256 `10f3f9386cfbd55bc3ab2c31eecc3045e0e4cd7b2f436c15d60d40ba1441b506`
- 1.2.5签名通过原生产公钥验证，公钥SHA-256为 `bed150a3f5fe01797312076f49628dd386c76379573a3b31181f465a41242417`。

## 数据库演练与备份

生产备份目录为 `/var/backups/apple-order-mgr/monitor-email-20260915T114835Z-52ea8d88f6f1`。19:57初始备份SHA-256为 `03de07d400f36775d174798f28a428d613c01a844573121b443a878114a3b859`；20:02停写后的最终备份SHA-256为 `1a6c8032d26dd50c2f5a51532bf4853c109abbf997efa34200fdd7ea8915d067`。目录同时保存发布前指针、生产环境文件副本、容器快照和行数记录，目录权限700，敏感文件权限600。

初始备份先恢复到无宿主机端口的PostgreSQL 15.19临时容器。新Migrator将 `SequelizeMeta` 从35条增加到36条，创建 `monitor_notification_settings`、`monitor_notification_deliveries`、`monitor_notification_events`；默认设置为关闭且收件人为空。迁移前后订单37、用户13、设备11、规则5、实例10、告警79保持一致，新版API在隔离网络的readiness返回200，临时容器、网络和卷随后清理。

正式停写时告警因采集器持续运行自然增加到81条。正式迁移前后订单37、用户13、设备11、规则5、实例10、告警81保持一致；通知设置1条且关闭，投递和事件均为0。

## 生产切换与验证

仅停止并重建 AppleOrderMgr 的API、邮件Worker和爬虫Worker，执行正式Migration后先等待回环readiness为200，再启动两个Worker并将 `current` 原子切换到新发布目录。公网 `https://apple.godp.me/api/health/ready` 返回healthy，公网服务器监控页面返回200，发布目录 `SHA256SUMS` 全部通过。三个应用容器均运行新Runtime，API健康；邮件Worker已连接原IMAP邮箱，页面显示发件账号已复用订单邮箱且发件配置正常。

已登录生产页面验证：服务器监控入口位于订单数据源之后；四个功能区以Tab展示；通知开关关闭、收件地址为空、恢复通知开关可配置、投递记录为空；实例当前窗口和告警历史均有错误日志列。升级No.1后，页面实际显示 `Log20260915_13960.txt` 的原始代理隧道629／630错误、具体行号和完整正文。

共享宿主机PostgreSQL进程自2026-07-15持续运行，1Panel OpenResty和其他项目容器保持原运行时长，本次没有重启或改写。API、邮件Worker、爬虫Worker切换后的错误级日志计数均为0。

## 采集器升级

先单独下发US Server No.1，任务依次进入queued、downloading和succeeded，设备心跳版本变为1.2.5，并在下一次监控上报中出现 `message`、`lineNumber` 和 `truncated`。随后向US Server No.2–No.10批量下发；10台US服务器任务最终均为succeeded、在线心跳版本均为1.2.5。

核对时10台实例均已使用新版上报。8台当前窗口存在规则命中，快照包含原始行与行号；No.3和No.10当前窗口没有命中样例，因此没有可展示的原始行。CN Server 01自16:52后离线，仍为1.2.3；既有1.2.4任务仍为queued，设备上线并完成该任务后需要再下发1.2.5，不能将离线待领任务记为升级成功。

## 回退与限制

应用可将 `current` 指针切回 `/var/www/apple-order-mgr/releases/20260915T105856Z-a6dd22b581a5`，并使用原发布文件启动旧API和Worker。数据库迁移为新增表，通知默认关闭；代码回退时可保留新表。若确需恢复数据库，必须先停止写入并评估发布后的新增监控数据，再使用最终备份恢复，不能在线直接覆盖。

本次没有配置任何收件邮箱、没有开启邮件通知、没有发送真实测试或告警邮件，因此已验证SMTP配置可用性提示和队列逻辑，但真实收件投递仍需用户配置地址并启用后验收。邮件正文按设计不包含原始日志；完整错误只在具有 `monitor.manage` 权限的网站内查看。
