# 首次官网刷新日志来源修复发布记录

> 日期：2026-09-13；状态：修复技术发布完成，新订单真实首次刷新闭环待后续自然入库核验。
> 授权：延续本次会话提交与生产部署授权，用户明确要求修复已定位问题。

## 现象与根因

生产订单 184、185 的首次任务分别于 20:53、22:10 创建并执行，最终失败；错误为“数据源必须是 auto、manual、scheduled 或 system”。两笔后来均手动刷新成功，队列没有 pending／running 积压。首次刷新并未取消，失败来自前次将首次任务标记为 initial 后，爬虫日志仍只接受旧来源枚举。

成功路径先在事务中更新订单，再写 CrawlLog；无效日志来源会使整笔事务回滚。失败路径和金额警告同样使用无效来源，可能丢失日志。原测试 Mock 了完整爬虫或 CrawlLog.create，没有覆盖真实模型校验与事务。

## 修复与验证

提交 `18e2cd49db0defce84b5634b2fd3492e719bce5a` 只在爬虫服务内将 initial 的成功、失败和金额警告日志来源映射为 auto。资格判断、队列 trigger 保留 initial，手动刷新仍为 manual。没有新增枚举或 Migration，不恢复周期调度。

新增专用 PostgreSQL 集成测试，Mock 外部请求与代理，但执行真实 Worker、解析、订单合并、CrawlLog 校验和事务。修复前 3 项失败、手动 1 项通过；修复后 4 项全部通过：首次成功提交金额与观察时间、手动路径兼容、首次失败保留 HTTP_403 与日志、金额异常保留其他字段及警告。均不安排下一次周期刷新。专用库完成后删除。

全量 53 套／533 项通过，137 项环境门控跳过；后端 Lint 0 error（1903 项警告）、文档检查和 git diff --check 通过。前端无源代码改动，生产构建正常。

## 发布与保护

干净 main 构建 linux/amd64 制品 `20260913T141936Z-b0e5d41f9998`，sourceTree `b0e5d41f9998e6879f8401a27c0a84c59d61832a`。生产完整备份恢复至无外网的专用 PostgreSQL，再用待发布运行镜像、合成官网响应验证真实首次成功／失败持久化，未访问 Apple、代理或通知外部服务。

北京时间 22:21:25–22:21:31 停止旧三个应用、完成最终备份并切换新镜像及前端指针。无 Migration，SequelizeMeta 保持 34，所有既有业务表（含付款码、更新任务）切换窗口前后内容摘要一致。API healthy／db ok、两个 Worker 正常运行；公网 HTML、JS、CSS 与制品逐字节一致。线上爬虫文件 SHA-256 `2db5ac0b11da09fd7513e6debeb0ed278902ebf2c1f4d33bb4e1fe66b757285d` 与修复源码一致。

app.env、其他项目容器 ID／启动时间、共享 Nginx 与 PostgreSQL 主进程启动时间不变。未推送远程、未主动刷新或补跑历史订单、未安装或更新 Windows 采集器。Windows 1.1.0 无需重新安装。

## 备份与回滚

服务器 `/var/backups/apple-order-mgr/initial-refresh-20260913/` 保存初始／最终数据库 dump 与校验摘要、旧指针、部署脚本、隔离镜像演练、各业务表摘要及运行核验。目录 700，敏感备份 600。专用恢复容器已删除。

旧版本 `/var/www/apple-order-mgr/releases/20260913T134521Z-9553cf5a3e8a` 保留，切换脚本含旧三个应用及前端指针恢复，本次未触发。无数据库结构变化，代码回滚不需要恢复或删除数据库数据；回滚后首次日志缺陷会重新出现。

## 后续业务核验

下一笔自然入库订单应自动创建 initial，执行成功后记录官网观察时间、金额和合法 auto 日志，且 nextAutoRefreshAt 为空。真实官网仍可能出现网络或业务失败，需根据原始错误分类处理；本次合成测试与健康检查不证明未来真实官网请求必然成功。
