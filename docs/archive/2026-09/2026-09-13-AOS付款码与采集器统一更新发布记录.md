# AOS 付款码与采集器统一更新发布记录

> 日期：2026-09-13；状态：服务端技术发布完成，Windows 1.1.0 实机及真实付款验收待完成。
> 授权：用户明确要求提交代码并部署生产，部署前说明安装、旧版覆盖、候选版及配置保留。

## 发布结果

功能提交 `b4f429b62fdd3a8079465f0c6ea7267a0f5db459`，本地干净 main 构建 linux/amd64 制品 `20260913T134521Z-9553cf5a3e8a`，sourceTree `9553cf5a3e8ac6b5c283468844818b6d6f0c3d17`。北京时间 21:48:40–21:48:48 完成 API、两个 Worker 与前端切换，未推送远程。

付款任务／付款调度新增查看付款码；微信保留订单信息、权限与终态提示，支付宝仅显示“支付宝暂无法获取付款码”。服务端允许既有订单仅补码，历史和乱序保护不改写订单字段。采集器新增本地关联及加密补码队列、重试公平调度、独立签名更新组件；平台可选择版本及设备创建更新任务。

## 数据与制品保护

完整生产备份恢复到无外部网络的专用 PostgreSQL 15 容器，先在隔离库执行正式 Migration，再用新镜像验证付款码查询、付款调度查询和签名发布读取。首次演练探针直接传入数据库 BIGINT 字符串，被服务层数字 ID 校验拒绝；修正探针按正式路由转换 Number 后通过，期间尚未停止生产或执行生产迁移。

生产停写后再次完整备份，执行 `20260913000002-add-payment-codes-and-collector-updates.js`，SequelizeMeta 33→34。新增付款码与更新任务两表及索引，所有既有业务表迁移前后行数与逐行内容摘要一致，订单为 33 条。未清理业务数据，未把真实附件导入生产。

API 只读挂载 `/var/www/apple-order-mgr/shared/collector-updates`，公钥和签名版本 `1.1.0` 可被 node 用户读取。私钥保留在本地项目外，不上传服务器或放入分发包。未向设备下发更新任务，发布核验时任务为 0、付款码记录为 0；设备升级并扫描原文件后才会补码。

候选包 `collector/artifacts/AOS采集器-1.1.0-候选版.zip` 的 SHA-256 为 `ea3caa394f16b4cb029fb04be34786ade19db5727e9f5df07692295462e4f138`。EXE 的 SHA-256 为 `0b71fbe10311ddb7a2e9c0d4ea6b41efdf19171e05884bfff301620a71e27f27`，服务器与签名清单一致。仅修正包内安装说明，无 EXE 变更。

## 验证证据

- 发布前后端全量 53 套／533 项通过，133 项环境门控跳过；前序新功能专项 25 项、采集器 Core 51 项通过，Windows x64 自包含构建完成。前后端 Lint、前端构建、文档检查通过，保留既有警告。
- 公网 HTML、`index-ClmFU7Lu.js`、`index-Bs8_c0w1.css` 与制品逐字节一致；HTTPS 与回环 readiness 均 healthy／db ok，API healthy，两个 Worker 运行且启动日志无结构化 error。
- 使用已有登录态新开临时标签页：采集设备显示 1.1.0 版本选项、无已下发任务；付款调度的支付宝仅提示无法获取，微信显示订单信息、已付款警告和未采集提示；本人任务按钮及读取正常。查看产生正常访问审计，没有保存调度设置、刷新官网、复制付款链接或发起付款。临时标签页已关闭。
- app.env 摘要、其他项目容器 ID／启动时间、共享 Nginx 主进程启动时间与 PostgreSQL postmaster 启动时间保持一致。只删除本轮专用恢复容器。

## 备份与回滚

服务器 `/var/backups/apple-order-mgr/aos-payment-20260913/` 保存初始／最终 dump 和 SHA-256、旧版本指针、部署脚本、迁移／服务演练日志、既有表摘要、启动与公网核验记录。目录 700、敏感备份 600。旧版本 `/var/www/apple-order-mgr/releases/20260913T102115Z-bb103e5dfaaa` 保留；切换脚本有恢复旧三个应用及前端指针的错误处理，本次正式切换未触发回滚。

本次新增表兼容旧程序，回滚代码时保留表和历史，不执行删除数据的 down。若已经向 Windows 下发更新，应先处理在途任务，避免旧 API 不支持更新协议；全库恢复另需评估发布后新增数据，不自动覆盖。

## 安装与未验证边界

同机覆盖升级不需要卸载，原 HTTPS、设备凭证、目录与待发送队列保留。退出旧托盘后，在完整解压目录用管理员 PowerShell 执行 `.\AosCollector.exe --install`，再点击“安装 / 修复”；公钥与 EXE 同目录。已有旧版时直接双击新 EXE 只打开配置窗口。首次每台 RDP 引导一次，之后可平台统一下发；不要重新注册现有设备或删除 ProgramData 数据。

候选版表示尚未通过 Windows 实机安装、无人登录运行、自动升级和断电恢复验收，不代表功能删减。先选一台实机验证，再扩展其他设备。真实微信长按保存／相册识别、真实支付有效性和业务闭环不在本次技术发布证明范围内。
