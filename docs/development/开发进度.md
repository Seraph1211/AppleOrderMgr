# 开发进度

> 状态：付款页面分页、批量刷新、行内反馈与下单时间优化已部署生产；技术及页面只读验收通过
>
> 最近核对：2026-09-09
>
> 生产代码基线：API 与前端 main@16b7f12，tree 46aced3953669c1ec1e252a7aad17abe2f4dfe6f，发布 20260909T091227Z-46aced395366；邮件与爬虫 Worker 保持上一运行镜像，发布验收文档单独本地提交
>
> 验证范围：定向单测、隔离 PostgreSQL 集成、全量回归、两封真实 EML 离线回归、linux/amd64 制品、生产备份恢复、配置与 Worker 技术检查；新真实邮件已补查入库并完成 Apple 同步与付款分配，但收信延迟约 3 分 52 秒，未验证真实付款

## 当前阶段

### 下单时间精度丢失：修复与历史恢复

2026-09-09 已定位：官网日期 `YYYY-MM-DD` 被写入来源 `order_date`，数据库存为 UTC 午夜，界面转换后统一显示 08:00。9 个付款任务关联订单命中，来源快照保留原始时分秒。此前仅核对页面字段一致，遗漏爬虫覆盖后的精度验证，本次补上完整持久化回归。

已阻止官网日期及官网精确时间覆盖邮件/人工来源字段；官网精确时间仍独立保存。订单列表、详情、渠道、付款页面、Apple ID 最近下单日期、人工补单、Excel 导出统一北京时间，日期筛选与统计使用完整北京时间日界。人工补单保留秒且拒绝仅日期；未知值不使用入库时间或付款截止倒推。恢复工具默认预览、明确指定订单、事务锁行，仅恢复同日 UTC 午夜覆盖且有精确来源快照的记录，重复执行无副作用。

验证与发布证据见[下单时间修复记录](../archive/2026-09/2026-09-09-下单时间精度修复记录.md)。421 项单测、19 项隔离数据库集成、UTC 浏览器回归及 Lint/构建/文档检查通过；生产备份已恢复临时库核对 109/9/23，生产尚未更新。

### 付款页面分页、刷新反馈与下单时间优化

2026-09-09 已按用户授权实现、本地提交，并于 17:13:58 完成最终生产更新。原调度接口只有条数上限，付款任务前端忽略已有分页；现调度补齐服务端分页总数，两页复用订单页分页组件并支持 10/20/50/100 条，筛选和条数变化回第一页、翻页清空勾选、越界自动退页。两页展示与订单管理一致的 orderDate，下单时间字段缺失时回退已确认的官网时间，两者都缺失显示待核实；不改变官网截止计算。调度刷新移除顶部入队横幅，两页逐行反馈排队、运行、成功／失败；本人任务新增当前页勾选批量刷新，保留权限与草稿，刷新完成回填当前页，单项失败不阻断整批。

当前锁文件镜像下全量 413 项单测通过（56 项外部测试按门禁跳过），隔离付款集成 20 项通过；两页合成 API 浏览器验证通过分页、筛选、部分失败、重试、草稿与窄屏。运行中旧开发容器曾因旧依赖出现 6 项失败，已使用当前开发镜像复测通过；未启动或重建本地邮件／爬虫 Worker。无 Migration；两次备份恢复成功，最终版本生产 API、静态资源和已登录页面核对通过，订单／任务／迁移数保持 109／9／23。线上最终核对本人 3 项与调度 9 项的下单时间、分页及勾选状态，未触发真实官网刷新或付款。邮件、爬虫、其他项目容器和共享服务未重启，配置未改。详见[付款页面发布记录](../archive/2026-09/2026-09-09-付款页面分页与刷新优化发布记录.md)。

### 少量手动刷新仍排队：持续补位修复

2026-09-09 15:45 只读定位：生产实际并发上限 20、全局 5 RPS、调度周期 5 秒，代理就绪且未暂停。手动任务 4074 于 15:43:44.600 入队、15:43:45.155 开始、15:43:53.890 完成；任务 4075 于 15:43:45.176 入队（晚于上批领取 21 毫秒），直到 15:43:55.155 才开始，15:44:03.725 成功。此前任务 4072/4073 同样出现跨批次等待；检查时五个手动任务均已成功，无 pending/running 遗留。

根因是 `refreshWorkerService.runOnce` 在整批 `Promise.allSettled` 结束前一直保留 `workerState.running=true`，后续 tick 仅心跳并跳过领取，空闲并发名额不会补入新任务。20 只限制每批最多领取数；此前扩容验证覆盖配置和会话池，未覆盖晚入队任务利用空闲名额的场景。已按用户授权改为每轮按在途任务数计算空闲名额，领取循环不等待订单执行；结果持久化前不释放名额，活跃任务续租，暂停或切换时停止补位，关闭时等待在途结果。保持全局 5 RPS、20 并发、每 5 秒领取。全量单测 413 项、隔离 PostgreSQL 集成 18 项通过，Lint 无错误。16:03:53 已部署生产，备份恢复验证通过，无 Migration；任务 4080 在慢任务 4079 结束前开始并成功完成，证实生产持续补位有效。慢任务最终遇到官网 HTTP 541，本次未掩盖该上游失败；当前无活动任务遗留，保持 5 RPS。发布及真实订单重叠执行验收见[修复记录](../archive/2026-09/2026-09-09-刷新队列持续补位修复发布记录.md)。

### 网帆统一默认线路

2026-09-09 用户指定网帆作为默认线路。生产环境与运行 Worker 原本已显式使用 `fanproxy_tunnel`，但通用默认常量、环境示例、本地环境和系统页首选标记仍为亦优。现已统一为网帆，其他 Provider 仍可手动切换；不会覆盖数据库中管理员已有的切换意图，也不新增自动跨供应商降级。默认值和 Provider 顺序回归已更新，全量单测 404 项通过、53 项外部环境测试按门禁跳过，前后端 Lint 无错误，前端构建、文档检查通过。代码已提交 `ae6b92b`，15:31:01 完成生产发布；浏览器显示当前生效和目标均为“网帆隧道代理（首选）”，爬虫代理已就绪、邮件监听正常。新容器确认默认及实际配置均为网帆，保持 `20/20/5/3`。备份恢复验证通过，未执行 Migration，数据库迁移仍为 23 条；详见[扩容记录后续验收](../archive/2026-09/2026-09-09-网帆二十并发扩容记录.md#后续网帆统一为默认线路)。

### 网帆套餐扩容

2026-09-09 14:44 已按授权在浏览器核实新套餐 20 并发、1 分钟粘性，并将生产改用对应的新账密；新旧套餐账号和密码均不同。显式配置会话槽 20、Worker 并发 20、5 RPS、最多 3 次尝试，仅重新创建爬虫 Worker。启动日志和实际配置确认生效，代理健康检查 HTTP 200，数据库代理就绪且未暂停；API 和邮件 Worker 未重启。20 槽循环复用回归及 Worker 测试共 18 项、Lint、文档检查通过。运行时代码、镜像和数据库结构不变。

扩容后尚无足量真实刷新样本，保持 5 RPS；已建立百单只读跟进，正常且样本不足时不通知，达到 100 个有效任务后报告是否适合提速。该跟进不自动改配置。配置备份、验证边界和回退见[扩容记录](../archive/2026-09/2026-09-09-网帆二十并发扩容记录.md)。

### 新单自动分配阻塞诊断

2026-09-09 13:25–13:27 新单自动分配阻塞只读诊断：订单尾号 7921／3883 分别于 13:23:19／13:23:39 入库，付款任务已创建，但首次官网任务 4028／4029 保持 pending，无抓取尝试、无失败计数，官方截止为空。付款调度 auto、两名接单人员均启用且容量 10；调度扫描正常，缺少有效官网截止使任务不进入自动分配候选。

根因是 `refreshWorkerService.reconcileProxyProvider` 在持久化 `requested=yiyou_http`、`active=fanproxy_tunnel`、`switchStatus=failed` 时提前返回 `awaiting_new_switch_request`。13:06 发布重启后 ProxyManager 内存实例为空，代码没有先恢复最后有效 Provider；runOnce 继续在领取任务前返回，仅更新心跳。数据库 active 字段是历史记录，不能证明新进程已初始化。隔离桩复现确认：心跳 1 次、代理恢复 0 次、自动入队扫描 0 次、领取 0 次。上线验收未覆盖“切换失败后重启”的恢复路径，之前健康结论不证明新单官网抓取可执行。

- 已落实方案：冷启动优先恢复并校验持久化最后有效 Provider，保留候选失败诊断；没有可用代理时公开阻塞原因，不以心跳代替可服务状态。补充失败后重启与新单官网截止→自动分配回归。
- 临时恢复路径：经明确恢复操作重新请求切换至网帆，候选健康检查通过后让现有任务自动执行，取得有效官网截止后按当前自动分配规则处理。未确认截止前不伪造计时或绕过分配资格。
- 定位阶段仅只读诊断和本地无外部 I/O 桩复现，未触发生产写入；后续经授权恢复和发布见下节。另有过期订单截止缺失仍显示“等待官网时间”的前端文案问题，来自倒计时仅判断 deadlineAt 为空，非新单阻塞根因。

### 代理重启恢复修复实施

- 已按授权重新请求网帆 Provider，通过候选校验后恢复队列。尾号 7921 已分配 Test_1、官网截止 13:52:43；尾号 3883 首次抓取失败后重试成功，已分配 admin、官网截止 13:53:08。没有人工伪造截止或跳过分配规则。
- 修复冷启动 failed 状态恢复最后有效代理，保留失败候选诊断；恢复失败最多每 60 秒重试，新请求可结束冷却；并发新请求出现时放弃旧恢复结果。恢复后才开始领取任务。
- 正式 Migration 增加 workerProxyReady／workerProxyErrorCode，API 根据初始化结果和心跳时效报告就绪；系统页区分上次生效与当前可用，显示阻塞原因，允许重新选择历史 Provider。付款调度与任务页优先展示官网已过期、已付款及取消状态。
- 全量单元 403 项通过；隔离 PostgreSQL 集成 16 项通过，包括失败后重启→代理校验→抓取截止→自动分配和新 Migration down/up。前后端 Lint 无错误、前端构建及付款阶段文案检查通过。生产于 13:45:48 完成切换，迁移 22→23；停写迁移期间 105 订单／25 邮件／5 付款任务及全部订单字段摘要不变。两个发布备份恢复验证通过，共享服务未变。生产浏览器确认新单负责人和倒计时正常，过期文案已修复，系统显示代理已就绪。见[发布验收记录](../archive/2026-09/2026-09-09-代理重启恢复与付款分配修复发布记录.md)。本地业务库另经备份后升级至 23 条迁移。

### 已发布的来源表达优化

2026-09-09 来源表达误报优化已于 13:06 部署生产。原比较仅统一空白、全半角和大小写，将“微信／WECHAT”及“深蓝色 256G／256GB 深蓝色”错误标成冲突。现按用户确认增加微信和支付宝别名，iPhone 名称按完整识别的代际、版本、容量与中文颜色比较；多商品按全局 SKU 优先、双方唯一的语义名称匹配，保留未知词语、真实字段差异及歧义告警。原始来源快照与官网文本不改变。规则及历史处理方式见[订单爬虫](../design/订单爬虫.md#来源表达差异与真实冲突)。

- 新增默认预览、明确指定订单号的重校验 CLI；执行时同事务锁行，只移除已有可证明等价的付款方式／名称冲突，保留其他问题。仅写校验字段，不更新生命周期、来源数据、updatedAt、最后官网更新时间或刷新计划。
- 本地回归：全量 43 套件、396 项通过，数据库门控用例默认跳过；最终定向 41 项和另行启用的隔离 PostgreSQL 集成 14 项通过，覆盖真实解析入口到落库的微信／支付宝等价、历史预览零写入、时间保持、重复执行和事务回滚。新增语义比较模块行覆盖率 100%。后端 Lint 0 error（保留既有 warning），文档 58 份／20 模型检查和差异空白检查通过。测试使用新建的固定隔离库执行正式迁移，结束后删除该临时库；未运行真实邮箱、官网请求或生产写入。
- 用户授权提交、部署及历史清理后，完成两次备份恢复验证并切换生产；2 笔订单共 4 条表达误报已清理，生产鉴权订单 API 返回 valid。全体订单除校验字段外的排序摘要一致，最后官网更新时间、付款过期状态及自动刷新关闭保持不变。102 订单／22 邮件／2 付款任务／22 迁移记录不变，API healthy、邮件扫描正常，共享服务未重启。见[本次发布记录](../archive/2026-09/2026-09-09-订单表达误报修复生产发布记录.md)。

### 已发布基线

2026-09-09 已发布邮件 UID 持久化扫描与订单管理页优化。邮件事件即时扫描、30 秒兜底、连接截止和中断恢复已上线；订单页显示官网最后成功更新时间、邮件 Apple ID/取机人回退、取机人标签和右侧单行刷新，移除电话列，修复官网逐台副本重复商品及窄屏布局。355 项测试、18 项独立数据库集成、协议及合成浏览器验证通过；生产备份两次恢复、新迁移 up/down/up 演练及切换通过，核心数据数量不变，共享服务未变。生产邮件连续 12 次扫描覆盖超过 5 分钟，最长间隔 30.146 秒，无错误或重连。真实任务 4019/4020 均成功，两笔商品均为一行、数量 2，过期订单自动刷新关闭。该发布仍保留严格来源差异提示，本地语义优化见上文；新到邮件 60 秒目标尚无新样本。见[邮件扫描与订单列表修复发布记录](../archive/2026-09/2026-09-09-邮件扫描与订单列表修复发布记录.md)。

## 近期执行记录（以下保留各阶段当时边界）

### 2026-09-09 收信时效修复本地完成

- 按已批准方案新增独立 UID 扫描器与可靠接收游标：mail 立即扫描，30 秒兜底；20 封分片全部原文持久化后才推进；UIDNEXT=0、已读状态、断线和重启均不改变可靠接收语义。
- SEARCH、FETCH／正文、STORE、连接和游标操作都有 30 秒截止；旧连接取消与迟到事件隔离，定时计划不因重连重置，停止最多等待 10 秒。received 超过 1 分钟的中断记录恢复到持久化重试，恢复仍校验来源。
- API／邮件处理页面区分心跳与最近成功扫描；从未成功、超过 90 秒或最近扫描失败均不显示收信正常。重复终态不刷新最近收信时间，也不重复写已读标记。
- 本地技术验证：匹配锁文件的开发容器全量 347 项通过，47 项数据库门控用例默认跳过；另启用邮件与游标隔离 PostgreSQL 集成共 18 项通过。真实 node-imap + 回环 IMAP 协议测试通过。扫描器行覆盖率 95.45%，游标模块隔离集成行覆盖率 90.47%；后端／前端 Lint 无错误、前端构建、文档 57 份／20 模型检查通过。
- 本地已备份并按顺序执行原待执行的 20260908000004 和本次 20260909000001 两条迁移，SequelizeMeta 20→22；订单 58、邮件 18、付款任务 31 均未改变，API ready 返回 200。未重启现有本地邮件／爬虫 Worker，未连接真实邮箱写标记。
- 初次全量检查失败来自现有开发容器依赖陈旧，改用与当前 package-lock.json 哈希一致的隔离开发镜像并挂载 vendor 后通过；不修改锁文件或降低既有依赖断言。生产仍为 8189a35。本次未提交、推送、生产迁移或发布；真实新邮件 60 秒入库验收待发布后执行。详见[本地修复记录](../archive/2026-09/2026-09-09-真实订单全流程生产验收记录.md#十一2026-09-09-收信时效修复本地实现与验证)。

### 2026-09-09 新订单实时收信延迟

- 尾号 7865 的邮件 05:05:15 到达，05:05:16 有新邮件事件，直到 05:09:06 自动重连回查才被获取；05:09:07 入库，05:09:17 官网刷新成功并分配 admin，付款截止 05:34:28。延迟约 3 分 52 秒，来源过滤、解析及入库没有失败。
- 已发现实时仅查 UNSEEN、已读兜底每 10 分钟的时效缺口；检查时该邮件为 Seen，但现有日志不足以证明标记变更与搜索的先后顺序，亦缺少重连原因。需改进 UID 增量发现和短周期兜底，尚未实施。本轮只读诊断，真实入库由 Worker 自行完成，详见[生产验收记录第十节](../archive/2026-09/2026-09-09-真实订单全流程生产验收记录.md#十2026-09-09-0505-新订单收信延迟只读诊断)。
- 根因复核：独立只读 IMAP 会话实测服务器约 200 秒后 BYE/autologout，与生产约 231 秒重连周期吻合；邮箱不支持 IDLE，45 秒观察中客户端持续发送 NOOP，不能归因于未保活。已隔离复现重连重置补查计时、并发 SINCE 降级 UNSEEN、未完成 SEARCH 在断线后阻断后续扫描三个缺陷，后者不符合本单已经自动恢复的直接表现。
- 已按用户确认实施[实时发现与连接恢复补充方案](../planning/邮件解析与处理可靠性优化方案.md#十四2026-09-09-实时发现与连接恢复补充方案)：UID 发现、30 秒兜底、可靠接收进度、命令超时、连接代际隔离和扫描诊断；本地实现见上节，尚未发布。真实 UID 定时搜索对照因 SSH 通道中断未完成，只清理诊断子进程，没有修改或重启生产服务。

### 2026-09-09 发布复验发现手动刷新日志来源不兼容

- 首轮已提交 63794c9 并发布 20260908T204608Z-f66174157e13，停机约 7 秒，备份恢复及技术验收通过；历史后续自动计划归零。手动刷新在成功抓取后的日志写入中因 manual_single 不属于 CrawlLog.source 枚举而回滚，已补映射与 631 非标准状态码日志兼容，322 项开发回归通过；补丁 8189a35 已发布，手动任务 3994 成功，见当前阶段。完整证据见[本轮发布记录](../archive/2026-09/2026-09-09-订单刷新修复生产发布记录.md)。

### 2026-09-09 本地提交与生产发布授权

- 用户授权提交当前本地改动并部署生产；本轮从干净 main 提交构建 linux/amd64 制品，备份与隔离恢复通过后切换，保持生产配置和共享服务。执行状态见[发布记录](../archive/2026-09/2026-09-09-订单刷新修复生产发布记录.md)。

### 2026-09-09 取消付款二维码功能并修复订单刷新

- 用户取消「查看付款码」。本轮新增按钮、弹窗、客户端方法、Router／Controller、取码服务及专属测试已全部撤下；原付款任务页面、复制订单链接与刷新操作恢复原代码，无新增二维码 API。
- 用户确认订单联系邮箱可与下单账户 Apple ID 不同：取消相等限制，有合法订单链接时不要求关联账号；保留 HTTPS、Apple 精确域名、路径、邮箱格式、订单号及官网返回身份校验。联系邮箱不会覆盖入库 Apple ID。此修复处理尾号 0752 在网络请求前被误判 IDENTITY 的原因，不代表线上异常已清除。
- 自动资格不再将所有空／未知支付状态视为未付款：processing、payment_received、ready_for_pickup、shipped 与终态均停止自动刷新；明确未付款继续分钟级轮询，未知支付仅在待核实阶段且付款窗口内核实。时间优先官网截止／准确创建时间，再来源订单时间（缺失才用入库时间）；无有效时间或超窗停止。到期扫描清除下一调度，已入队任务执行前复核并跳过，手动刷新仍可用。未回填历史支付字段，未清理线上队列。
- 本次相关用例 100 项通过，包括历史空支付、新单 30 分钟边界、时间优先级、已排队任务不访问官网、联系邮箱与账户不同及账号缺失仍进入网络流程。全量开发容器首次运行 309 通过／6 失败／42 跳过，失败来自旧安装依赖（SheetJS、semver、uuid、ip-address、qs），不是放宽用例解决。
- 复用隔离镜像 apple-order-risk-dev:20260909，核对 package-lock.json SHA-256 与工作树完全一致后只读挂载当前 src/test/scripts/vendor，禁用外部网络、不挂载 .env：全量 Jest 39 套件／315 项通过，3 套件／42 项环境门禁跳过。前后端 Lint 0 error；锁文件一致的隔离前端 Vite 6.4.3 构建通过，保留既有大包警告。文档链接／模型字段检查通过。
- 验证边界：本轮未提交、推送、部署、迁移、重启 Worker 或修改生产订单。生产旧 IDENTITY 标记须在发布后经受控手动刷新成功再恢复；真实邮件→官网→付款任务的端到端复验仍未完成，不把 Mock 或只读官网探查当作生产业务验收。

### 2026-09-09 历史订单刷新与新订单身份异常只读诊断

- 生产只读快照：历史 100 单的 `payment_status` 全为空，Apple ID 全为 `test…@example.com`；其中 pending 12、processing 20、ready_for_pickup 14、shipped 15 共 61 单符合当前自动资格。策略仅排除 paid/refunded、终态和显式停用，没有区分历史空支付状态与新单待核实，导致持续自动入队。截图所示处理／可取货历史订单已连续失败 40 余次，主要为 HTTP 403；不是手动刷新或纯展示残留。
- 新订单（内部 ID 108，尾号 0752）已在 03:49:10 入库，已有 pending 付款任务；03:49:35 自动刷新与 03:54:40 手动刷新均报 IDENTITY。生产运行容器内只读校验确认 Apple URL 的协议、主机、路径和订单号合法，解码后的链接邮箱与订单 Apple ID 快照不一致，且不存在关联账号覆盖或首尾空白原因；在访问官网前被拒绝。当前已关闭该单自动刷新。当时尚未确认邮箱语义；随后用户确认属于合法差异，本地修复见上节，生产标记未改。
- 后续决定：用户确认取消联系邮箱与 Apple ID 相等限制，并取消付款二维码功能；历史刷新资格修复与验证见上节。
- 本轮仅更新诊断记录，未修改业务代码、生产订单、队列、配置或服务，未提交／推送／发布。明细见[生产验收记录](../archive/2026-09/2026-09-09-真实订单全流程生产验收记录.md#八历史订单刷新与新订单身份异常只读核对)。
- 文档验证：开发 API 容器未挂载 vendor，完整 docs:check 在已有 vendor/README.md 引用处失败；改由宿主机运行同一链接脚本，56 份文档通过，容器内 docs:check:schema 的 19 个模型检查通过。未为文档检查重建容器或修改挂载。

### 2026-09-09 真实订单全流程生产验收与功能开启

- 已按邮件设计、配置、处理服务、测试和运维文档的级联顺序实现来源修复：新增 `IMAP_ALLOWED_SENDER_DOMAINS`，地址/完整域名任一命中即通过；主题未命中保留最小 `SUBJECT_NOT_ALLOWED` 审计，主题命中但 From 未授权时以 `SENDER_NOT_ALLOWED` 进入 `manual_review` 并保留加密 MIME。
- 定向单测 3 套件/25 项和精确隔离 PostgreSQL 集成 13 项通过；两封真实 EML 在生产等价配置下均通过来源与必填字段解析。全量后端 39 套件/288 项通过，3 个环境套件/42 项跳过；后端 Lint、供应链校验、文档检查、前端 Lint 和 Vite 构建通过。后端生产依赖 0 漏洞；前端生产依赖存在 2 个中危 `react-router` 通告，未达本次高危阻断阈值。
- 已构建并校验 `linux/amd64` 发布制品，最终备份路径为 `/var/backups/apple-order-mgr/20260908T192317Z-20260908T192217Z-0c4c67045dfd/`，备份恢复计数为 21/100/19。生产已切换发布，三个应用容器均使用新镜像；未执行 Migration，未重启共享 PostgreSQL 或 Nginx，未 commit、push。
- 生产技术验收：HTTPS 首页/readiness 均为 200，未鉴权订单 API 为 401；邮件 Worker 连接正常、连续失败 0；爬虫 Worker 运行且未暂停，代理切换成功、全局 5 RPS/并发 8/单单 3 次重试；付款调度启用自动模式且有 2 名可自动分配人员。生产分类器已证明 `lanu.cn` 精确命中，子域名/相似后缀/普通主题分别以稳定错误码拒绝。

- 已按生产版本、邮件处理、官网刷新、付款任务和前端权限建立分阶段检查表；原始邮件、完整订单号、订单链接、密码、验证码及个人信息不进入文档或 Git。
- 用户随后明确本轮是正常线上真实业务并授权代理、自动刷新、爬虫 Worker 和付款调度全部打开。执行前备份生产配置和数据库，新备份已在隔离 PostgreSQL 15 恢复，100 个订单、16 条邮件日志与 21 项 Migration 一致；临时验证容器和网络已精确清理。
- 已同步本机既有代理配置到生产，凭据未输出；`PROXY_ENABLED=true`、`AUTO_ORDER_REFRESH_ENABLED=true`。付款调度通过业务服务启用为 `manual`，范围从 02:11:36 开始，写入正式调度审计；历史 100 单不会进入本场付款任务。
- crawler-worker 启动后为存量调度生成 69 个任务；`yiyou_http` 对真实订单页连续 24 个任务返回 HTTP 403，且未自动暂停，已于 02:13:50 精确停止 crawler-worker。API 与邮件 Worker 保持运行。
- 追加门禁确认 `fanproxy_tunnel` 使用现实现、全国混播格式及 Chrome 128 请求头均返回 403；`kdl_private` 提取成功但请求返回 407。生产配置保留 `fanproxy_tunnel`，爬虫保持停止，等待本次新订单链接做单次门禁。
- 用户随后要求在创建订单前恢复 crawler-worker，并已在生产页面开启自动分配。02:42 已精确重建 crawler-worker：`fanproxy_tunnel` 为活动 Provider，代理切换与心跳正常；调度为 `auto`，2 名人员均开启自动分配、容量各 10。存量任务快照为失败 30、待处理 53、运行中 2，历史订单页 HTTP 403 问题仍未关闭，本次新订单需单独跟踪。
- 03:00:19 和 03:00:39 Worker 收到新邮件事件，同时间窗新增记录 20、21 均在约 0.1 秒内转为 `ignored/non_order` 并完成已读确认；邮件日志由 16 增至 19，订单保持 100。当前代码只接受主题包含 `NULL`、`预订助手`、`预订成功` 之一且 From 命中生产唯一白名单的邮件；`ignored` 流程会立即清除主题、From、Message-ID、MIME 指纹和原文且不保存未命中的规则，因此现有生产证据无法进一步区分两项条件。已登记 LIVE-05 和 LIVE-06，未修改生产数据或配置。
- 用户提供两封原始 EML 后已在本地开发容器按当前版本离线核对：两封主题均命中全部 3 个关键词，均成功解析 1 个商品及 Apple ID、订单链接和订单时间等必填字段；两封标准化 From 彼此不同且均不匹配生产唯一白名单，虽同属 `lanu.cn`。LIVE-05 根因确认为动态发件地址与精确白名单策略不兼容；原邮件已进入不可自动重处理的 `ignored` 终态，未更改生产配置、邮件标记或业务数据。
- 已记录 P1：首页健康检查不能代表订单页、连续 403 不自动暂停、日志输出完整订单号和代理端点、私密代理生产认证失败。当前未修改业务代码、未重新发布、未提交或推送文档。

## 本轮完成

### 2026-09-09 生产发布与技术核验

- 已将 75 个文件的改动提交为 `61499d04351ea1af28501433759503249514aa07`，普通推送至远程 main；GitHub 运行 `34257553332` 的 backend、frontend、secret-scan 均通过。Secret Scan 实际检查本次新增 9 个提交，不代表更早历史已清理。
- 从干净本地 main、精确 commit/tree 构建 `linux/amd64` 制品，完整 SHA-256 校验通过；生产 `current` 已指向新发布目录。停机窗口为北京时间 2026-09-09 01:35:40 至 01:36:18，约 38 秒。
- 初次及最终停机备份均在隔离 PostgreSQL 15 恢复并迁移，8 项生产 Migration 全部成功。迁移前后 542 个 Apple ID、30 个取机人、100 个订单、4 条邮件日志与 1 个用户一致；1,312 个敏感字段解密和 30 个盲索引校验通过，生产未重复执行敏感字段转换。
- API healthy，仅监听 127.0.0.1:3001；本地与 HTTPS readiness、前端首页／JS 制品散列及未登录订单 401 通过。邮件 Worker 已连接、连续失败 0，启动回查新增 12 条 ignored 记录并完成已读确认，正文保留数 0；订单仍为 100，邮件日志现为 16。这不是新订单邮件入库验收。
- 生产 app.env 与 Nginx 配置散列不变；共享服务、其他容器的启动时间不变，无 Nginx reload、凭据轮换或远程强推。专用隔离库／容器／网络已清理，初次和最终备份及旧发布版本保留。
- 生产真实登录、登录后的只读 API、人工页面验收、真实订单邮件、官网与代理未验收。爬虫停用期间人工刷新任务也不会由 Worker 执行。历史 Git 敏感资产和外部凭据风险按用户决定延期，未关闭。完整命令、失败处理与证据见[生产发布记录](../archive/2026-09/2026-09-09-订单生命周期与安全修复生产发布记录.md)。

### 2026-09-09 提交、推送与生产发布授权

- 用户明确授权将当前工作区提交并普通推送，再从提交后的本地 `main` 构建生产制品和部署；暂不强制改写远程分支、暂不轮换外部凭据。历史泄露仍为未关闭风险，不降低扫描规则。
- 发布前重新核对现网：`current` 指向 `20260906T092727Z-81340dbf509e`，API 与邮件 Worker 运行，爬虫未启动；保留代理／自动刷新关闭状态。宿主机数据库实际服务为 `postgresql-15.service`，不操作共享 PostgreSQL、Nginx 或其他容器。
- 本节记录当时的提交门禁与发布准备阶段；实际提交、推送、制品、备份演练、迁移及切换结果见上一节及发布报告，不以授权代替完成证据。

### 2026-09-09 剩余依赖风险修复与构建回归

- 用户确认处理剩余风险后，已采用 SheetJS 官方 0.20.3 固定制品，增加 SHA-256 安装前校验与 npm integrity；前端更新 Vite 6.4.3、React Router 7.18.3 及受影响工具链，保留 React 18、现有 URL 和权限规则。后端全依赖、后端生产依赖、前端全依赖审计均零告警，退出 0；不是全系统无风险保证。
- 新增 17 项 Excel 开发自测：真实解析与序列化、两份现有模板、中文／前导零／空值／行号、畸形文件、10 MB 上传限制、1000/1001 行、字段校验、权限、会话重放和导出脱敏。发现列映射读取原型继承键的既有问题，按自有键白名单修复；修复前回归用例失败，修复后通过。导入服务语句覆盖率 97.75%、分支 100%。
- 浏览器自测工具调整为支持 Node 20 的 Playwright Core 1.63.0，不下载或启动业务浏览器；专用临时 Chrome 使用合成 API 完成 13 项回归，增加未登录、会话失效、强制改密、越权跳转、客户端导航及后退。前端仍保留既有大包和窄屏分页布局问题，不计入本次安全修复完成范围。
- Node 20.20.2 的全量 Jest 39 套件／283 项通过，3 套件／41 项默认跳过；后端及前端 Lint、54 份文档／19 模型检查通过。当前工作区 348 个非忽略普通文件的 Gitleaks 扫描零命中。正式 Dockerfile 的运行／迁移目标、前端构建及开发镜像在本地 arm64 构建并探针通过，无 Node engine 不匹配；尚未构建最终提交对应的 linux/amd64 发布制品。
- Docker 构建上下文排除历史 release-artifacts 和导入运行文件，避免不必要地进入开发镜像。未覆盖既有 .env、未重装既有 API/Worker 依赖、未执行数据库 Migration 或真实数据写入。
- 当前工作区仍未新增交付提交，未 push 或生产部署。远程 main@0fd7f80 已发布历史与外部凭据轮换仍未闭环，需要明确强推范围、维护窗口和外部账号处置；不得通过扫描忽略项绕过。详情见[依赖风险收尾与构建回归](../archive/2026-09/2026-09-09-依赖风险收尾与构建回归.md)。

### 2026-09-09 本地安全修复与剩余发布门禁

- 经用户确认，已脱敏两份历史归档，并仅重建 8 个未推送提交；`main` 从 `57859e7` 变为 `405ed22`，远程 `origin/main@0fd7f80` 未改变。逐提交树差异仅含两份归档，其他工作区文件校验值保持一致，未强推、未删除旧 reflog 对象。
- 新的 8 提交区间 Gitleaks 扫描退出 0、零命中；已发布 `origin/main` 历史扫描退出 1、3 处命中，历史泄露与凭据轮换仍未闭环。未降低 CI 扫描规则。
- 修复 B1/B2/B3/B4/B6/B7/B8/B9/B10 的后端依赖链；保留 Express 4、Sequelize 6、node-cron 3 和 node-imap，增加经过兼容性验证的局部 overrides。后端全依赖及生产依赖审计均剩 `xlsx` 1 项高危，退出 1；B5 官方来源替换方案待具体确认。
- 更新依赖后开发自测：Jest 38 套件／266 项通过，3 套件／41 项默认跳过；另外隔离 PostgreSQL 的 2 套件／29 项通过，含生命周期 Migration 回退重放、事务回滚、付款并发和 150 单分配。新增 10 项依赖兼容用例在旧依赖下 5 失败／5 通过，新依赖下全部通过。未更新既有运行容器的依赖。
- 后端与前端 Lint、前端构建、文档检查通过；保留既有大包警告。只读检查另发现前端依赖审计 6 高危／3 中危，前端锁文件未修改，另列升级范围供确认。
- 尚未新增工作区交付提交、push、构建生产制品或部署；真实邮箱、Apple、代理、现有开发数据库及生产环境未操作。完整文件用途、版本、命令、失败分类及下一步见[安全修复与门禁复核报告](../archive/2026-09/2026-09-09-本地安全修复与发布门禁复核.md)。

### 2026-09-09 提交与生产发布检查受阻

- 用户已要求提交当前工作区、推送并构建部署。只读核对时本地为 `main@57859e7`，`git fetch origin main` 后为落后 0／领先 8 个提交；尚未创建本轮提交。
- 复跑开发容器检查：后端 Jest 37 套件／256 项通过，3 套件／41 项按环境门禁跳过；前后端 Lint、文档检查与 Vite 构建退出 0，保留前端大包警告。本轮未重跑隔离库或浏览器测试。
- 发布阻断：`npm audit --audit-level=high` 退出 1（12 高危、6 中危）；`npm audit --omit=dev --audit-level=high` 同样退出 1（9 高危、6 中危）。未执行自动依赖升级，审计严重性不代表已证明实际可利用。
- 推送阻断：Gitleaks 对 `origin/main..HEAD` 的 8 个提交扫描退出 1、命中 3 处。接口归档中的 Token 是带省略号的示例；另两处为旧归档代理凭据，未验证有效性，详见[敏感资产处置记录](../security/敏感资产处置记录.md#十2026-09-09-推送前安全检查)。不能只在最新文件脱敏后直接推送仍含副本的历史。
- 生产只读核对：`current` 仍指向 `20260906T092727Z-81340dbf509e`；API 健康、邮件 Worker 运行、爬虫 Worker 未运行，代理及自动刷新关闭；PostgreSQL 15 与 Nginx 服务运行。数据库已执行 13 项 Migration，候选代码有 8 项待执行。
- 停止边界：未 commit、push、构建发布制品、上传、备份恢复、生产 Migration、配置修改、服务重启或切换；未改业务代码与依赖，保留原工作区。本轮只新增本节及安全检查记录，待授权处置凭据副本与依赖风险后重新执行门禁，再安排备份恢复演练及生产切换。

### 2026-09-09 订单生命周期与官网权威数据

- 依据用户批准的[生命周期方案](../planning/真实订单生命周期与官网数据映射优化方案.md)实现：等待付款、已收款、已取货、付款过期显式状态；官网有效数据优先、导入来源快照与行首叹号冲突提示；准确官网付款截止优先；官网收款停止新付款分配，人工任务四态保持独立。
- 最新刷新决策：未付款／支付待确认的非终态订单每 60 秒自动刷新；已付／退款／终态停止自动刷新，仅手动刷新。页面打开与旧 page_open 不产生官网请求，已排队自动任务执行前复核资格，跳过不伪造成功时间。
- 身份不一致拒绝改号／跨单覆盖，标记安全异常并阻止新分配；多商品逐项保存，混合或未知阶段提示核对。金额、币种、截止后续缺失不清空；有效零数量采用官网值。未新增状态历史表、图片／取货说明或动作令牌字段。
- 开发自测：最终全量 Jest 37 套件／256 项通过，3 个环境门禁套件／41 项默认跳过；另外独立 PostgreSQL 的生命周期与付款权限 2 套件／29 项通过，含正式 Migration up → down → up、事务回滚、并发旧结果保护和 150 单分配。核心合并 Statements 98.30%／Branches 86.58%，序列化 Statements 100%。
- 后端 Lint 0 error／984 warning，前端 Lint 0 error／40 warning，Vite 构建通过（主包约 856.12 kB，保留大包警告）。独立 Chrome 合成 API 自测通过行首图标、悬停差异、键盘、390px 浮层、状态筛选、只读页面与详情，未产生自动官网刷新请求。窄屏既有分页挤压仍记录为范围外问题。
- 新 Migration 只在 apple_order_mgr_lifecycle_test_20260909 验证；开发库新迁移记录计数为 0，生产未访问。当前工作树需另行授权迁移后才能对开发库联调；不要自行启用 Worker。未 commit、push、测试部署或生产发布，未操作既有 Worker 服务、.env、凭据或真实订单。
- 复现命令、文件清单、失败与复测及验收边界见[开发自测报告](../archive/2026-09/2026-09-09-订单生命周期改造开发自测.md)。本次新订单最终取货、真实多商品／配送／退款、真实付款入口及生产验收均未验证。

本轮专用合成测试库已精确删除并复核不存在，可按报告重建；专用临时 Chrome 已退出，9338 端口无监听。未清理真实订单或既有 Docker 资源。

### 文档治理历史摘要

- 保留核心规范路径，建立 planning、testing、archive 分类；根 README 与文档导航明确分工。
- 合并前端 Agent 指南、仪表板和渠道重复说明；保留事故、测试与迁移历史。
- 更新数据库字段、API 端点、邮件/爬虫现状、导入会话、列配置和部署说明，旧示例退出当前指南。
- 提炼付款人与内部结算规划，不新增业务模型或替用户决定未确认规则。
- 同步 AGENTS、提示词和维护脚本，扩展文档链接、锚点、可达性、旧路径及命令检查。
- 移除根目录孤立实验、人工调试、历史解析副本和可再生成产物；Sequelize 种子路径统一指向 `seeds/`。

## 关键实现笔记

文档中的规范、已实现、未验证和规划状态分开表达。模型字段检查只反映收录情况；当前导入会话为进程内存、列排序刷新不恢复、独立 Worker 控制不持久化、迁移执行器依赖和敏感导出等边界均保留。

## 本轮验证

详细检查结果见[文档整理记录](文档整理记录.md)。历史单测、构建、审计数字属于下面的 2026-09-05 基线，不能当作本轮重新执行的结果。

## 2026-09-08 多 Provider 代理安全切换开发

- 目标：在已有快代理隧道 Pro、快代理私密代理运行时切换链路上接入网帆国内隧道，使三套凭据预配置后可由管理员三选一；切换只在批次边界和候选健康检查通过后生效，失败保留旧线路。
- 实现：新增 `fanproxy_tunnel` Provider，按网帆 `acc/cty/reg/sid` 账密规则使用有界粘性会话槽并关闭连接复用；补齐环境校验、Provider 工厂、管理 API／页面选项、数据库三值约束和通用 A/B 脚本。快代理专属 `441/517` 不再误套到网帆响应，通用 `407` 仍作为代理鉴权故障暂停领取。真实套餐诊断后增加 `FANPROXY_TUNNEL_SESSION_MODE` 和 `FANPROXY_TUNNEL_SESSION_POOL_SIZE`；订单抓取默认使用有界 `sticky_pool` 保持跳转出口一致，官方 `basic` 账号模式仅用于连通性诊断。只读诊断脚本支持显式选择单个 Provider，并优先保留 HTTP 4xx 状态分类。
- 安全与兼容：凭据继续只存在于 Worker 环境，不进入数据库、日志或前端响应；三种 Provider 预配置后切换不需要改代码或重启，新增或更换凭据仍需经过配置变更和 Worker 重建。A/B 脚本保留原有快代理双线路输出字段，并可选增加网帆组合统计。
- Migration：在精确命名、可丢弃的隔离 PostgreSQL 库完成全量迁移、新三值约束写入、`down` 恢复旧二值约束并拒绝网帆值、再次 `up`；隔离库随后删除。开发库和生产库均未执行本 Migration。
- 开发自测：三方 Provider、配置、错误分类、健康检查、切换控制器和 Worker 原子切换相关 9 个 Jest 套件／51 个测试通过；最终代码全量 Jest 35 个套件／199 个测试通过，2 个集成套件／31 个测试按环境门禁跳过。网帆 Provider Statements 85.71%、Branches 83.33%、Lines 89.13%；全仓覆盖率不作为本功能结论。全仓后端 Lint 为 0 error／910 warning，前端 Lint 为 0 error／40 warning；Vite 构建成功并保留 852.90 kB 主包警告，49 份文档链接、导航和 19 个模型字段覆盖检查通过。
- 真实链路验证：用户授权从已登录的网帆控制台读取有效 1 天测试套餐并安全写入本地忽略的 `.env`，未写入数据库、日志、文档或 Git 跟踪文件。基础账号模式 Apple 首页 `3/3`，但真实订单 `1/10` 且 27 次失败均为 541；最终使用 5 槽 `sticky_pool` 对同一 83 条订单执行只读测试，成功 `68/83`（81.93%），低于上一日快代理隧道 Pro `75/83`（90.36%）。网帆 156 次尝试含 541 50、424 20、631 8、超时 6、连接重置 4，最终失败 15；身份不匹配和零商品订单均为 0。该结果是跨时段对照，不是三线路同时段 A/B，也不证明生产规格。
- 2026-09-08 本地运行配置：开发库已执行 `20260908000002`；API 与 crawler Worker 在新增网帆凭据后重建，网帆配置采用 `sticky_pool`、5 个会话槽。调度继续持久化暂停，当前活动 Provider 保持 `kdl_private`，没有因首页健康检查通过而把低成功率网帆线路设为默认。未写订单、未恢复自动刷新、未执行生产 Migration、部署或发布。
- 本次增量开发自测：全量 Jest 35 个套件／200 个测试通过，2 个集成套件／31 个测试按环境门禁跳过；网帆 Provider Statements 89.7%、Branches 89.28%、Lines 92.18%。后端 Lint 为 0 error／912 warning，前端 Lint 为 0 error／40 warning，Vite 构建成功并保留 852.90 kB 主包警告；本次修改的 6 个 JS 文件通过 Prettier 检查，`git diff --check` 通过。全局文档检查因当前工作树另外两份未从总入口可达的生命周期文档失败，本任务未擅自修改其索引关系。
- 亦优 HTTP 接入：新增 `yiyou_http` 独立 Provider，官方 HTTPS 提取域名与 IPv4/端口/独立账密格式失败关闭；100 条、5 分钟短效节点按 240 秒安全 TTL 刷新，541 立即隔离当前 IP，连接重置达到阈值后隔离。补齐环境校验、Provider 工厂、四值数据库约束、管理 API/页面和只读诊断脚本。响应流中断不拼接残缺 HTML，重试使用下一代理从头完整抓取，已补回归用例。
- 亦优真实链路：管理页候选初始化、Apple 首页健康检查和批次边界原子切换成功，活动 Provider 已确认为 `yiyou_http`。同一 83 条真实订单只读测试成功 `82/83`（98.80%），首次／第二次／第三次成功 `74/7/1`；94 次尝试的错误为 HTTP 541 6 次、`ECONNRESET` 6 次，唯一最终失败为 541。身份不匹配与零商品订单均为 0，总耗时 1452.1 秒，平均约 17.5 秒/单。成功率高于快代理隧道 Pro `75/83` 和网帆 `68/83` 跨时段基线，但尚未进行同时段并行 A/B、长时稳定性或生产规格验收。
- 亦优本地状态：提取 URL 仅写入 Git 忽略且权限为 600 的 `.env`；开发库已执行 `20260908000003`，API 和 crawler Worker 已重建以读取新配置。自动刷新仍持久化暂停，真实测试未写订单或 HTML；未执行生产 Migration、部署、发布、Git 提交或推送。
- 亦优首选策略：基于同订单集 `82/83`（98.80%）的当前最高成功率，建立 `DEFAULT_PROXY_PROVIDER=yiyou_http` 为唯一代码默认，`.env.example` 和本地 `.env` 同步为 `PROXY_PROVIDER=yiyou_http`，管理页将亦优置于首位并标记“首选”。该调整仅改变无显式值时的启动选择和展示顺序，不开启失败后自动跨供应商降级；候选健康检查失败仍保留原活动 Provider。
- 亦优开发自测：在精确命名的可丢弃 PostgreSQL 库完成全量 `up` → 写入四值约束 → 最新 Migration `down` 清空亦优值并拒绝重写 → `up`，随后删除隔离库。相关 9 个 Jest 套件／78 个测试通过；全量 Jest 36 个套件／210 个测试通过，2 个集成套件／31 个测试按环境门禁跳过。亦优 Provider Statements 78.65%、Lines 83.54%；后端 Lint 0 error／928 warning，前端 Lint 0 error／40 warning，Vite 构建成功并保留 852.95 kB 主包警告。模型字段文档检查通过；全局文档链接检查仍只因另一任务的两份生命周期文档未从总入口可达而失败。
- 亦优首选增量验证：默认值、Provider 顺序、管理 API 和 Worker 切换相关 4 个 Jest 套件／32 个测试通过；最终代码全量 Jest 36 个套件／211 个测试通过，2 个集成套件／31 个测试按环境门禁跳过。后端 Lint 0 error／928 warning，前端 Lint 0 error／40 warning，Vite 构建成功并保留 852.96 kB 主包警告；模型字段文档检查和 `git diff --check` 通过。API 与 crawler Worker 已在本地重建并确认启动默认、数据库请求值和活动值均为 `yiyou_http`；自动刷新保持持久化暂停，没有执行订单写入。全局文档链接检查仍仅被另一任务两份未从总入口可达的生命周期文档阻断。

## 2026-09-08 逐用户权限与订单付款任务首版开发

- 契约：逐用户显式授权为唯一普通账号权限来源，管理员固定全权；可登录用户均为内部员工，外部付款人不会登录，员工按订单手工填写一个当前 `payer_name` 并保留审计，不建立付款人候选主数据；当前负责人具备专用权限后直接复制任务关联 `orderUrl`，不按核实、截止或业务状态限制；付款窗口固定为官网订单创建时间起 30 分钟，官网只返回日期时保持未知，不用本地入库、邮件接收或当天 00:00 代替。
- 权限：新增版本化权限目录、授权／审计表、完整集合替换、依赖与管理员保留项校验、存量 `operator/readOnly` 权限映射和即时数据库权限加载。全部业务 Router 按模块动作门禁，导入预览先按 type 鉴权且执行阶段复核服务端会话类型；本地敏感字段仍要求管理员身份、显式敏感权限和环境开关。
- 前端：用户管理增加分组权限配置和“仅本人付款任务”快捷组合；登录恢复、403 后刷新、导航、页面路由和主要动作按钮改用服务端 `permissions`。仅付款员工账号首页为“付款任务”，不因旧 operator 标签获得全局订单、渠道、统计或邮件权限。
- 付款任务：新增任务、任务／付款人姓名／调度审计、全局和逐人员设置；订单入库事务内幂等登记，API 内 10 秒补偿扫描，多实例使用 PostgreSQL advisory lock。支持自动负载比分配、容量、原子批量手动分配／转派、四态、加密备注、任意状态付款人姓名登记、组合筛选、服务端时钟倒计时、本人订单链接复制以及单项／批量刷新。
- 并发修复：撤权和分配统一使用“调度锁→用户／权限→任务”顺序，消除反向行锁导致的死锁；撤销任一付款执行权限在同事务关闭自动接单，已提交撤权后的新分配失败，存量负责人不自动改变。
- 开发自测：隔离库 Migration 曾执行全量 up、最后一项 down→up 和存量映射校验；150 张合成任务按两人 75/75 分配且未超容量。2026-09-08 用户重新确认付款人为自由文本后，旧付款人主数据相关证据作废，需以本节后续最新复测记录为准。
- 边界：仅完成开发自测，不等同于独立测试或人工业务验收。浏览器权限撤销后的敏感状态清理、300 单余量、多 API 副本、真实付款入口／官网创建时间同步、真实 30 分钟付款结果及生产 Migration 均未执行；事件已持久化但历史合并页面、待纳入／忽略页面和站内提醒仍为后续增强。
- 2026-09-08 自由文本修订：付款人从候选下拉改为最长 100 字的手工输入，移除付款人专用按钮并并入行保存；四态使用黄／蓝／绿／红区分。服务端删除 `payers` 主数据、`orders.payer_id` 和三项 `payers.*` 权限，仅保留 `orders.payer_name`、独立版本及加密变更审计；可登录用户明确为内部员工，外部付款人不登录。
- 2026-09-08 行操作修订：复制按钮在本人任务权限与当前归属通过后直接复制 `orderUrl`，不再受核实、时限、人工状态或官网状态限制；状态／备注／付款人改为只提交实际变化并在同一事务原子保存，页面提供保存中／成功／失败反馈。刷新按钮新增提交中禁用、排队／运行／终态轮询及结果反馈，任务终态后重新加载列表，本人刷新进度接口按当前归属和关联订单隔离；新增“最后更新时间”列，取任务或关联订单较新时间并按 `YYYY/MM/DD HH:mm:ss` 显示。
- 2026-09-08 导航命名修订：侧边栏和页面标题统一使用“付款任务”，导航项移至“付款调度”之后；权限码和 `/payment-tasks` 路由保持不变，没有付款调度权限的员工仍可单独看到付款任务入口。前端 Lint 0 error／40 个既有 warning，Vite 构建成功，文档检查和 `git diff --check` 通过；本地浏览器已确认管理员侧边栏顺序和页面标题。
- 本次增量开发自测：精确命名隔离库全量 Migration 后，17 项权限与付款任务集成场景全部通过，覆盖 150 单集中分配、无业务状态门禁复制链接、行级原子提交／失败回滚、只改付款人不增加任务版本、幂等重放和刷新 job 归属隔离；隔离库已删除。全量 Jest 33 个套件／184 个测试通过，2 个套件／29 个测试按环境门禁跳过；后端 Lint 0 error／880 warning、前端 Lint 0 error／40 warning，Vite 构建通过并保留大包警告。浏览器已确认新页面可加载，但当前 admin 没有本人任务，未在浏览器执行员工行操作；爬虫 Worker 未启动，未访问 Apple 官网。
- 最新开发自测：本地开发库对修订后的 Migration 执行 `down → up` 均成功并恢复 `local-mock-v3` 数据；库中不存在 `payers` 表，53 个订单中 22 个有合成付款人姓名，30 个任务为待处理 15／处理中 7／已完成 4／异常 4。另建精确命名的本地隔离库执行全量 Migration 和 13 项真实数据库集成场景，150 单集中分配、撤权／分配并发、转派范围、四态登记、自由文本付款人幂等／审计／乐观锁均通过；付款四个核心服务 Statements 80.19%、Functions 92.85%、Lines 83.05%，验证后隔离库已删除。Mock 员工真实登录、读取 7 条本人未完成任务和自由文本付款人写入均返回 HTTP 200，写后重新造数恢复预设值；浏览器确认付款人文本框、本行保存、付款人专用按钮移除和四态差异化颜色。全量 Jest 33 个套件／185 个测试通过，2 个套件／25 个测试按环境门禁跳过；后端 Lint 0 error／876 warning，前端 Lint 0 error／40 warning，Vite 构建成功并保留 842.29 kB 大包警告；文档检查 49/49 可达、19 个模型字段覆盖，`git diff --check` 通过。
- 2026-09-08 付款调度修订：新增 `orders.official_order_created_at` 正式 Migration；爬虫只在官网提供时分时保存创建时间，并在同一事务把任务截止同步为创建时间加 30 分钟。付款调度删除逐行截止核实，新增官网状态、付款倒计时、最后官网更新时间、订单号／商品／负责人／官网状态／处理状态组合筛选、逐行刷新、勾选批量刷新，以及“勾选任务→打开弹窗→选择负责人→确认”的原子批量分配／转派。
- 2026-09-08 付款调度 UI 优化：仅调整 `PaymentDispatch.jsx`，将配置型复选框改为开关，统一按钮图标与单行文案、禁用／保存中状态、筛选区对齐、处理状态徽章、表格 Hover 和操作列对齐，并将单字段批量分配弹窗收窄至 448px；接口、权限、数据结构及业务流程未变。前端 Lint 0 error／40 个既有 warning，Vite 构建和 `git diff --check` 通过；本地浏览器已复核桌面布局及人员设置未保存状态，未执行真实保存、扫描、刷新或分配。
- 2026-09-08 付款方式展示：付款任务共享 DTO 增加关联订单已有的 `paymentMethod`，本人“付款任务”和管理员“付款调度”表格均新增只读“付款方式”列，空值显示 `-`；未新增数据库字段、付款方式选择或任务处理规则。全量 Jest 34 个套件／187 个测试通过，2 个套件／31 个测试按环境门禁跳过；定向时间／序列化测试 2/2 通过，后端 Lint 0 error／905 个既有 warning，前端 Lint 0 error／40 个既有 warning，Vite 构建和受影响代码 Prettier 检查通过，文档检查 49/49 可达、19 个模型字段覆盖。浏览器已确认两页显示支付宝、银行卡和微信支付；未执行 Migration、数据写入、官网请求或付款操作。
- 付款调度开发自测：新字段在精确命名隔离库完成全量 `up → down → up`，19 项权限和付款集成场景通过，覆盖 150 单 75/75 分配、批量分配成功与失败全回滚、转派范围、撤权并发、四态付款人登记、负责人及其他组合筛选和多项批量刷新；隔离库复测后已删除。时间解析和任务倒计时等 3 个定向套件／33 个测试通过；全量 Jest 34 个套件／187 个测试通过，2 个套件／31 个测试按环境门禁跳过。后端 Lint 0 error／905 warning，前端 Lint 0 error／40 warning，Vite 构建成功并保留 850.49 kB 大包警告；文档检查 49/49 可达、19 个模型字段覆盖。浏览器已确认筛选、按秒倒计时、勾选、批量分配弹窗和批量刷新成功反馈；2026-09-08 本地开发库定向删除并重建 48 条 `local-mock-v3` 合成订单及 30 条付款任务，重建后 26 条仍在倒计时、4 条为脚本预设的刚超时边界样本，5 条非 Mock 订单保持不变。本地 crawler-worker 未启动且未访问 Apple 官网。
- 仓库与环境：上述付款任务与调度改动已纳入本地 `main` 提交，未 push、测试部署或生产发布；本地开发库只改动本功能 Migration、权限和合成体验数据，未修改 `admin` 密码，未访问生产库。未启动、停止或重启邮件／爬虫 Worker；检查时 email-worker 已由此前环境处于运行状态。

## 2026-09-08 本地付款任务体验数据

- 完成：扩展受保护的本地 Mock 脚本，新增 3 个 `mock_pay_*` 工作人员和 30 个付款任务，并为部分订单填写合成的外部付款人姓名；任务覆盖待处理、处理中、已完成、异常四态，包含 24 个已分配和 6 个待分配场景，并继续保留 8 个 Apple ID、20 个取机人和 48 个订单。
- 权限与安全：两个工作人员具备完整的五项本人付款权限，一个工作人员仅有 `payment_tasks.read_own`；临时密码通过 `LOCAL_MOCK_USER_PASSWORD` 注入且不写入代码、文档或日志。脚本只允许显式确认的本地 development 数据库，可重复执行且只重置合成工作人员，不修改 `admin` 或其他账号。
- 开发实测：自由文本修订后 Mock 用户／订单／任务数量保持 3／48／30；数据库不存在付款人主数据表，22 个订单带合成付款人姓名。完整权限账号真实登录、读取本人任务和写入付款人姓名均返回 200，页面已完成浏览器开发自测。仅查看账号跨业务 403 证据来自上一轮且本轮未重复执行，不等同于独立测试或人工验收。
- 边界：这是本地合成数据和开发自测，不是真实付款、Apple 官网、邮件、独立测试或生产验收；未访问生产环境，未执行 commit、push、测试部署或生产发布。

## 2026-09-07 付款任务与逐用户权限规划

- 当前阶段：已按第二轮反馈修订[付款任务方案](../planning/订单付款任务分配与处理方案.md)，新增[用户权限分配方案](../planning/用户权限分配与访问控制方案.md)，待用户复核其余细则后安排开发。
- 用户已确认：复用 users、不新增业务角色、管理员逐用户配置权限；保留待处理／处理中／已完成／异常四态；不选择本人支付／外部分发；四态均可登记付款人，第一版每单一名当前付款人，允许修正并留历史；支持商品筛选、订单号搜索、每行剩余付款时间。
- 方案产出：现有模块权限目录与逐用户授权、旧接口和前端边界、撤权／迁移／回退；订单付款人稳定关联和任意状态补录；商品同项匹配及服务端分页；自动分配、容量、临期和转派。开发拆分 ACL-01 至 ACL-05、PD-01 至 PD-08，附 26 项任务与 13 项权限用例。
- 验证：已静态核对权限常量、全部挂载路由、关键控制器、前端认证／导航、订单与商品字段；本地 API 容器 `npm run docs:check` 通过，49 份文档全部可达，链接、锚点、命令及 11 个模型字段覆盖检查通过；本轮目标文档 `git diff --check` 通过。权限、关联和任务功能均未实现，未执行真实付款或 30 分钟业务验收。
- 下一步：复核权限目录／存量映射、实际付款入口及截止时间；付款人自由文本、四态和每单单人已确认，不重复列为阻塞。复核后先更新权威契约再开发。
- 本轮范围：仅修改规划及相关文档导航、进度与计划；保留其他未提交工作，未修改业务代码、实际用户权限、数据库或服务状态，未 commit、push 或发布。

## 2026-09-07 邮件解析与处理可靠性开发

- 已确认规则：邮件处理页面/API 仅 admin；管理员可查看和修正全部字段，包括 Apple 密码、完整身份证号和系统内部状态；内容保留 180 天；混合邮箱的非订单邮件仅留最小状态审计；Authentication-Results/DKIM/SPF 只记录；`IMAP_ALLOWED_SENDERS` 可选且为空时 development/production 均接受任意 From；临时错误每分钟重试一次、最多自动重试 3 次。
- 解析与安全：MIME 只解析一次，保留 HTML/文本行边界；商品全有或全无，修复 Apple ID `@` 误切分、标签吞正文和中国时间偏差；Apple URL 限制中国官网 `vieworder` 并校验订单号一致。删除返回 0 的历史 `console.assert` 脚本，日志只保留内部 ID、错误码和数量。
- 状态与幂等：新增 `email_logs` 正式 Migration 和 `email_worker_states`；解析前加密保存 MIME，以邮箱身份哈希、UIDVALIDITY、UID 为主幂等键，Message-ID/MIME SHA-256 辅助去重。持久化失败分类、重试/确认队列、尝试历史、管理员操作审计、5 分钟中断恢复及 180 天内容清理。
- Worker：启动回查 24 小时，每 10 分钟补查 30 分钟，不只依赖 UNSEEN；`IMAP_MARK_SEEN=false` 不再触发递归处理循环；终态提交后才确认已读，失败每分钟重试；停止时等待在途处理。独立心跳提供连接、最后收信/成功、积压和连续失败指标。
- 事务与并发：统一订单服务在 PostgreSQL advisory lock 下串行处理同订单；订单、邮件终态和首次刷新任务同一事务提交。多 Worker 使用 `FOR UPDATE SKIP LOCKED` 领取重试，重复邮件保留为 `superseded` 且不覆盖订单。
- API 与前端：新增 admin 专属 `/api/email-processing` 列表、完整详情、指标、重解析预览、加密草稿、确认入库、逐条批量重解析及人工忽略/关联已有订单；前端增加邮件处理列表、筛选、Worker 指标、详情抽屉、商品表格、重复订单提示、“查看订单”和“处理下一封”。所有完整查看和成功写操作记录加密审计；Apple ID 使用关闭自动填充的文本输入并保留邮箱输入模式，避免浏览器凭据填充干扰受控字段，服务端邮箱校验保持不变。
- 开发自测：严格解析/来源/权限/人工数据/API 契约等 7 个相关 Jest 套件 38 个测试通过；隔离 PostgreSQL 12 个集成场景通过，邮件状态机 Statements 覆盖率 91.53%。最终 Migration 完成 `up → 字段/索引/表校验 → down → 消失校验 → up`；同 UID、UIDVALIDITY 变化、Message-ID/MIME 重复、多 Worker 单次领取、中断恢复、同订单并发和事务回滚通过。空白名单策略的配置与过滤定向测试为 2 个套件、10 个测试通过；后端全量 Jest 为 28 个套件、154 个测试通过，另有 1 个隔离集成套件/12 个测试按环境门禁跳过；后端全仓 Lint 0 error/745 warning，前端 Lint 0 error/52 warning，Vite 构建成功并保留 815.45 kB 主包警告。文档检查通过 47 份文档可达和 11 个模型字段覆盖，`git diff --check` 通过；精确命名的隔离测试库在复测后删除。
- 本地联调：当前开发库已执行 `20260907000001-create-order-refresh-queue`、`20260907000002-enhance-email-processing` 和工作区已有的 `20260907000003-add-proxy-provider-switch-state`；其中第三项由本次启动邮件 Worker 时的 Compose Migration 依赖自动执行，不属于邮件测试本身。管理员浏览器使用唯一合成 MIME 完成重新解析、Apple 密码/完整身份证号/联系方式/地址/内部状态修正、加密草稿保存、确认入库、成功列表及订单详情检查；数据库核对订单、邮件终态和各 1 条首次刷新计划/任务同事务存在。验收后精确删除 1 条合成邮件和 1 条合成订单，级联刷新计划/任务均为 0，页面恢复空状态。
- 真实邮箱联调：本地正式 `email-worker` 在 `IMAP_ALLOWED_SENDERS` 为空、`IMAP_MARK_SEEN=true`、自动订单刷新关闭的条件下接收 5 封由 QQ 邮箱转发到配置 163 邮箱的历史订单样本，并在同一批次完成并发处理。5 条邮件终态和 IMAP 确认均为 `succeeded`、重试次数均为 0；从 48 条订单基线新增 5 条订单，共解析 6 个商品，其中 1 封为多商品。5 条记录的 Message-ID、MIME SHA-256 和关联订单均互异，订单号、链接、时间与付款方式完整，订单号均能在订单链接中匹配。受控重启后启动回查再次发现同一批 5 封邮件，但 `email_logs` 仍为 5、订单仍为 53，未重复建单；Worker 心跳显示已连接、连续失败 0。测试数据未匹配本地 Apple ID 和取机人主数据，因此保留邮件快照字段并产生预期告警；事务同时创建 5 条首次刷新任务，但爬虫 Worker 未启动且 `crawl_logs` 为 0。
- 验证边界：已验证真实 163 邮箱收信、转发 MIME 解析、5 封并发处理、单/多商品解析、Apple 订单号与链接一致性、订单事务入库、终态已读确认、Worker 心跳、受控重启回查和幂等；结论属于开发实机联调，不等同于独立测试或生产验收。未启动爬虫 Worker，未访问 Apple 官网、代理或生产；生产库未执行本 Migration。未 commit、push、测试部署、生产部署、重启生产服务或修改生产数据。

## 2026-09-07 订单状态刷新与爬虫重构开发

- 已确认需求：新订单继续由邮件提供订单号和链接；未付款或支付状态未知的订单每分钟刷新；已付款订单停止定时刷新，只在打开订单页面或人工操作时刷新；人工操作支持单个和全部订单。
- 完成实现：新增逐订单调度、刷新任务、全量批次和 Worker 单例状态四张表及 Migration；独立 Worker 使用 PostgreSQL 租约与 `FOR UPDATE SKIP LOCKED` 领取，支持任务合并、优先级提升、过期租约恢复、持久化暂停和全局请求时隙。
- 接口与前端：单笔、指定批量、刷新全部和页面打开均改为异步提交；新增任务/批次查询。订单列表和详情返回新鲜度与活动任务，前端展示待付款筛选、排队/刷新/最新/过期/失败及全量批次进度，不把“任务提交”显示为“官网已更新”。
- 代理与兼容：新增 `kdl_tunnel`/`kdl_private` Provider。Apple 订单入口存在跨域重定向，快代理默认“每 HTTP 请求换出口”会让同一次抓取的首跳和重定向使用不同 IP；隧道模式现为每次尝试生成独立 `sid`，用 `period-0.5` 固定出口 30 秒，下一次重试再换出口，并使用兼容性优先的 `type-std`、质量优先的 `pool-q10`、`Connection: close` 和 `Accept-Encoding: gzip`。自定义 `HttpsProxyAgent` 对照实验没有提高成功率，继续使用 Axios 原生代理配置。新增 517 建链错误分类，541 断路改为仅在连续订单均耗尽三次 541 后累计，单订单重试成功不会暂停全局 Worker。环境示例只保留占位符，试用密码未写入仓库或日志。
- 安全一键切换：管理员系统页面可提交 `kdl_tunnel`/`kdl_private` 切换意图，API 只持久化 Provider 枚举和操作者；Worker 在批次边界串行处理切换，先初始化候选 Provider，再通过全局限流访问 Apple 中国首页健康检查，成功后原子替换，失败保留旧 Provider 并记录脱敏错误。切换状态、活动 Provider 和两侧“是否已配置”可查询，用户名、密码和私密提取 URL 不进入数据库或响应；未知 Provider 失败关闭。
- 解析兼容：订单状态以首个订单项的 `currentStatus` 为第一权威来源，兼容真实的 `orderItem-1of2-*` 键；已验证 `PICKED_UP → completed`、`PAYMENT_EXPIRED_STORED_ORDER → cancelled + unpaid`，未知枚举才回退到清理后的可见文本。支付状态不再扫描隐藏脚本、隐藏节点和页脚中的退款说明。官网没有总计标签和金额时记为“未提供”，只有明确出现总计标签但无法解析才记录解析错误。
- 开发自测：后端全量 31 个套件、173 个测试通过，另有 1 个集成套件/12 个测试按环境门禁跳过；新增切换状态机、候选失败回滚、并发切换串行化、Provider 配置脱敏、私密 IP 冷却和健康检查回归。代理子模块语句覆盖率 65.28%，其中隧道 Provider 83.33%、健康检查 69.56%、切换管理器 67.92%。后端全仓 Lint 0 error/789 warning；前端 Lint 0 error/52 warning；Vite 构建成功，主包 818.34 kB/gzip 234.58 kB 并保留大包警告；文档结构检查 48/48 可达，11 个模型字段覆盖。新 Migration 已在独立可丢弃库执行全量 up、7 个字段/3 个约束检查、非法值拒绝、down 和再次 up；当前开发库未执行。
- 真实订单只读验证：83 条链接格式均有效。初始“每 HTTP 请求换出口”策略按 1 RPS、单订单最多 3 次成功 49/83、失败 34/83；失败是抓取失败，不是 HTML 解析失败，主要为 HTTP 541，另有 HTTP 200 后响应流中断、连接重置、超时和一次 517。改为 30 秒固定出口且每次重试换 `sid` 后，`type-std + pool-q10` 的 83 条复测首次/第二次/第三次成功为 44/18/9，共成功 71/83、失败 12/83，身份不匹配 0；失败尝试包含 52 次 HTTP 541、9 次 HTTP 200 响应流中断、6 次连接重置、4 次超时和 1 次 HTTP 517。增强池的 34 条对照仅恢复 24 条，低于标准池；自定义 Agent 的 4 条样本对照也无收益，因此没有采用。真实稳定性验收仍未通过。
- 真实页面结构与最终 A/B：对真实已取货页的可见 HTML 标签和内嵌订单 JSON 分别分析，确认 `currentStatus=PICKED_UP`、“已收到付款/已取货”可见，而“退款”仅来自隐藏内容；页面确实可能不提供金额。另一个真实样本出现 `PAYMENT_EXPIRED_STORED_ORDER`。最终解析版对 83 条同订单集执行 9 个不重叠偏移批次的只读 A/B，统一使用隔离 PostgreSQL 全局 1 RPS、每订单每 Provider 最多 3 次：隧道 Pro 成功 75/83（90.36%），私密代理成功 55/83（66.27%）；配对双方成功 48、仅隧道 27、仅私密 7、双方失败 1，身份不匹配和零商品订单均为 0。隧道成功样本为 `completed/paid` 58、`cancelled/unpaid` 17，75 条均未提供金额；私密的 28 个最终失败中 23 个为响应流中断。当前私密试用线路不应替代隧道默认线路，两者真实稳定性验收均未通过。
- 边界：真实链接只通过禁用业务日志的临时只读验证进程访问，未保存 HTML、JSON、完整链接或个人信息；A/B 脚本只输出聚合结果并保留为可复现诊断工具。未启动邮件或爬虫 Worker，未修改当前开发库或生产库，未执行真实订单写库、生产 Migration、部署、重启、commit 或 push。Parse、Validate、Persist 的物理拆分、脱敏 HTML Fixture、真实 API/前端/Worker 切换验收、真实退货枚举及一分钟时效仍待后续验证。

## 2026-09-07 取机人生成操作二次确认

- 需求：生成联系方式或地址时，如果任一选中取机人已有对应数据，在首次生成确认后增加一次覆盖确认，防止误覆盖。
- 完成：页面按当前选中记录统计已有电话/邮箱和已有地址数量；联系方式首次确认、地址地区选择确认后，仅在存在已有数据时显示红色二次确认弹窗。取消联系方式二次确认不调用 API；取消地址二次确认同时清理暂存的省、市、区参数。
- 兼容：没有已有数据时保持单次确认后直接生成；生成 API 请求与响应契约、数据库结构及生产脱敏边界均未改变。
- 验证：本地浏览器分别验证了联系方式和地址的二次覆盖确认：已有数据时弹窗正常出现；取消后原数据保持不变且未出现生成成功提示；再次操作并确认覆盖后，新联系方式或新地址正常写入。后端 Jest `18` 个测试套件、`76` 个测试全部通过；后端 ESLint `0` error、`623` warning，前端 ESLint `0` error、`56` warning；文档检查通过（`45` 份文档及 `6` 份模型文档覆盖检查）；Vite 构建成功，保留既有大于 `500 kB` chunk 警告。浏览器开发自测对本地一条合成取机人记录分别执行了 `1` 次联系方式覆盖和 `1` 次地址覆盖；未访问生产环境。
- 未涉及：未执行 Migration、生产访问/写入、commit、push、测试部署或生产发布。

## 2026-09-07 取机人联系方式生成与详细地址展示

- 现象与根因：批量生成联系方式只填补空电话或空邮箱；本地 Mock 记录已有两项数据，因此点击后接口成功但返回 `updated=0`，页面没有可见变化。前端确认文案还把号码写成固定的 `138xxxxxxxx`。详细地址由 API 固定返回 `street_address=null`，前端只能显示省市区和隐藏提示。
- 修复：联系方式生成改为对所有选中记录重新生成并覆盖电话与对应邮箱；电话号码按 `^1[3-9]\\d{9}$` 生成，不固定 138 号段，邮箱继续使用“电话@8lvv.com”。本地 development + admin 明文门禁同时覆盖 `street_address`，取机人页面拼接省、市、区和详细地址完整展示。
- 兼容与安全：批量接口仍使用 `recipient_ids`，响应字段保持 `total/found/updated/skipped`；确认弹窗明确提示会覆盖已有联系方式。production、非 admin 或未开启本地开关时详细地址仍隐藏。
- 验证：修复前真实本地 API 对已有联系方式记录返回 `found=1、updated=0、skipped=1`；修复后 API 返回 `found=1、updated=1、skipped=0`，详情断言新号码格式、邮箱对应关系及详细地址通过。Chrome 页面勾选一条记录后完成确认弹窗、成功提示、号码变化、邮箱联动和完整地址显示验证。后端 18 个 Jest 套件、76 个测试通过，覆盖率 Statements 40.82%、Branches 34.72%、Functions 39.71%、Lines 40.9%；后端 Lint 0 error、623 warning，前端 Lint 0 error、56 warning；前端构建通过并保留 792.66 kB 主包警告；文档检查和 `git diff --check` 通过。
- 未涉及：未修改数据库结构或 Migration，未访问生产环境，未执行 commit、push、测试部署或生产发布。

## 2026-09-07 本地管理员敏感字段展示

- 现象与根因：本地数据库中的 Apple ID 密码和取机人身份证号为可解密密文，手机号保存完整值；页面看到的星号来自 API 列表/详情序列化脱敏，不是前端再次处理或后台 Mock 数据本身缺失。
- 修复：新增本地敏感字段展示三重门禁，仅在 development、显式开启 `ALLOW_LOCAL_SENSITIVE_DISPLAY=true` 且当前用户为 admin 时，Apple ID 返回密码、取机人返回完整身份证号与手机号、订单返回完整手机号。前端继续直接展示 API 值，无需复制解密或脱敏逻辑。
- 安全边界：operator、readOnly、未开启配置及 production 继续保持原脱敏契约；地址、订单身份证号、Apple 订单链接等不在本次范围，生产 Compose 未修改。
- 验证：本地 admin 真实登录后，三个列表 API 分别断言密码、身份证号和手机号为完整格式且不含 `*`；Chrome 刷新订单、Apple ID、取机人页面后完成相同的页面级断言。后端 17 个 Jest 套件、74 个测试通过，覆盖率 Statements 40.57%、Branches 34.75%、Functions 39.28%、Lines 40.65%；后端 Lint 0 error、625 warning，前端 Lint 0 error、56 warning；前端构建通过并保留 792.54 kB 主包警告；文档检查、指定新增文件与文档格式检查、`git diff --check` 通过。
- 未涉及：未修改数据库数据或结构，未执行 Migration、生产访问/写入、commit、push、测试部署或生产发布。

## 2026-09-07 本地验收数据与可用取机人指标

- 问题：本地开发库只有管理员账号，仪表板无订单、Apple ID 和取机人数据；原“活跃收件人”实际按当前订单筛选中的 `recipient_ref` 去重，与需要展示的可用取机人口径不一致。
- 完成：仪表板字段改为 `availableRecipients`，直接统计状态为“使用中”或“未使用”的取机人，并显示“可用取机人 / 可用”；新增 `db:seed:local-mock`，仅允许显式确认的本地 development 数据库执行，使用事务生成可重复的合成关联数据并关闭 Mock 订单自动刷新。
- 本地数据写入：生成 8 个 Apple ID、20 个取机人和 48 个订单；可用取机人 16 个，订单关联缺失 0 条，自动刷新启用 0 条；二次执行后数量仍为 8/20/48，验证幂等。未读取或复制生产数据。
- API 与浏览器验证：2026-09-01 至 2026-09-07 的仪表板接口返回 28 个订单、待取 12 个、金额 267960 元、可用取机人 16 个，趋势 7 点、型号 6 组、门店 4 组；本地浏览器显示“可用取机人 16 / 可用”及非空图表。
- 开发自测：后端 16 个 Jest 套件、69 个测试通过，覆盖率 Statements 40.51%、Branches 34.6%、Functions 39.06%、Lines 40.59%；后端 Lint 0 error、625 warning，前端 Lint 0 error、56 warning；前端构建通过并保留 792.54 kB 主包警告；文档检查与 `git diff --check` 通过。
- 边界：本次只写入本地合成数据，未访问生产数据库、邮箱、Apple 官网或代理，未执行 Migration、commit、push、部署或生产操作；生产与真实业务数据验收未执行。

## 2026-09-06 密码最小长度调整

- 问题：登录、改密、用户创建和管理员种子的密码长度校验分别写死为 6 位或 12 位，前后端提示与本次确认的 8 位规则不一致。
- 完成：后端集中使用 `MIN_PASSWORD_LENGTH=8`，同步登录、改密、创建用户与管理员种子；前端登录、改密和新增用户统一使用 8 位校验、输入约束及提示；API 契约和本地开发指南同步更新。
- 回归：新增 7 位拒绝、8 位接受的服务层、接口层和管理员种子测试。后端 15 个 Jest 套件、65 个测试通过，覆盖率 Statements 39.99%、Branches 33.95%、Functions 38.05%、Lines 40.09%。
- 质量检查：后端 Lint 0 error、625 warning；前端 Lint 0 error、56 warning；前端生产构建通过，保留 792.54 kB 主包警告；文档检查与 `git diff --check` 通过。
- 边界：未修改已有用户密码，未执行数据库 Migration、真实数据写入、commit、push、部署或生产操作；浏览器人工验收仍由用户在本地开发环境执行。

## 2026-09-06 Agent 初始化提示词优化

- 问题：原 Coding Agent 初始化 Prompt 混入具体任务、目标、版本、环境和授权占位符，本地验证命令也未完全跟随最新 Docker Compose 开发方式。
- 完成：将所有角色初始化 Prompt 与任务交接 Prompt 分离；重点重写 Coding Agent 初始化，补齐固定与按模块必读文档、编码和数据规则、文档级联、本地开发、生产发布、自测、授权、停止条件及交付证据。
- 关键边界：初始化只建立长期角色和规范，不自行选择任务；具体目标、范围、验收标准、环境和授权只通过后续任务交接提供。开发自测不代替独立测试、人工验收或生产验收。
- 验证：初始化模板占位符隔离检查通过；模板文件 Prettier 检查通过；容器内 `npm run docs:check` 通过，45 份文档链接、锚点、导航、旧路径、命令及 6 个模型字段覆盖检查通过；`git diff --check` 通过。
- 未涉及：未修改业务代码、数据库、环境配置或线上状态，未执行 commit、push、构建、部署、Migration、服务操作或真实数据写入。

## 2026-09-06 文档维护规则补充

- 问题：原规则缺少新增文档具体归属、功能模块对应文档和 Bug 修复的分类指引。
- 完成：在[文档管理规范](文档管理规范.md)补充目录放置、功能对照、普通修复/行为变化/重大故障及完成前检查；AGENTS 增加强制速查，两级 README 提供维护入口。
- 原则：恢复既有正确行为的修复不改写正确规范；普通 Bug 不逐个新建报告，重要历史证据集中归档。未闭环问题继续记录，避免以归档代替解决。
- 验证：本次仅修改五份 Markdown；45 份文档的链接、锚点及导航检查通过，指定文件的 Prettier 和 git diff --check 通过；未执行业务测试或修改业务代码。

## 2026-09-06 根目录清理

- 删除无正式入口的 `price_scraper.js`、`price_data.json`、`start-frontend.sh` 和两份根目录收件人调试脚本。
- 删除已由 `src/services/`、`src/workers/` 取代的根目录邮件、爬虫历史实现，以及依赖临时网页样本的人工解析脚本。
- 清理 `.DS_Store`、coverage、logs、test-artifacts、frontend/dist、空 backend 和 tmp 等忽略项；保留本地依赖目录和 `.env`，避免中断当前开发环境。
- `.sequelizerc` 直接使用 `seeds/`，移除仅用于兼容路径的 `seeders` 符号链接；模板和上传目录因正式接口依赖继续保留。
- 同步系统架构、邮件、爬虫和编码规范中的实现路径，避免继续引用已删除的历史文件。
- 验证：13 个 Jest 套件、56 个测试通过；文档链接、导航和六个模型字段检查通过；ESLint 为 0 error、625 warning；Sequelize 配置中的四个目录均可解析。本轮未连接或修改数据库。

## 2026-09-06 本地 Docker Compose 改造

- 新增独立的 `docker-compose.dev.yml`、前后端开发镜像和专用 ignore 文件；默认编排 PostgreSQL、Migration、Express API 与 Vite 前端，邮件和爬虫 Worker 仅通过 profile 显式启动。
- API 与 Worker 共用后端镜像；源码目录按模块 bind mount 以支持热更新，依赖封装在镜像中，数据库、日志、上传、覆盖率和前端构建产物使用 Docker volume。
- Compose 强制容器使用 `postgres:5432`，宿主机数据库默认映射到 `127.0.0.1:5433`；Vite 通过服务名 `api` 代理请求。生产 Compose 与生产部署文档未改动。
- 修复安全迁移在全新数据库中修改触发器依赖列时报错的问题：迁移和回滚均先移除同步触发器、修改列类型，再恢复触发器。
- 本机 `.env` 已补齐随机开发密钥且保持 Git 忽略；因 Docker Hub 拉取默认镜像时出现 EOF，本机验证使用已有的 Node.js 22 与 PostgreSQL 17 镜像覆盖，仓库默认值仍为 Node.js 20 与 PostgreSQL 16。
- 清除宿主机前后端 `node_modules` 及空的构建、覆盖率和日志目录；容器重建后依赖探针、API 与前端健康检查仍通过。
- 验证：从空 volume 一次完成全部 Migration，管理员种子执行成功；API live/readiness 和前端 HTTP 均为 200；后端 13 个 Jest 套件、56 个测试通过，ESLint 为 0 error、625 warning；前端 ESLint 为 0 error、56 warning，生产构建通过并保留 792.37 kB 主包警告；45 份文档链接、导航和 6 个模型字段覆盖检查通过。
- 边界：未启动邮件或爬虫 Worker，未连接真实邮箱、Apple 官网或代理，未验证生产环境；依赖审计仍为后端 18 项、前端 9 项。

## 下一步与阻塞

1. 用户查看新的本地 Compose 使用方式和文件体系，反馈需要调整的开发流程。
2. 按[订单状态刷新与爬虫重构方案](../planning/订单状态刷新与爬虫重构方案.md)实施持久化任务、每分钟未付款刷新、页面触发刷新、手动刷新和代理治理，并按阶段记录验证证据。
3. 按[邮件解析与处理可靠性优化方案](../planning/邮件解析与处理可靠性优化方案.md)执行本地浏览器人工流程、真实邮箱受控验证和独立测试；生产 Migration 与发布需单独授权。
4. [付款人与结算方案](../planning/付款人与内部结算方案.md)仍需确认应结、退款、部分结算、币种及审批。
5. 历史 Git 敏感资产与外部凭据轮换尚未闭环，见[处置记录](../security/敏感资产处置记录.md)。

## 2026-09-06 生产部署改造

- 生产保留宿主机 PostgreSQL 15 和共享 Nginx，Compose 只管理 API 与独立 Worker；API 仅映射到 `127.0.0.1:3001`。
- 本地 `main` 构建 `linux/amd64` 镜像与前端静态制品，制品包含工作树 tree hash、manifest、源码快照和 SHA-256 校验和；服务器不执行 npm 构建。
- 发布前完成三次数据库备份，其中最终停机备份与新字段加密密钥配套保存；在独立 PostgreSQL 17 容器恢复备份并演练两项 Migration 与存量敏感数据转换。
- 演练发现 `payment_screenshot` 已是 JSONB 时仍调用 TEXT 转换函数的问题；修复后重新演练通过，再执行生产迁移。
- 生产 13 项 Migration 全部到位；542 个 Apple ID、30 个取机人、100 个订单、4 条邮件日志行数与备份一致，受检非空敏感字段无明文残留，取机人盲索引完整。
- Nginx 配置检查通过并 reload，PostgreSQL 仅 reload 精确的 Compose 网段访问规则；两项共享服务和其他容器均未重启。
- API 本地回环与 HTTPS readiness 均为 200；邮件 Worker 完成 TLS、登录、邮箱打开和白名单过滤验证。未读样本不是订单邮件，因此不能作为真实订单入库验收。
- `PROXY_ENABLED=false` 且 `AUTO_ORDER_REFRESH_ENABLED=false`，爬虫 Worker 计划内未启动；Apple 官网同步与代理仍待单独业务验收。
- 后端 13 个 Jest 套件、56 个测试通过，Lint 0 error/625 warning；前端 Lint 0 error/56 warning且构建通过，保留约 792 kB 主包警告；45 份文档和 6 个模型字段覆盖检查通过。

## 🔧 2026-09-05 - 全项目审查与第一批基础修复

### 已完成（当前本地工作区）

- 新增并完善 `docs/planning/项目优化计划.md`，补充已实现功能、完整度、审查结论、分阶段任务、验收标准和付款人/内部结算演进方案。
- 修复取机人身份证密文被 Sequelize 格式验证误判的问题，改为从 getter 明文派生后四位和盲索引。
- 修复安全迁移后旧触发器用密文尾部覆盖 `id_card_last4` 的问题，并让存量迁移显式写入明文派生值。
- 修复 Sequelize 关联使用数据库字段名，导致隐式生成重复模型属性的问题。
- 业务 API 统一在应用层执行认证和强制改密检查；系统日志与自动刷新控制限管理员访问。
- 修复临时锁定到期后账号状态未恢复、已签发 token 却立即被拒绝的问题。
- 密码哈希改用 `bcryptjs`，移除本机 x86_64 原生 `bcrypt` 在 arm64 环境下阻断模型加载的问题。
- 修复取机人导出未携带 JWT 且硬编码 `localhost:3000` 的问题。
- 移除仪表板默认 Mock 数据与渠道“每单 8000 元”估算，金额统一使用官网解析字段；修正产品统计 SQL 错误字段。
- 邮件监听增加生产发件人白名单、显式已读开关，修复单封邮件重复完成回调、主动停止后误重连和异常重复重连。
- 爬虫增加 Apple URL 与官网返回订单号双重身份校验，将网络抓取移出事务，并在短事务内锁行、拒绝并发旧结果；取消订单数量为 0 时保留邮件权威数量。
- 独立爬虫 Worker 拓扑不再由 Web API 伪造运行状态或内存恢复成功，状态改为日志观测，恢复操作明确返回 409 并给出运维动作。
- 渠道订单统一脱敏手机号并隐藏 Apple 订单链接；独立 `/orders/:id` 页面移除演示数据，接入真实详情 API。
- 顶栏不再硬编码“邮件监听中”，在心跳接口完成前明确显示“邮件监听状态未接入”。
- 新增 `docs/archive/2026-09/2026-09-05-项目现状审查与修复报告.md`，记录完整功能盘点、架构演进理由、修复/规划/未验证边界、隔离数据库条件和付款结算模型。
- 更新数据库和 API 权威文档中与本批修复相关的契约。

### 验证证据

- 后端：12 个 Jest 套件、49 个测试全部通过；覆盖率 Statements 36.29%、Branches 30.25%、Functions 34.29%、Lines 36.66%。
- 后端 Lint：0 error、686 warning。
- 前端 Lint：0 error、56 warning。
- 全仓库格式检查：未通过，仍有 97 个历史文件不符合 Prettier；本轮已消除引号规则冲突。
- 前端生产构建：通过；主 JS 包 792.37 kB（gzip 226.95 kB），仍有大包警告。
- 文档一致性检查：通过，检查 6 个模型。
- Sequelize 模型加载冒烟：通过。
- 依赖审计：后端 18 项（6 moderate、12 high）；前端 9 项（3 moderate、6 high），尚未治理。

### 关键实现笔记

- Apple 官网支付状态与系统内部结算状态必须分离；付款人、取机人、Apple ID 和系统操作员是不同业务主体。
- 本轮未创建付款人/结算表，因为应结口径、退款责任、部分结算、币种和审批角色仍需业务确认。
- 本轮未执行任何真实数据库 Migration、真实邮件/官网请求、生产写入、提交、推送或部署。

### 下一步计划

1. 完成 Git 历史敏感数据处置和凭据轮换（需单独授权）。
2. 在隔离 PostgreSQL 中执行 Migration 的 `up → 校验 → down → up`，并补齐 API 集成测试。
3. 完成邮件持久化幂等/重试、启动与定时回溯，以及爬虫刷新租约锁和状态分级冷却。
4. 请求确认 `PAY-01`、`PAY-02` 的业务规则，再编写付款人和结算权威契约。
5. 按模块补齐核心失败分支，将核心业务覆盖率提高到不低于 70%。

### 阻塞问题

- Git 历史清理、真实凭据转换、真实数据迁移和生产发布需要单独授权与可回滚窗口。
- 结算开发缺少应结金额、调整项、退款/冲正、部分结算、币种和审批角色的最终语义。

---

## 历史入口

[2026 年 7 月开发记录](../archive/2026-07/开发进度记录.md) · [历史索引](../archive/README.md)

### 2026-09-09 订单列表修复（已发布）

- 根因：列表只读取关联档案，遗漏订单已有 Apple ID/取机人邮件快照；Apple 逐台节点同时携带总数量，造成同一商品重复展示。
- 已补齐列表/详情快照回退和取机人标签，姓名与关键词查询覆盖快照；移除订单页电话列，以官网成功更新时间替换刷新状态，增加右侧固定单行手动刷新与进度反馈。
- 官网副本按明确节点后缀、序号、数量和一致商品状态验证后合并，保留总数量；不按名称全局去重。
- 验证：42 个单元套件、355 项测试通过；邮件隔离数据库 18 项已通过；本地真实列表/关键词/姓名接口 HTTP 200，快照命中。合成浏览器验证桌面、390px 窄屏、旧列配置、单行排队/执行/成功/失败以及只读权限通过；同时修复窄屏页头与分页溢出。后续制品构建、生产迁移演练、切换与真实官网验收均通过，详见本页当前阶段。
