# 订单 TAG 授权生产发布记录

> 日期：2026-09-18（北京时间）
>
> 代码提交：`5e140b7`
>
> 生产制品：`20260917T184524Z-e64d2b8cdaa2`
>
> 验证边界：自动化权限隔离、迁移演练和生产只读技术验证通过；未修改真实用户授权，实际配置和业务验收待执行。

## 交付内容

用户管理权限弹窗支持全部订单或多个指定 TAG，按 `orders.tag` 精确匹配，与功能权限共同限制订单接口及相关统计、导出、刷新和关联操作。本人付款任务保留现有完整信息和操作，任务归属不授予完整订单访问。存量用户保留全部范围，新普通用户默认空范围；范围变更审计、版本冲突、撤权缓存清理和渠道改名授权同步一并交付。

## 发布前验证

后端 63 套／638 项通过，15 套／195 项环境门控跳过；隔离 PostgreSQL 集成 43 项通过，新增授权服务语句／函数覆盖率 100%、分支 96.66%。合成浏览器覆盖 375／768／1024／1440px 的配置及撤权流程。前后端 Lint 0 error、Vite 构建、文档检查通过，既有 warning 保留。测试未调用真实官网、付款或供应商服务。

正式发布包 SHA-256 校验通过，镜像为 linux/amd64。在本地隔离库恢复生产完整备份，再执行新增 Migration，验证 15 个存量用户全部保留 all 范围、订单和付款任务计数不变。演练库 `apple_order_mgr_tag_restore_test_20260918` 与本地敏感 dump 副本在完成后清理。

## 生产执行与证据

用户授权提交并生产部署后，于北京时间 02:48:45—02:48:54 停止三个 Apple 应用、生成停写备份、执行 `20260918000002-add-order-tag-access.js`、启动新 API、验证健康、原子切换 current 并恢复两个 Worker。应用切换约 9 秒，未触发回滚。

发布目录：`/var/www/apple-order-mgr/releases/20260917T184524Z-e64d2b8cdaa2`。旧版本 `20260917T172904Z-507ac19ace67` 保留。

受保护备份目录：`/var/backups/apple-order-mgr/20260918T0245-order-tag-access`，目录权限 700，敏感文件 600。包含发布前及停写 PostgreSQL 完整备份与校验、旧指针、环境配置备份与摘要、迁移日志、数据摘要、容器基线、健康检查和发布验证结果。

- Migration 从 38 增至 39；订单 37、用户 15、Apple ID 1,926、取机人 1,862、付款任务 37。
- 迁移前后订单、用户原有字段、权限集合及付款任务摘要一致；15 个存量用户范围均为 all。
- 公网／内网 readiness 为 healthy、db=ok；API 和两个 Worker 使用新镜像，restart=0，API healthy；启动窗口错误日志检查零命中。
- 使用现有有效管理员会话进行只读检查：auth/me、用户 TAG 候选（8 项）、订单列表、渠道和仪表板统计均为 200；匿名 TAG 候选接口为 401。未创建会话或输出访问凭据。
- 公网首页、JS 与 CSS 字节摘要匹配新制品。app.env 摘要不变；其他项目容器 ID 和镜像不变，未重启共享 PostgreSQL 或 Nginx。

## 回退与业务边界

旧应用不了解订单范围限制。若发布后已经配置受限用户，禁止直接切回旧应用扩大其可见范围；必须先制定保留访问限制的回退方案。未配置任何受限用户时，可在受控停写窗口使用旧发布目录重建三个应用并原子切回 current，保留新增数据库结构。不得直接 down 或覆盖恢复生产数据；多个活动批次存在时 down 会被旧唯一约束拒绝。

本次没有修改实际账号的权限或 TAG 配置，没有手动刷新真实订单、付款或调整任务归属。生产只读检查与隔离权限测试不替代业务人员按真实授权配置后的验收。代码及发布记录仅本地提交，未 git push。
