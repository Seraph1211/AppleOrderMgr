# AOS 本地实现与验证记录

> 状态：历史归档（本地开发阶段记录，Windows 实机验收待完成）
>
> 最近核对：2026-09-10
>
> 基线：`codex/aos-file-ingestion` 未提交工作树，起点 `main@d052678`
>
> 适用范围：本地 API、前端、隔离 PostgreSQL 与 Windows 采集器源码和测试包；不代表生产发布。

## 实现与确认

已实现设备登记和凭证轮换、GUI 目录配置、后台服务与托盘、加密本地队列、追加／重写／重命名与尾行恢复、HTTPS 可靠回执、服务端异步入库、邮件／AOS 来源切换及当天补录、管理员记录处理与来源追踪。复用订单首次刷新和付款任务登记，事务失败整体回滚。

用户确认的六项要求均纳入实现：重复订单只去重留痕；3–4 台 x64 电脑各配置本机目录；按附件格式并兼容未知写文件方式；唯一姓名／手机号匹配且联系资料不冲突才关联、不自动建 Apple ID 档案；已取得许可的本地队列跨日首次上传自动恢复并去重；来源开关覆盖自动、重试和来源人工入口，独立人工订单／Excel 规则保持。

附件仅做离线内存解析，当前解析器结果为 11 行、11 个订单、22 台商品、0 个格式问题。没有把真实附件、订单或密码写入测试数据库、仓库或分发包。

## 验证结果

| 检查 | 本次结果与边界 |
| --- | --- |
| 后端全量回归 | 48 个套件、453 项通过；7 个外部环境门控套件、103 项跳过 |
| AOS／邮件专项 | 独立 PostgreSQL 与 HTTP、解析及扫描共 4 个套件、65 项通过；其中 AOS 17 项、邮件处理 13 项、解析 15 项、扫描 20 项 |
| AOS 核心覆盖率 | 5 个核心文件合计语句 83.12%、分支 72.52%、函数 89.01%、行 84.83%；共用订单创建核心语句／行 100% |
| 采集器核心 | 34 项通过：真实本地文件事件、SQLite 队列及合成 HTTPS 响应；不代表 Windows 服务／DPAPI 实测 |
| 正式迁移 | 独立数据库执行两条新迁移 up/down/up，历史订单保留、邮件来源及未终态重试资格回填、默认邮件通过 |
| 后端 Lint | 0 错误、1607 警告，包含既有和新增命名／魔法数字等警告，未关闭规则 |
| 前端 Lint／构建 | 0 错误、41 警告；Vite 构建通过，保留大包提示 |
| 文档检查 | 68 份文档全部可达，本地链接与锚点通过；29 个模型字段覆盖通过，不代替真实表结构校验 |
| Windows 构建 | .NET SDK 10.0.401 构建 win-x64 自包含单文件，运行时 10.0.12；未签名 |
| 页面操作 | 隔离合成管理员验证来源预览和切换、等待补录、暂停入库、设备与记录表、敏感内容默认掩码、同订单双来源和 AOS 记录链接、未知门店／待关联／TAG 展示 |
| 窄屏范围 | 实际 DOM 宽度 780 px 已检查；工具未成功应用 390 px，不能声明手机宽度验收 |

最终专项测试补充了直接邮件保存入口的重复状态及日期断言：来源暂停返回受控错误，不额外生成误导性的失败日志；重复成功记录为 `superseded`，来源关系为 `duplicate`。首次补测暴露该问题及后续状态依赖，修复后 65 项全部通过。

主要复现命令在[测试指南](../../testing/测试与验收指南.md#aos-文件采集与来源切换验收)，采集器命令在[构建说明](../../../collector/README.md)。本次临时日志保留在执行机器 `/tmp/aos-backend-tests-final.log`、`/tmp/aos-db-tests-final.log`、`/tmp/aos-collector-tests.log`、`/tmp/aos-migration-roundtrip.log`；临时文件可能被系统清理，以上表格保留执行结论。

## 本地状态与制品

本地开发库已应用 `20260910000001-add-aos-ingestion.js` 和 `20260910000002-preserve-ingestion-history.js`；默认来源仍为 email。为文档检查只读挂载采集器 README，重新创建了本地 API，健康检查通过；邮件与爬虫 Worker 未主动重启或启动。

两台临时 UI 容器已停止并删除。专用合成测试库 `aos_ingestion_test_20260910` 与 `aos_migration_test_20260910` 保留，未接入真实邮箱或官网。

本地测试包：`collector/artifacts/AOS采集器-1.0.0-win-x64.zip`，包含程序、安装说明、依赖许可、第三方声明及 SHA-256 清单（13 个文件，ZIP 完整性及清单逐项验证通过）；该目录被 Git 与 Docker 构建忽略。安装程序 SHA-256：`cb47069d5348e4a7d14337aec596045f3824776c06266a2567d3165a8f6780d0`。构建成功只证明编译／打包，不证明安装、服务启动或真实上传成功。

没有提交、推送或生产部署；开始前已有的未提交文档保留在开发分支。

## 待完成的实机和业务验收

目标机器由用户确认：Windows 10 专业版 22H2、OS Build 19045.6456、x64、管理员权限。用户已允许本次 RDP 证书提示继续连接；但远程窗口自动化仍报找不到窗口，点击和键盘未能可靠作用到远程桌面。已请求将 Windows App 远程连接退出全屏、切成普通窗口，待恢复操作后继续。

尚未安装或运行本次 EXE；LocalService 目录权限、DPAPI、命名管道、开机与登录任务、升级／卸载保留队列、真实软件文件写法和 GUI 实机显示均待验证。3–4 台设备峰值负载、真实 HTTPS 接入、官网同步和付款业务闭环也未验收。按[可复用验收项](../../testing/测试与验收指南.md#aos-文件采集与来源切换验收)逐项记录，不以本地测试替代。
